/master_tboxes_ingested.parquet
/lineage_replaced.parquet
/pilot_genomes_fetch_log.jsonl
/pilot_genomes
# P2-10c′-fetch: the DVC-tracked production genome dir + its resumable fetch log (large,
# never committed to git). The provenance sidecar production_genomes.provenance.json + the
# audit report data/processed/audits/production_fetch_report.json ARE committed deliverables.
/production_genomes_fetch_log.jsonl
/production_genomes
/homolog_db
/production_annotations
# P3-16: the benchmark contig set, the two Stage-1 window-logit archives (~56 MB each) and
# their Stage-2 logits + candidate tables. Large and model-derived; the committed, replayable
# artifacts are the folded per-item score table (reports/p3/two_stage_precision_items.json)
# and the graded report (reports/two_stage_precision.json).
/p3_16
