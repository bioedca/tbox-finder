# ADR-0005 — Non-circular evaluation design

- **Status:** Accepted (user sign-off 2026-07-11; CLAUDE.md §7 item 2)
- **Date:** 2026-07-11
- **Deciders:** bioedca (project owner)
- **Phase:** P0 (seed ADR)
- **Delegated from:** PRD §2.3 (formal acceptance gates — GATE-1, GATE-2, and the gate-precedence / blinded-freeze policy), §5 (the circularity problem & the five non-circular mechanisms), §6 (Stage-1↔Stage-2 integration + overlapping-window reconciliation + strand resolver), §9.1 (four negative/decoy classes + mining protection), §11 (recalibration stack order, aux-loss weighting), §12 (evaluation & validation framework — ECE, FDR, OOD-drift rule), §13.1 (scan decision machinery)
- **Supersedes / superseded by:** none
- **Related:** ADR-0001 (architecture & aims — the non-circularity principle this ADR operationalizes end-to-end), ADR-0002 (environment & ML stack — the two Stage-1 checkpoints [production + class-II-CM-naive] whose gate attribution D9 fixes; the RiNALMo parity gate whose swap-trigger margins D17 pins), ADR-0003 (cluster & scan ops — the governing GTDB release, minimum-viable scan, and per-corpus GPU/wall ceilings the GATE-1/GATE-3 scan runs against), ADR-0004 (split & leakage policy — the homology-cluster / leave-clade-out / literature-anchor partition and the variant→parent→fold provenance every GATE-1 arm is resampled and blocked on; the GATE-4 0.80 floor, a sibling delegated default), ADR-0006 (validation decision rule & tiering — the §13.3 confirmed-novel rule + the model-independent homolog-search thresholds that make the open-scan precision orthogonal; **pending P0-29**)

This ADR pins the **evaluation contract for the project's central methodological result — the generalization claim (GATE-1, "beat `cmsearch` at matched precision on held-out clades and on synthetic divergence") — and the calibration/FDR gate (GATE-2)** that decides whether the system is sound. It is the document that operationalizes PRD §5, the **#1 design constraint**: both the training labels and the naïve ground truth are CM-derived, so without a pre-registered non-circular design a model can at best re-learn what the CM already sees. Every decision below is a **rule**; the numeric **values** the PRD delegates here are stated as **defaults** under the PRD §2.3 precedence carve-out (the ADR-pinned value is authoritative; a recalibration still requires ADR re-sign-off, CLAUDE.md §7 item 2) and are **blinded-frozen at P0** — they may not change after P4 unblinding. Three numeric values are **deferred by design** and pinned by later P0 steps that amend this ADR: the **per-bin / per-arm minimum-real-homolog N** (P0-26), the **OOD-ECE min-N admissibility floor** (P0-27), and the **authored magnitude-rationale text** for each gate default (P0-28); this step pins the rules those steps fill in.

---

## Context

The headline result is a **generalization claim** and the **precondition for the flagship distribution-expansion result** (GATE-3). Its trustworthiness rests entirely on the evaluation being non-circular (PRD §5): a CM-trained, CM-scored model cannot demonstrate "finds what CMs miss" unless the benchmark is explicitly constructed to reach beyond the CM. PRD §5 defeats the circularity with five pre-registered mechanisms — (1) held-out-clade extrapolation, (2) synthetic-divergence stress, (3) class-II-CM-naive anti-mimicry, (4) PU framing for model-unique open-scan hits, (5) orthogonal model-independent confirmation — and PRD §2.3/§12/§13.1 delegate the operationalizing **thresholds and decision rules** to this ADR. The split that feeds all of this (homology-cluster / leave-one-order-out / literature-anchor, with variant→parent→fold provenance) is pinned in ADR-0004; the discovery-side orthogonality (the CM-free de-novo covariation and model-independent homolog search) is pinned in ADR-0006. This ADR sits between them and owns the **grading**.

Two facts drive the shape of the decisions. **First**, the catalogue is ~90 % Firmicutes across 29 phyla (P0-12), so only **leave-one-order-out** is statistically well-powered (≈31 orders with ≥20 positives; PRD §12) and the gated statistic must be **macro-averaged across held-out orders** (micro-averaging would let Firmicutes dominate) with **block-level** (cluster / held-out-order) resampling carrying the phylogenetic-exchangeability caveat into every CI. **Second**, the leave-clade-out positives are themselves CM-derived and therefore cannot contain T-boxes below `cmsearch`'s gathering cutoff — the **genuinely CM-invisible region is reached only by the synthetic-divergence arm and the §13 discovery campaign**, so those two arms carry an outsized evidential load and need their own power budget and structural-realism controls.

**The low-identity claim region is literature-sourced, not chosen for convenience.** The synthetic-divergence and lowest-identity-bin arms make their claim precisely where sequence-based homology search is documented to fail. According to PubMed, Freyhult, Bollback & Gardner (2007) benchmarked BLAST, FASTA, HMMer and Infernal on curated ncRNA sets and found the most popular (sequence-based) methods are often the least accurate on divergent homologs — the "genomic dark matter" region that motivates structure-aware search [PMID:17151342; DOI:10.1101/gr.5890907 (accessed 2026-07-10)]; Menzel, Gorodkin & Stadler (2009) independently concluded that homology search beyond the reach of BLAST "is not at all a routine task" and depends on curated structural alignments [PMID:19861422; DOI:10.1261/rna.1556009 (accessed 2026-07-10)]. These two agreeing sources ground the **fixed** low-identity bin edges in D1 (the edges isolate the region both papers identify as where sequence homology collapses); they are cited for the qualitative divergence-sensitivity finding — no specific numeric cutoff is transcribed from either paper's tables.

---

## Decision

### D1. Pre-registered identity bins (fixed edges, Freyhult-sourced) + controlled-divergence levels (locked rule; default values)

Held-out positives are binned by **% nucleotide identity to the nearest seed (training-fold) member** into **fixed, pre-registered edges — never quantile bins** (quantile bins would make the claim region data-dependent, PRD §5):

> **default edges: `<50` | `50–70` | `70–85` | `85–100` %** identity.

The **"lowest-identity bins" = the bottom two** (`<50` and `50–70`, i.e. the `<70 %` region). The edges are **fixed for the life of the project** and positioned to isolate the low-identity region where sequence-based homology search is documented to lose sensitivity and where the CM-vs-model gap is expected to open [PMID:17151342 / DOI:10.1101/gr.5890907; corroborated PMID:19861422 / DOI:10.1261/rna.1556009 — accessed 2026-07-10]. A cell with fewer than the **per-bin min-N** (D18, value at P0-26) is **pooled upward per the sub-min-N pooling rule** (adjacent-bin pooling toward higher identity, disclosed) or **reported-not-gated**, never silently passed.

**Controlled-divergence synthetic variants** (mechanism 2) are generated at a **fixed number of target-identity levels** — **default: one level centered in each of the four bins plus one deep `<40 %` stress level (5 levels total: ≈ {40, 55, 65, 78, 92} % identity)** — by structure-preserving mutation along the CM consensus, with **each variant's parent provenance pinned** (so ADR-0004's variant→parent→fold no-leakage guard applies). The level set and count are frozen here; recall-vs-divergence is plotted for the model and for `cmsearch` on identical inputs.

### D2. Canonical `cmsearch` operating point (locked)

The head-to-head baseline operating point is the **Rfam RF00230 GA (gathering) threshold for class I** and the **published `TBDB001.cm` class-II threshold for class II** (PRD §4/§2.3). A **full E-value sweep is reported as a supplementary PR curve** (not the gated point). `cmsearch`'s **build/gathering cutoff is reported next to the matched-precision threshold** on every GATE-1 figure, so the CM-invisible region (below that cutoff, reachable only by mechanism 2 + §13) is explicit. Infernal ≥ 1.1.4; a filter-off (`--max` / `--nohmm`) arm is reported per bin to attribute failures to the HMM pre-filter vs the CYK/structure stage (PRD §5, [PMID:24008419]).

### D3. Scan decision machinery — Stage-1 threshold, locus-construction, Stage-2 operating point (locked rules; values frozen at the phase gate)

Pre-registered as **rules** here; the *values* are frozen at the phase gate (§13.1) and never tuned on the test set:

- **Stage-1 threshold** = the **most-liberal (highest-recall) value that retains a pinned per-locus recall floor** (default **≥ 99 %**, measured on individually-windowed leave-one-order-out positives).
- **Locus-construction rule** (an explicit spec): the **overlapping-window logit-reconciliation operator** — at window 1024 / stride 512 every interior nucleotide is covered by ≥ 2 windows; the per-position 8-class logits from all covering windows are **averaged in log-sum-exp space then arg-maxed into one per-position prediction** *before* along-sequence element merging (a seam-free operator, so the Stage-1 recall floor holds under overlapping tiling and boundary IoU is not a 512-grid artifact) — then per-class-vs-global threshold scope, minimum span, gap-merge distance, **recall-favoring** required-element co-occurrence (mandating canonical elements would re-impose the §5/§13.3 bias), and flank size. **Training positives are offset-augmented** (random window-phase offsets) for phase robustness; contig ends are zero-flanked and flagged.
- **Stage-2 operating point** = the **most-liberal calibrated posterior with empirical false-discovery proportion ≤ 10 % (point estimate)** on the consensus null (GATE-2 then certifies this FDR on its bootstrap-CI upper bound, D12).

Reported metrics always distinguish **Stage-1-only**, **two-stage**, and **two-stage+orthogonal**.

### D4. GATE-1 two-part effect-size bar + min-N-conditioned fallback (locked rule; default values; blinded-frozen)

GATE-1 recall (vs `cmsearch` at **matched precision**, D7) passes an arm iff:

> **point estimate ≥ +10 pp recall AND block-resampled bootstrap-CI lower bound > +5 pp** (the pinned positive floor).

The weaker clause **"point ≥ +10 pp AND CI lower bound > 0"** is admissible **only as an explicit, disclosed min-N-conditioned fallback** (invoked when an arm's real-positive N is below the D18 per-arm min-N so the tighter CI cannot be estimated — never as a convenience loosening). Recall is graded on the **two-stage** system with **Stage-1-only reported alongside** (exception: the class-II anti-mimicry sub-arm is Stage-1-only, D9).

The **+10 pp / +5 pp** values are **defaults** under the precedence carve-out and **blinded-frozen at P0**: each carries a **documented magnitude rationale** — a smallest-effect-of-interest / effect-size argument or a power calculation at the D7 pinned decoy prevalence and the D18 per-bin min-N — **authored before any P4 result at P0-28** and frozen; any *pre-P4* change needs ADR re-sign-off (CLAUDE.md §7 item 2); no *post-P4* change is permitted.

### D5. GATE-1 resampling & averaging — block-level + macro-average (locked)

All GATE-1 (and per-order ECE) confidence intervals are **resampled at the homology-cluster / held-out-order (block) level, not the record level**, because the positives are phylogenetically correlated (ADR-0004; PRD §12) — this carries the exchangeability caveat into the gate CI. The gated leave-clade-out statistic is **macro-averaged across held-out orders** (not micro, which the ~90 %-Firmicutes corpus would dominate); the **per-order distribution is reported alongside**. Where N is small, the model-vs-`cmsearch` recall difference is additionally read with **small-N-appropriate inference** (an exact/permutation test or a Bayesian recall-difference posterior) beside the bootstrap CI (PRD §5).

### D6. GATE-1 headline-certifying arms + AND-of-powered-arms combination rule (locked)

The two-part bar (D4) is **required on**:

1. the **lowest-identity bins pooled** (bottom two, D1), **and**
2. a **pooled non-Firmicutes-order subgroup and/or the independent literature anchor** (arm (c), PRD §7.1) — so *beyond-Firmicutes* generalization is **certified**, and

3. **recovery of held-out class II from the class-II-CM-naive checkpoint** (the anti-mimicry sub-arm, D9).

**Combination rule = logical AND of the powered arms.** GATE-1 passes iff **every arm that is powered** (clears its D18 min-N) passes its bar. An arm below min-N is **"reported-not-gated"** — removed from the AND and **disclosed**, never silently counted as a pass. **Floor:** a GATE-1 pass **requires** arm (1) powered-and-passing **AND** at least one of {non-Firmicutes-order subgroup, arm (c)} powered-and-passing. If the **anti-mimicry sub-arm (3)** falls below the D9 min-graded-evidence floor, GATE-1 is **reported-not-gated for anti-mimicry → CLAUDE.md §7 stop-and-ask** (PRD §2.3 branch 2), not a silent waiver. The **synthetic-divergence arm being powered is a pre-registered precondition for the §1 CM-invisibility claim** (D8); unmet, that claim rests on the §13 campaign, disclosed. *(These GATE-1 arms (a)/(b)/(c) are distinct from the ADR-0004 §9.2 split-ladder (a)/(b)/(c).)*

### D7. GATE-1 closed-benchmark precision — single pinned decoy prevalence + sweep + arm-(c) basis (locked rule; default value; blinded-frozen)

GATE-1 precision is computed on the **closed leave-clade-out + synthetic-divergence benchmark** using the **four §9.1 decoy classes as labeled negatives at a single pinned prevalence** (so precision is well-defined and the §5-mechanism-4 PU framing never enters it, D10):

> **default pinned benchmark decoy prevalence: 100 : 1 (decoy : positive)** — deliberately **distinct from** the ~10 : 1 training seed ratio (§9.1) and from the ~10³–10⁴ : 1 genome-scale prevalence.

The **+10 pp gap is additionally reported as a prevalence-sensitivity sweep** spanning **10 : 1 → 10² : 1 → 10³ : 1 → 10⁴ : 1** (toward genome scale), so the gap's prevalence-robustness is visible. Because the comparison is **matched-precision** (model recall read at the threshold where model precision equals `cmsearch` precision, both scoring the identical negative pool), the pinned prevalence sets the operating point, not the fairness of the comparison. **Arm (c)'s precision** uses the **§9.1 decoy classes over the anchor's host contigs at the same pinned prevalence**, so the beyond-Firmicutes gated arm has a defined precision denominator. **Arm-(c) sourcing fallback:** if the P0-16 literature anchor / P0-17 additional class-II set falls below the D18 min-N, the P0-26 audit invokes the **arm-(c) sourcing-fallback trigger** (source more independent positives, or drop arm (c) from the AND and rest the beyond-Firmicutes certification on the non-Firmicutes-order subgroup) as a **§7 stop-and-ask**. The pinned prevalence is a **blinded-frozen default** (magnitude rationale at P0-28).

### D8. Synthetic-divergence arm — generator validity, structural realism, model-side leakage control, power budget (locked)

Before synthetic bins count toward GATE-1, **all** of the following hold (PRD §5 mechanism 2):

- **Generator-validity (structural-realism, not detectability alone):** synthetic variants at identity *X* must match **real** low-identity homologs at *X* on **both** (i) `cmsearch` detectability **and** (ii) a **structural-realism axis** — compensatory-vs-disruptive mutation distribution, MFE, and base-pair distance to the consensus. The per-bin `cmsearch` failure mode (HMM-filter pre-discard vs CYK) is reported with the D2 filter-off arm.
- **Model-side leakage control:** each variant's parent provenance is pinned (ADR-0004 variant→parent→fold) and GATE-1 **requires no train-vs-test recall gap at matched divergence** (or a held-out-clade checkpoint matching production recall) as an **acceptance condition** — so consensus-anchored generation cannot smuggle in "easiness" correlated with the CM-derived training signal.
- **Power budget → gated-vs-reported-not-gated:** P0 audits, **per leave-one-order-out round and pooled**, the per-%-identity-bin N of real held-out positives and the count of real low-identity homologs; **below the D18 minimum-real-homolog N the synthetic arm is reported-not-gated** and GATE-1 rests on the real bins (under their min-N floor) + the §13 campaign. A **powered synthetic arm is the pre-registered precondition for the §1 CM-invisibility claim** (D6).

### D9. Class-II anti-mimicry sub-arm — Stage-1-only, construction-powered, min-graded-evidence floor, within-phylum control (locked)

Anti-mimicry (mechanism 3) tests whether the **class-II-CM-naive Stage-1 checkpoint** (trained with class-I-style / shared-element labels only, withholding `TBDB001.cm` — ADR-0002/§8/§10.1) still recovers held-out translational T-boxes:

- **Scored Stage-1-only** on the naive checkpoint — **never routed through the production Stage-2** (TBDB-class-II-trained), which would rescue class-II hits and confound the test.
- Graded by a **recovery metric carrying the same block-resampled min-N floor + CI as the recall arm (D4/D5)**, plus a **within-phylum-homology control** that separates anti-mimicry from within-Actinobacteria memorization.
- The **18-record single-phylum natural set (PRD §7.1) is at chronic risk of sub-min-N**, so the sub-arm is **augmented by a construction-powered synthetic-class-II recovery measure** (above min-N by construction, built in P2) **and** by the **P0-sourced additional independent non-Actinobacteria class-II positives** (P0-17).
- A GATE-1 pass requires a **pinned minimum of graded anti-mimicry evidence** (the D18 min-graded-evidence floor); below it, **reported-not-gated for anti-mimicry → §7 stop-and-ask** (D6), never a silent waiver.

### D10. §5-mechanism-4 PU-framing scope — open-scan only (locked)

The **Positive-Unlabeled framing applies only to the open §13 discovery scan, never to GATE-1's closed benchmark.** On the discovery scan, CM-negatives are treated as **unlabeled, not true-negative**: a **model-unique hit is not auto-scored a false positive**, and its **precision is established only by orthogonal evidence** (§13 / ADR-0006, P6) — never by "another CM agrees." This framing **never enters GATE-1's closed-benchmark precision denominator** (there the four §9.1 decoys are labeled negatives at the D7 pinned prevalence). This scoping is what keeps GATE-1 precision computable while leaving the open scan free to surface CM-invisible novelty.

### D11. GATE-2 in-distribution ECE — named posterior, binned estimator, recalibration stack (locked rule; default value; gated at P3 exit)

The **recalibration stack order is pinned**: **train → temperature-scale on a disjoint calibration split [arXiv:1706.04599] → prior-shift to the deployment prior via a Saerens/Elkan log-odds correction**. GATE-2's calibration sanity gate is measured on the **named posterior = the temperature-scaled posterior *before* the deployment prior-shift, at the in-distribution split's own prevalence** — a **machinery-failure check**, not a deployment-prevalence check (a prior-shifted posterior is miscalibrated by construction at benchmark prevalence, so the **prior-shifted / deployment-prevalence ECE is reported separately, non-gated**).

> **default gate: in-distribution ECE ≤ 0.05, gated at P3 exit** (catching a calibration-machinery failure before the P5 scan; the FDR half of GATE-2 is graded at P5, D12).

**Binned-ECE estimator (pinned):** **equal-mass (equal-frequency) bins, default 15**, with a **debiasing correction**, on the positive-class posterior. Equal-mass over equal-width because equal-width bins are unstable in the sparse high-confidence region. The 0.05 default is **blinded-frozen** with its P0-28 magnitude rationale.

### D12. GATE-2 genome-scale FDR — FDP CI-upper-bound ≤ 10 % on the consensus null (locked rule; default value; gated at P5)

Genome-scale FDR uses **three nulls** (distinct from §9.1's four training-negative classes): a **structured-RNA-retaining target-decoy run as the PRIMARY estimator** [PMID:17327847]; a **dinucleotide-shuffle null as a validated optimistic lower bound** (must first reproduce `cmsearch` decoy hit-rates); and a **reversed-sequence** null (reverse, **not** reverse-complement — RC is useless against an RC-equivariant scanner) as a composition-exact control.

> **default gate: false-discovery *proportion* with bootstrap-CI upper bound ≤ 10 %** of the **pre-orthogonal candidate table** (denominator = post-Stage-2 candidate count) on the **consensus (most-conservative / highest-of-three) null**.

Gated on the **CI upper bound** because the max-of-three handles between-null but not within-null sampling error. The **primary target-decoy null additionally carries an empirical-calibration check** (held-out known-negatives, or agreement with the reversed control within tolerance) symmetric with the shuffle-null check, **before GATE-2 is graded**. **Expected false hits / Mb / genome is reported as a separate density** (not the gated quantity); retained recall on divergent loci is reported at the operating point. The 10 % default is **blinded-frozen** (P0-28 rationale). **Conformal prediction enters only as a reported held-out-clade empirical-coverage diagnostic** [arXiv:2107.07511] — both operating points are set by the D3 recall-floor and FDP ≤ 10 % rules (not a conformal α), and clade-holdout breaks exchangeability, voiding the nominal guarantee.

### D13. OOD-ECE / drift decision rule + calibrated-negative-PASS conditions (locked rule; min-N floor deferred to P0-27)

A leave-clade-out / OOD ECE ≤ 0.05 is likely infeasible under clade shift, so **OOD ECE is reported, not gated**, and adjudicated by a **decision rule**:

- Estimated with a **small-N-robust OOD estimator** — a proper-scoring-rule decomposition or a smoothed/kernel calibration estimator, with bootstrap CIs — **distinct from the D11 in-distribution binned estimator** — subject to a **pinned min-N admissibility floor (value at P0-27)**.
- A per-corpus result is a **"calibrated-negative PASS" only if (i)** its nearest-relative leave-clade-out ECE (with CIs) meets a **pinned drift bound**, **(ii)** it clears **min-N**, **and (iii)** it clears a **corpus-specific *detection-power floor*** — an extrapolated recall@matched-precision at the corpus's phylogenetic distance and/or a **synthetic-Tier-2N spike-in recovery** test — so a well-calibrated-but-*blind* model cannot earn a bounded-distribution claim. Otherwise **"sensitivity-bounded / inconclusive negative *by rule*."**
- A **sub-min-N or zero-positive corpus** (e.g. Archaea/DPANN, where no OOD ECE is computable) is inconclusive **unless** stated **nearest-relative extrapolations** (an ECE-vs-phylogenetic-distance *and* a recall/power-vs-phylogenetic-distance regression, both with **prediction intervals**) clear **both** the drift bound **and** the detection-power floor. Near-zero-prior corpora carry their **own deployment prior + per-corpus FDR null** (PRD §7.2). Per-corpus verdicts roll up to one GATE-3 outcome per the PRD §2.3 project-level rollup (pinned in ADR-0006).

### D14. Hard-negative mining spare-rule + Tier-2N probe (locked; phase-conditioned)

All known T-box loci (**the full union prior + the run's own training positives + flank**) are **masked from every negative/decoy pool** (matching the §13.3 clade-level null's masking reference), and residual contamination against that union denominator is reported. Because a CM-missed **non-canonical (Tier-2N)** T-box is unknown to masking and fails any canonical predicate, the **mining-exclusion rule is a spare rule, not keyed to canonical architecture**: a candidate is excluded from the hard-negative-mining pool if it passes **relaxed-architecture detection OR any-helix R-scape covariation OR downstream-aaRS synteny** (the three *model-independent* disjuncts, sufficient for the **P2 Stage-1 mining loop** where no Stage-2 exists yet) **OR**, at the **P3 re-mining round once Stage-2 exists**, a **high Stage-2 posterior**. The **5′UTR / tRNA-adjacent leader pool is retained** (the hardest, most-useful hard negatives). A **Tier-2N probe set** (non-canonical + synthetic-Tier-2N positives) is evaluated **each mining round**, and a per-round **recall drop on it halts/rolls back** the iteration — so aggressive mining cannot directionally train the production scanner to reject the flagship Tier-2N class; the worst case is directionally-bounded Tier-2N sensitivity, not an invalid generalization claim.

### D15. Strand resolver + strand-robustness diagnostic (locked)

Caduceus-PS is strand-agnostic, so a **strand-resolver** orients each locus from the **predicted element order** (Specifier/Stem I → antiterminator/terminator), populating the §13.1 strand column. The **RC hidden-state combination ablation is constrained to a directionality-preserving (non-averaged) form** (an order-destroying average would defeat the resolver). **Ambiguous loci** (single confident element, or scrambled order) are **flagged low-order-confidence and carried through on both strands**; the resolved strand is corroborated by **strand-specific model-independent signals** (R-scape covariation and R2DT architecture pass only on the correct strand — §13.2/§13.3(c)). A **strand-robustness diagnostic** (fraction of confirmed loci tier-invariant to strand re-resolution) is reported, so a mis-resolution degrades to a **bounded false negative on divergent loci, never a false-novelty claim**. The low-order-confidence / both-strand-carry-through fraction is reported at the sizing gate (concentrated on divergent loci).

### D16. Stage-2 multi-task aux-loss weighting + with/without-aux GATE-2 check (locked rule; default method)

Stage-2 trains a binary T-box head + boundary-refinement + regulatory-mode + auxiliary specifier/amino-acid/tRNA-family heads with a **pinned weighting method**:

> **default: fixed manual task weights** (binary head weighted to dominate), with **uncertainty-weighting (Kendall-style) as the pre-registered alternative** if the fixed weights underperform on the validation ladder.

A **with/without-aux check** confirms the aux heads **do not degrade the calibrated primary head's GATE-2 grade**; the aux-loss weight is a Hydra `--multirun` sweep axis (PRD §11), promoted via the validation ladder (never the test set).

### D17. RiNALMo → RNA-FM swap trigger margins (locked; numeric defaults; blinded-frozen)

The RiNALMo Stage-2 is swapped for RNA-FM only under the PRD §10.2 conditions; the two numeric margins the PRD delegates here are pinned as **defaults for sign-off**:

- **(c) [P3–P4] calibration margin:** swap if RiNALMo's **post-calibration leave-clade-out ECE exceeds RNA-FM's by > 0.02 (absolute ECE)**, sustained across the held-out-order distribution (block-resampled).
- **(d) [P4] discovery-metric margin:** swap if RiNALMo transfers worse on the primary discovery metric — **leave-clade-out recall@matched-precision by > 3 pp, or AUPRC by > 0.03** — even if ECE matches.

The pre-P3 RiNALMo forward-throughput probe (condition (b)) is **advisory-only**; the binding latency decision is frozen at the P5 sizing gate (ADR-0003), and a crude "clearly-hopeless" pre-P3 reading is a §7 stop-and-ask, not an auto-switch. **On a swap, the substituted RNA-FM Stage-2 must clear the *absolute* GATE-1 (+10 pp point / +5 pp CI-floor, D4) and GATE-2 (in-distribution ECE ≤ 0.05, D11) thresholds and re-pass the P4→P5 go/no-go on its own P3/P4 numbers before entering the GATE-3 scan** (PRD §2.3/§10.2) — the shipped backbone's gates are never inherited from RiNALMo. The 0.02 / 3 pp / 0.03 margins are **blinded-frozen defaults** (recalibration → ADR re-sign-off).

### D18. Delegations — values pinned by later P0 steps / elsewhere (locked map)

- **Per-bin / per-arm minimum-real-homolog N** (D1/D4/D6/D8/D9) — the floor below which an arm is reported-not-gated and the weak-clause fallback is admissible — **pinned by P0-26** (GATE-1 power-budget audit), which **amends this ADR** and renders the min-N-reachability verdict + arm-(c) sourcing-fallback trigger for the P0-16/P0-17 sets.
- **OOD-ECE min-N admissibility floor** (D13) — **pinned by P0-27** (OOD-ECE min-N coverage simulation), which amends this ADR.
- **Authored magnitude-rationale text** for each blinded-frozen default (+10 pp / +5 pp — D4; benchmark decoy prevalence — D7; ECE ≤ 0.05 — D11; FDR ≤ 10 % — D12; the RNA-FM margins — D17) — **authored at P0-28** and blinded-frozen; this ADR pins the defaults and the freeze policy, P0-28 pins the *why*.
- **Governing GTDB release + union novelty prior** (used by every GATE-1/GATE-3 novelty determination) — the release is pinned in **ADR-0003 (Amendment A1)** and the union prior is **frozen at P0** (P0-14); this ADR consumes them, does not re-pin them.
- **GATE-4 segmentation floor (≥ 0.80)** and the **split/precedence/variant-provenance contract** — pinned in **ADR-0004**; **§13.3 confirmed-novel rule + model-independent homolog-search thresholds + project-level rollup** — pinned in **ADR-0006** (pending P0-29).

---

## Consequences

- **The generalization claim is falsifiable and non-circular by construction.** Every GATE-1 number is block-resampled, macro-averaged, matched-precision, and certified beyond Firmicutes; the CM-invisible region is reached only by the powered synthetic arm and the §13 campaign, both explicitly gated or explicitly disclosed-as-resting-on-§13.
- **Under-power is a first-class, non-silent outcome.** The AND-of-powered-arms rule + reported-not-gated + the §7 stop-and-ask (D6/D8/D9) mean an arm can never be quietly dropped or loosened to manufacture a pass; a min-N shortfall routes to PRD §2.3 branch 2 (re-power or the resource-paper path), not to a downgraded claim.
- **Two numeric values and all magnitude rationales are deliberately deferred** (D18) so the min-N floor is measured (P0-26/P0-27) and the rationales are authored blinded (P0-28) — but the *rules* that consume them are locked now, so those steps fill in numbers into a fixed frame.
- **Calibration is a machinery gate, not a deployment claim** (D11): the named-posterior ECE catches a broken calibration head before the scan, while the honest OOD/deployment miscalibration is reported and adjudicated by the drift rule (D13), so a well-calibrated-but-blind model cannot earn a bounded-distribution claim.
- **Risk owned:** the fixed bin edges (D1) and the pinned benchmark prevalence (D7) are judgment calls frozen before results; if P0-26 finds the lowest-identity bins are chronically sub-min-N, the synthetic arm carries the CM-invisibility claim and its generator-validity budget (D8) becomes the load-bearing control — a dependency this ADR makes explicit rather than hiding.

## Related documents

- **PRD.md** §2.3, §5, §6, §9.1, §11, §12, §13.1 (delegating sections).
- **ADR-0001** (aims / non-circularity principle), **ADR-0002** (two Stage-1 checkpoints; RiNALMo parity gate), **ADR-0003** (governing GTDB release; minimum-viable scan; compute ceilings), **ADR-0004** (split & leakage policy; GATE-4; variant→parent→fold), **ADR-0006** (§13.3 rule; model-independent homolog search; rollup — pending P0-29).
- **Amended by:** P0-26 (min-N), P0-27 (OOD-ECE min-N floor), P0-28 (magnitude-rationale text).

## Cross-reference impact list

- **P0-26** consumes D1/D4/D6/D7/D8/D9/D18 and amends this ADR with the per-bin/per-arm min-N.
- **P0-27** consumes D13/D18 and amends this ADR with the OOD-ECE min-N admissibility floor.
- **P0-28** consumes D4/D7/D11/D12/D17/D18 and authors the blinded magnitude rationales.
- **P0-30** (decoy prevalence + mining spare rule) consumes D7/D14; **P2** (hard-negative mining, class-II augmentation) consumes D8/D9/D14; **P3** (ECE) consumes D11/D13; **P4** (GATE-1) consumes D1–D10/D15–D17; **P5** (FDR) consumes D3/D12; **P6** (orthogonal validation, PU precision) consumes D10.
- **Phase-0 exit (2026-07-12):** the blinded-frozen GATE-1 (recall@matched-precision vs `cmsearch`, +10 pp/+5 pp bar) and GATE-2 (ECE ≤ 0.05, FDR CI-upper ≤ 10 %) design is described (values unchanged, no result asserted) in the `paper/manuscript.qmd` §Non-circular evaluation design paragraph and the `README.md` P0 headline; consolidated in `docs/dev-log/phase0_2026-07-12.pdf`.
- **Phase-2 exit (2026-07-31):** **no blinded-frozen value moved; nothing this ADR gates was graded.** What Phase 2 exercised: **D3 + A3** — the overlapping-window `log_mean_exp`-then-arg-max reconciliation operator is the one GATE-4's boundary IoU was computed through, so those extents are not 512-nt-grid artifacts; **D15** — the directionality-preserving RC constraint holds in the shipped checkpoint (`rc_combine=concat`), and the forbidden averaged form is refused in code, a refusal P2-12 confirmed is not vacuous (the learned `gate` arm *becomes* the average at g ≡ 0.5, so its verdict was withheld for cause); **A7** — the §9.1 negative mix realized a 0.9083 stream share over a 19,409-item pool with out-of-fold decoy parents refused; **A11** — single-scalar temperature scaling was fitted (**T = 0.989587**, out-of-sample read on the other cluster-disjoint half of `selection_val`) and is **explicitly not shipped and not gated**: every Phase-2 number, GATE-4 included, is on the **uncalibrated** posterior, and **D11's GATE-2 ECE remains ungraded until P3** on the P3-02 `calib` carve, which does not exist yet. **A9/A10 — the hard-negative-mining round did NOT run.** The producer is certified and its budget pinned (A10 Phase 2, ≈4.42 core-h/round), and round-0 was measured (N₀ = 941 FPs / 660 genomes), but only **1 of the 3** spare-rule evidence disjuncts has a backend; the rule fails closed, so yield is structurally zero and a round would be a verified no-op. **Deferred, not cancelled**, by a recorded §7 decision — the Phase-2 exit checklist item is disclosed as unmet rather than ticked (`docs/model_card.md` §8). Recorded in `README.md`, `paper/manuscript.qmd` §sec-gate4, and `docs/dev-log/phase2_2026-07-31.pdf`. **⏭ Discharged at P3 by Amendment A12** (2026-08-13): the round ran with all four backends live, decided 941 candidates (3 mined / 938 spared), and declined the retrain — so this deferral is closed, and the P2-trained scanner stays canonical.

## Sign-off

**Accepted — user sign-off 2026-07-11 (CLAUDE.md §7 item 2, P0-25), "accept as drafted."** The scientific-evidence gate for the D1 fixed identity-bin edges was cleared with two independent agreeing sources [PMID:17151342 / DOI:10.1101/gr.5890907; PMID:19861422 / DOI:10.1261/rna.1556009, accessed 2026-07-10]. All numeric defaults below are now **blinded-frozen at P0**: they may not change after P4 unblinding, and any pre-P4 recalibration requires ADR-0005 re-sign-off.

**Blinded-frozen numeric defaults:** D1 bin edges `<50|50–70|70–85|85–100 %` + 5 divergence levels; D3 Stage-1 recall floor ≥ 99 %; D4 **+10 pp / +5 pp**; D7 benchmark decoy prevalence **100:1** + sweep 10:1→10⁴:1; D11 **ECE ≤ 0.05**, 15 equal-mass debiased bins; D12 **FDR CI-upper ≤ 10 %**; D16 fixed aux weights; D17 **ECE +0.02 / recall +3 pp / AUPRC +0.03** swap margins. The min-N floor (P0-26), OOD-ECE min-N admissibility floor (P0-27), and authored magnitude rationales (P0-28) are pinned by later P0 steps that amend this ADR (D18).

---

## Amendment A1 — Minimum-real-homolog N pinned + GATE-1 power-budget audit (P0-26, 2026-07-11)

- **Status:** Accepted (user sign-off 2026-07-11; CLAUDE.md §7 item 2 — the D18 min-N delegation). Pins D18; consumed by D1/D4/D6/D8/D9.

**Pinned value.** `MIN_REAL_HOMOLOG_N = **20**` real held-out positives **per cell** — each D1 identity bin and each D6 headline-certifying arm. Below it a cell is **reported-not-gated** (removed from the D6 AND-of-powered-arms, disclosed) and the D4 weak clause (point ≥ +10 pp AND block-resampled CI lower bound > 0) is admissible; **at or above it** the strong D4 bar (CI lower bound > +5 pp) applies. **Blinded-frozen at P0** — a change needs ADR-0005 re-sign-off.

**Rationale.** (i) One internally-consistent floor: 20 equals the split's already-pinned well-powered leave-one-order-out unit (`conf/data/splits.yaml min_heldout_positives = 20`) and PRD §12 ("31 orders with ≥ 20 positives"; "the ~20-positive floor is statistically unstable"). (ii) It is an **estimability floor, not a power-to-pass floor**: at N < 20 the per-positive recall granularity 1/N > 5 pp exceeds the D4 strong-bar positive floor, so the tighter CI cannot resolve it (D4's "the tighter CI cannot be estimated"); at N ≥ 20 the strong CI is estimable — whether a *powered* arm then *passes* is a separate P4 question. (iii) N ≥ 20 keeps the normal-approximation binomial interval non-degenerate for central recall (extreme recall uses the D5 exact/Wilson / permutation inference). A **companion block count** is reported per arm (D5 resamples at the homology-cluster / held-out-order block level; < 2 blocks → not block-resamplable) — **reported, not a second pinned floor**.

**Audit result** (`src/tbox_finder/power.py`; `data/processed/audits/power_budget_report.json`; over the real 23,569-record P0-22 partition; identity = the ADR-0004 D2 consensus-column metric, `coverage_cut = 0.70`):

- **Held-out N = 8,749.** Every held-out positive has nearest-training identity **< 0.70 (max 0.699)** by whole-cluster holdout, so every one falls in the bottom two D1 bins — `<50`: 85 · `50–70`: 6,793 · `70–85`: **0** · `85–100`: **0** · no-coverage-adequate-neighbour (‑1): 1,871. **The 70–85 and 85–100 bins are provably empty for the leave-clade-out arm** (reachable only by the D8 synthetic-divergence arm + the §13 campaign).
- **Low-identity homolog count:** 6,878 measurable `< 70 %` + 1,871 no-adequate-neighbour = 8,749. **Per leave-one-order-out round:** 43 held-out orders, **30 ≥ min-N**.
- **Reachability of the headline-certifying cells:** (1) lowest-identity bins pooled — N = 8,749 / 43 blocks → **POWERED**; (2) non-Firmicutes-order subgroup — N = **1,635** / 34 blocks (Actinobacteria 1,230 · Tenericutes 202 · Chloroflexi 117 · Proteobacteria 31 · Deinococcus-Thermus 28 · Synergistetes 27) → **POWERED**; upper bins 70–100 % — N = 0 → **structurally empty**; arm (c) literature anchor (P0-16) — N = **16** (5 coordinate-novel + 11 corpus-overlap) → **below min-N**; class-II anti-mimicry independent-of-Actinobacteria natural set (P0-17: 0 + blind non-Actino: 0) — N = **0** → **below min-N**.

**Determinations.**

1. **Arm-(c) sourcing-fallback (D7 §7 stop-and-ask; user sign-off 2026-07-11).** The P0-16 anchor's N = 16 < 20 → **arm (c) is dropped from the D6 mandatory AND**; the **beyond-Firmicutes certification rests on the powered non-Firmicutes-order subgroup** (N = 1,635 / 34 blocks). The D6 floor is satisfied (arm 1 powered **AND** the non-Firmicutes-order subgroup powered). The 16 re-derived anchor loci are **reported as corroborating, non-gating** model-independent evidence, not sourced-up further.
2. **Class-II anti-mimicry (D9).** The natural independent-of-Actinobacteria class-II set is N = 0 < 20 → **reported-not-gated**; the sub-arm **rests on the P2 construction-powered synthetic-class-II recovery set** (above min-N by construction) **+ the D9 within-phylum-homology control**, exactly as D9 pre-registered. (The total held-out class-II N ≈ 1,190 is Actinobacteria-dominated — single-phylum, so within-Actinobacteria memorization-confounded — and the ~12 non-Actinobacteria held-out class-II corpus records are CM/DB-derived P0-17 *leads*, not independent positives.)
3. **Synthetic-divergence arm (D8).** Remains the **pre-registered precondition for the §1 CM-invisibility claim**: the real leave-clade-out bins reach only to 0.699 identity, so the deep-divergence (`< 40 %`) cells are supplied only by the P2/P4 synthetic arm and the §13 campaign — unmet, that claim rests on §13, disclosed.

**Cross-reference impact:** P0-28 uses the pinned per-bin/per-arm min-N in its D4 magnitude-rationale power calculations; P4 reads the powered-vs-reported-not-gated arm set (arm (c) dropped from the AND; beyond-Firmicutes on the non-Firmicutes-order subgroup); P2 builds the construction-powered synthetic-class-II recovery set the anti-mimicry sub-arm now depends on.

---

## Amendment A2 — OOD-ECE min-N admissibility floor pinned + coverage simulation (P0-27, 2026-07-11)

- **Status:** **Accepted (user sign-off 2026-07-11; CLAUDE.md §7 item 2 — the D18 OOD-ECE-min-N delegation), "accept 20 as drafted."** Pins D18 (the second deferred value); consumed by D13; feeds the ADR-0006 rollup (P0-29). Committed atomically with the P0-27 simulation artifacts.

**Pinned value.** `OOD_ECE_MIN_N = **20**` real positives in a deployment corpus's nearest-relative leave-clade-out **calibration unit** (the leave-one-order-out benchmark unit). Below it the D13 small-N-robust OOD-ECE estimator is **inadmissible** → the corpus is **"sensitivity-bounded / inconclusive negative *by rule*"** (never a silent calibrated-negative PASS). The floor is an **admissibility gate on condition (ii) of the D13 three-part PASS** (drift bound ∧ min-N ∧ detection-power floor); conditions (i)/(iii) are separate P3/P4 quantities. **Blinded-frozen at P0** — a change needs ADR-0005 re-sign-off. **Frozen in code** (`src/tbox_finder/coverage.py::OOD_ECE_MIN_N`, no CLI/config override) so no committed report can contradict the pin (Amendment A1 precedent).

**Rationale.** (i) **One internally-consistent floor:** 20 equals the recall floor `MIN_REAL_HOMOLOG_N` (Amendment A1), the split's `min_heldout_positives = 20`, and PRD §12 ("31 orders with ≥20 positives"; the ~20-positive per-order ECE is "statistically unstable"). (ii) **Estimator-matched:** the D13 OOD estimator is a *small-N-robust* proper-scoring-rule / kernel calibration estimator with bootstrap CIs — chosen precisely so N = 20 is admissible (bootstrap CI reported, not per-bin filled), distinct from the D11 in-distribution 15-equal-mass-bin estimator that would need far more; below 20 the per-order calibration estimate is PRD-documented unstable → inadmissible. (iii) **Conservative by construction:** a *higher* floor only makes *more* corpora inconclusive-by-rule (anti-overclaiming), and this is an *estimability* floor, not a power-to-pass floor — clearing it admits an OOD-ECE estimate, it does not manufacture a PASS. A more conservative reviewer floor (30/50) is reported in the coverage sweep so the sensitivity is visible; 20 is pinned for internal consistency.

**Simulation result** (`src/tbox_finder/coverage.py`; `data/processed/audits/ood_ece_coverage_report.json`; seeded block bootstrap over held-out orders, seed 42, B = 2000; over the real 23,569-record P0-22 partition):

- **43 held-out leave-one-order-out units; 30 clear the floor.** Adjudicable fraction **among positive-bearing orders = 0.698** (block-bootstrap 95 % CI **0.558–0.837**; clearing-count CI 24–36) — the PRD §12 quantity. **13 sub-min-N orders** are inconclusive-by-rule. Floor sweep {10 → 32 · 15 → 31 · 20 → 30 · 30 → 22 · 50 → 16} clearing (fraction 0.744 → 0.372).
- **Adjudicable calibration footprint = 7 bacterial phyla, 0 archaeal** (Actinobacteria, Chloroflexi, Deinococcus-Thermus, Firmicutes, Proteobacteria, Synergistetes, Tenericutes), **hyper-concentrated**: Firmicutes = 81.2 % of corpus held-out positives, the single largest order (Lactobacillales) = 61.1 % of all held-out. The named zero-positive scan superclades **{Archaea, DPANN, CPR/Patescibacteria, candidate phyla}** are **measured-absent** from the partition's resolved phyla ("0/23,535 TBDB records are archaeal", PRD §7.2 — the catalogue's silence, ascertainment-limited).
- **Non-fabrication guard:** per-order N is **byte-identical to the signed P0-26 `power_budget_report.json`** (0 mismatched orders). Internal consistency `OOD_ECE_MIN_N == MIN_REAL_HOMOLOG_N == 20`.

**Determinations.**

1. **"Discovery-predominantly-inconclusive" is a pre-registered modal GATE-3 shape (§2.3), quantified ex ante.** The adjudicable calibration footprint spans only **7 bacterial phyla**, while the §13 scan (PRD §7.2) targets the whole prokaryotic tree — including the named zero-positive superclades (all Archaea/DPANN, CPR/Patescibacteria, candidate phyla) and every T-box-free bacterial phylum, each **inconclusive-by-rule ex ante** (no leave-clade-out ECE is computable). So the modal per-corpus verdict is inconclusive-by-rule *before any scan runs* — the terminal outcome is a distinct pre-registered state, not a failure (§2.3).
2. **Two-scope adjudicable fraction.** The 0.698 figure is scoped to positive-bearing orders (PRD §12). The **scan-wide** adjudicable fraction is far lower and **not pinnable here** (the GTDB corpus enumeration is deferred to ADR-0003); it is bounded above by nearest-relative reach of the 7-phylum footprint and is dominated by the zero-positive complement.
3. **Anti-under-scope guard.** Because the floor is an admissibility gate, a **budget-forced under-scope cannot masquerade as a calibrated-negative PASS** (a PASS additionally requires the D13 drift bound *and* the corpus-specific detection-power floor — separate P3/P4 quantities, not this gate).

**Cross-reference impact:** P0-29 authors the ADR-0006 project-level rollup function that consumes this verdict-vector shape; P3 (ECE) reads the D13 OOD estimator's admissibility floor; P6 (GATE-3 tiering / orthogonal validation) reads the ex-ante adjudicable-fraction expectations. The floor does **not** alter the GATE-1 recall arms (Amendment A1) — it governs GATE-2/GATE-3 deployment calibration only.

---

## Amendment A3 — Authored gate-default magnitude rationales + blinded-freeze declaration (P0-28, 2026-07-11)

- **Status:** **Accepted (user sign-off 2026-07-11; CLAUDE.md §7 item 2 — the D18 authored-magnitude-rationale delegation, the third and last deferred value), "accept both as drafted".** Pins D18; records the SESOI / power argument for each blinded-frozen default of D4, D7, D11, D12, D17 (and cross-references the ADR-0004 D6 0.80). Committed atomically with `analyses/gate_default_rationales.qmd` + `src/tbox_finder/power.py::magnitude_rationale` + `tests/unit/test_magnitude_rationale.py`.

**What this amendment pins.** ADR-0005 pinned the *rules and the default values* (D4/D7/D11/D12/D17) and declared them blinded-frozen; it deferred the **authored magnitude-rationale *text*** to P0-28 (D18). This amendment supplies that text — a smallest-effect-of-interest (SESOI) / effect-size argument or a power calculation for **each** delegated default — **authored 2026-07-11, before any P4 result exists**, and declares the freeze. The full derivations live in `analyses/gate_default_rationales.qmd`, rendered verbatim from `src/tbox_finder/power.py::magnitude_rationale` (single source of truth; a unit test asserts the code constants match these ADR-pinned values byte-for-byte).

**Scientific-evidence gate (§10.1) — cleared, ≥2 agreeing method sources per high-stakes convention** (identifiers verified against PubMed / the publisher of record; no numeric cutoff transcribed from any paper — §10.3):

- **SESOI / equivalence-bound method:** Lakens 2017 [DOI:10.1177/1948550617697177]; Lakens, Scheel & Isager 2018 [DOI:10.1177/2515245918770963] (accessed 2026-07-11).
- **Binned-ECE / well-calibration:** Guo et al 2017 [arXiv:1706.04599]; Naeini, Cooper & Hauskrecht 2015 [PMID:25927013] (accessed 2026-07-11).
- **Discovery-stage FDR:** Benjamini & Hochberg 1995 [DOI:10.1111/j.2517-6161.1995.tb02031.x]; Storey & Tibshirani 2003 [DOI:10.1073/pnas.1530509100]; Elias & Gygi 2007 [PMID:17327847, already D12] (accessed 2026-07-11).
- **Low-identity field context (recall SESOI):** Freyhult et al 2007 [DOI:10.1101/gr.5890907]; Menzel et al 2009 [DOI:10.1261/rna.1556009] (already D1).

**Authored rationales (compact; full text in the QMD).**

1. **D4 recall point bar (+10 pp).** *SESOI + power.* The smallest recall gain over `cmsearch` at matched precision that is scientifically material **and** robustly separated from noise at the pinned min-N: +10 pp = **two per-positive granularities** (2 × 1/20), so a passing arm's point estimate sits a full CI-floor-width above the +5 pp non-trivial-improvement boundary. In the bottom-two identity bins where sequence homology search is documented to lose sensitivity (D1 sources), +10 pp = recovering ≥1-in-10 more divergent T-boxes than the CM.
2. **D4 recall CI floor (+5 pp).** *Power (estimability).* The finest recall-difference lower bound the block-resampled CI can resolve at min-N = 20 (1/20 = 5 pp per positive). This is the **same 1/N ≥ 5 pp argument that pinned `MIN_REAL_HOMOLOG_N` (Amendment A1)** — the recall bar and the min-N floor are **one internally-consistent construction**, not two independent picks. Below min-N the D4 weak clause (CI lower > 0) is the disclosed fallback.
3. **D11 in-distribution ECE (≤ 0.05).** *SESOI + power.* A ≤ 5 % confidence-accuracy gap on the temperature-scaled named posterior (a machinery-failure budget). Two backings: **(i)** it is half the D12 FDR budget (0.05 / 0.10), so a GATE-2-passing calibration head cannot by itself exhaust the downstream FDR budget; **(ii)** with 15 equal-mass debiased bins the binned-ECE estimator resolves 0.05 above its own noise floor at realistic calibration-set N, yet 0.05 is tight enough to catch a broken head. Post-hoc temperature scaling routinely reaches this range → achievable-yet-meaningful.
4. **D12 genome-scale FDP (≤ 0.10).** *SESOI.* A **discovery-stage** FDR on the *pre-orthogonal* candidate table — the candidates then pass ADR-0006 orthogonal Tier-1/2/2N validation (the terminal FP control), so the right error rate here is a *discovery* FDR (FDR/q-value is the established genome-wide-discovery criterion, "more liberal" than a terminal 1–5 %). 10 % is the SESOI on the candidate table: tight enough that orthogonal validation isn't swamped (E[false] = 0.10 × table size, reported as a density), loose enough to retain the divergent-loci recall that is the model's advantage. Gated on the CI upper bound.
5. **D7 benchmark decoy prevalence (100 : 1).** *Design (not SESOI/power).* Sits ~1 decade above the ~10 : 1 training seed ratio and 1–2 decades below the ~10³–10⁴ : 1 genome scale; because the comparison is matched-precision, prevalence sets only the operating point (the +10 pp gap is reported over a 10 : 1 → 10⁴ : 1 sweep). Documented here per the D18 delegation.
6. **D17 swap margins (0.02 ECE / 3 pp recall / 0.03 AUPRC).** *SESOI on the backbone-choice decision.* 0.02 ECE = 40 % of the D11 gate (a meaningful fraction of the calibration budget), **sustained** across the held-out-order distribution; 3 pp = the smallest *sustained* recall deficit worth switching backbones over (a single-arm point difference is already governed by the +5 pp floor); 0.03 AUPRC = a comparable threshold-free ranking deficit.
7. **ADR-0004 D6 GATE-4 floor (≥ 0.80).** Co-authored into ADR-0004 (Amendment A2, this session): the smallest per-nt F1 on the 3 core elements that demonstrates learned element *extents* and supports §13.1 locus construction + §13.3(d) specifier read, while tolerating the ~1–2 nt dot-bracket-projection boundary ambiguity; the N ≤ 9 PDB label-noise ceiling C (P0-21, non-gated) must not one-directionally lower it.

**Honest caveat (recorded, not hidden).** The naïve normal-approximation minimum-detectable-effect at a 0.5 baseline and N = 20 is ≈ 22 pp — larger than the +10 pp bar. This is by design: min-N = 20 makes the +5 pp CI floor **estimable**, not the exactly-min-N arm **powered to pass**. GATE-1's gated inference is the block-resampled bootstrap CI + the D5 small-N exact/permutation test, and the powered arms pool far above N = 20 (lowest-identity pool N = 8,749 / 43 blocks; non-Firmicutes-order subgroup N = 1,635 / 34 blocks — Amendment A1). An arm clearing min-N but not the bar is reported-not-gated / §7 stop-and-ask, never a silent pass (D6).

**Blinded-freeze declaration.** All rationales were authored **2026-07-11, before any P4 benchmark result exists**. **No post-P4 change is permitted**; a **pre-P4 recalibration requires ADR re-sign-off** (CLAUDE.md §7 item 2), recorded as a further amendment. This closes the D18 "authored magnitude-rationale text" delegation — the third and final deferred value.

**Cross-reference impact:** P4 (GATE-1) and P5 (FDR) inherit these frozen defaults and may not change them post-unblinding; the ADR-0004 D6 0.80 rationale is co-authored there (Amendment A2). No rule or data artifact changes — this amendment adds the *why* behind numbers already pinned by ADR-0005 / ADR-0004.

---

## Amendment A3 — D3 locus rule: the reconciliation operator normalises per window before averaging (P2-03, 2026-07-16)

- **Status:** **Accepted (user sign-off 2026-07-16; CLAUDE.md §7 item 2), "(A) Normalise per window — amend ADR."** Disambiguates D3's locus-construction rule (it pins no new *value*); consumed by P2-03 (`infer/reconcile.py`), P2-14 (GATE-4), and the P5 scan. Committed atomically with the P2-03 operator + its unit/golden gates.

**The ambiguity.** D3 pins "the per-position 8-class **logits** from all covering windows are **averaged in log-sum-exp space then arg-maxed** into one per-position prediction" (identically in PRD §6). That sentence admits **two coherent operators**:

- **(B) literal** — `argmax_c log( mean_w exp( logits[w,p,c] ) )` — a soft-max pooling over windows, applied to the raw logits.
- **(A) implemented** — `argmax_c log( mean_w softmax(logits[w,p])_c )` — the arithmetic mean of the per-window **posteriors**, computed in log space.

They are **not notational variants**. Measured at P2-03 on the golden fixture's real pinned-tiling geometries (window 1024 / stride 512 over nine real sampled P2-00 context lengths, 1,380–2,556 nt): **3,418 of 8,931 multi-covered positions — 38.3% — receive a different class** under (A) vs (B). (Synthetic near-uniform logits maximise the disagreement; a trained head's peaked logits agree far more often. The number establishes the choice is **material**, not that it is this large in production.)

**Pinned form.** **(A).** `log_probs[p] = log( mean over covering windows w of softmax(logits[w,p]) )`, computed as a coverage-normalised log-sum-exp of per-window `log_softmax` values, then `argmax`, applied **before** any along-sequence element merging. **Frozen in code** (`src/tbox_finder/infer/reconcile.py`, no config override), with `diagnostics()` reporting `pinned_by = "ADR-0005 D3 + A3"` (the A1/A2 freeze-in-code precedent).

**Rationale.**

1. **(B) weights each window by an unconstrained nuisance quantity — the discriminating argument.** `exp(logit)` is an *unnormalised* score, so averaging it weights window `w` by its partition function `Z_w = Σ_c exp(logits[w,p,c])`. **Nothing in the training objective constrains `Z_w`**: the softmax inside the cross-entropy is invariant to a constant shift of a position's logits, so the model is never trained to control that offset. Under (B), two windows predicting the **identical posterior** contribute unequally whenever their logit offsets differ — for a reason the model never claimed. Under (A) they contribute equally, by construction.
2. **(A) is the standard ensemble average** of member predictive distributions, and the reconciliation *is* an ensemble over covering contexts.
3. **The D3 consumer wants a posterior.** The Stage-1 threshold is "the most-liberal value retaining a per-locus recall floor" and the §11 recalibration/ECE stack grades a calibrated posterior; (A) emits one directly (`Σ_c exp(log_probs) = 1`). *Recorded honestly: this argument is **weaker than it looks** and did not decide the matter — (B)'s output can also be softmaxed over `c`, yielding a distribution with the same arg-max. It is a convenience argument; rationale 1 is the discriminating one.*

**What this amendment does NOT change.** The **coverage normalisation** (`− log |W(p)|`) is common to both readings and was never at issue: it is what "**averaged**" means as against "summed", and D3's own stated purpose — seam-freeness, "boundary IoU is not a 512-grid artifact" — *requires* it, since without it an interior nucleotide (coverage 2–3) would outscore a tail nucleotide (coverage 1) on identical evidence. The **arg-max-last** ordering, the **before-merging** placement, the **zero-flank-and-flag** contig-end rule, and the 1024/512 tiling are implemented exactly as D3/PRD §6 pin them. No numeric gate value moves.

**Cross-reference impact:**
- **P2-03** — `infer/reconcile.py::reconcile_windows` implements (A); golden digest `08f088c9…` is (A)'s. A unit test pins the operator identity against **external literals**, and the three behavioural claims in `diagnostics()` are **re-derived from measured behaviour**, not echoed (the P1-15/P1-16 self-certification lesson).
- **P2-14 / GATE-4** — per-nucleotide F1 and boundary IoU are graded on (A)'s arg-max.
- **P5 scan** — the §13.1 candidate table derives from (A); the D3 Stage-1 threshold is set on (A)'s posterior at the phase gate (unchanged **as a rule**).
- **D12/GATE-2, D4/GATE-1, the strand resolver** — all consume locus calls *downstream* of this operator; unaffected as rules.
- No data artifact is invalidated: no Stage-1 checkpoint exists yet (P2-04), so nothing was reconciled under the other reading.

---

## Amendment A4 — Tier-2N probe eligibility requires a length-matched excision control (P2-07, 2026-07-19)

- **Status:** **Accepted (user sign-off 2026-07-19; CLAUDE.md §7 item 2), "ADR-0005 Amendment A4 approved."** Amends **D14** (the Tier-2N probe set) with the eligibility rule D14 left open; pins no new *value* (the D18/A1 floor `MIN_REAL_HOMOLOG_N = 20` is untouched). Consumed by P2-09 (round-0 mining), P5 (synthetic-Tier-2N spike-in recovery / §12 detection-power floor), P6. Committed atomically with the P2-07 modules + their unit gates + `reports/p2/tier2n_probe.json`.
- **Note on numbering:** this file already carries **two** amendments labelled `A3` (P0-28 and P2-03) — a pre-existing collision flagged for reconciliation at the P2 exit gate. This amendment takes `A4` to avoid compounding it.

**What D14 leaves open.** D14 pins that a Tier-2N probe set is evaluated each mining round and that a recall drop halts/rolls back the iteration. It does not say how a synthetic construct **earns** Tier-2N membership. P2-07 built that generator and found the obvious operationalisation — "the covariance model misses it" — is confounded twice over, measured rather than assumed.

**Measured (P2-07, `RF00230.cm --cut_ga`, INFERNAL 1.1.5; `reports/p2/tier2n_probe.json`).**

| quantity | value |
|---|---|
| unablated corpus records missed (n=500, seed 42) | **27.6 %** (362/500 detected) |
| — independently reproduced (n=300, seed 20260719) | 27.0 % (219/300 detected) |
| real element ablations missed (n=599) | **66.7 %** |
| **length-matched random excisions missed (n=599)** | **78.8 %** |

1. **Divergence confound.** Better than a quarter of real, architecturally-canonical TBDB T-boxes are already missed at the GA cutoff, because RF00230's `GA` is 93.00 bits and the corpus was built with a different model. Labelling a CM miss as Tier-2N would grade *sequence divergence* — the separate GATE-1 divergence arm (D6/D8) — as *architectural* novelty.
2. **Excision-length confound, the decisive one.** A covariance model is an alignment, so removing nucleotides degrades it regardless of *which* ones. Excising an equal-length segment from a **random non-element position breaks detection MORE often (78.8 %) than removing the actual Stem II or Terminator (66.7 %)**. Element excision is therefore *less* destructive than the null it must beat: on this evidence a parent/variant discordance carries **no architecture-specific signal at all**, and a probe set built from it would measure excision.

**Pinned rule.** A synthetic construct is **Tier-2N-probe-eligible** only as a measured **triple**:

| leg | required verdict | confound removed |
|---|---|---|
| parent | CM-**detected** | divergence |
| variant (element ablated) | CM-**missed** | — the effect under test |
| length-matched control (equal-length excision at a non-overlapping position) | CM-**detected** | excision length |

Every emitted variant **carries its own control**, generated with it, so a control cannot be omitted; an absent control arm leaves every variant ineligible rather than degrading to the confounded two-leg filter. An unmeasured leg is **never** read as a miss. Discards are reported per cause and the per-family split is reported alongside the pooled count, so a family resting on the min-N floor stays visible. The D18/A1 floor `MIN_REAL_HOMOLOG_N = 20` applies to the probe set unchanged.

**Effect at P2-07:** 599 emitted → 45 eligible (21 class-II, 24 stem-II; both clear min-N independently) = 161 parent-already-missed + 200 ablation-did-not-break + **193 length-confounded** + 45 + 0 unmeasured. The two-leg filter would have admitted **238**, of which 193 — **81 %** — are length artifacts.

**Scope limits, disclosed rather than buried.**
- The construct's architecture families are the **two** that cleared the CLAUDE.md §10.1 ≥2-independent-source bar (class-II platform swap; joint Stem II + IIA/B deletion). Its diversity is a **lower bound**, not a sample of the natural non-canonical space.
- Attribution is to the **excised element via a CM proxy**, *not* to ADR-0006 D9's relaxed-architecture detector **(b)**, which has no in-repo backend until P6-01/P6-11. The in-repo element-level `stem2_structure_only.cm` was tested and is **not** one (1/300 parents detected even at E ≤ 1000). The triple is **pre-registered for re-derivation against the real (b) detector at P6**, and the probe set is provisional until then.
- The **natural** Tier-2N arm is **N = 0 by construction** — the corpus is 100 % CM-derived and so cannot contain a CM-invisible locus, and no published non-canonical architecture is CM-invisible. Reported at zero, in the D6/D9 reported-not-gated spirit, never dropped from the accounting.
- **No published RF00230 false-negative rate exists** (§10.1 gate: `NO DIRECT EVIDENCE FOUND`; only the *procedural* fact that TBDB needed a second class-II CM, PMID:32882008). Every Tier-2N recall figure this project reports is therefore **first-party, measured in-repo**.

**Cross-reference impact:**
- **P2-07** — `src/tbox_finder/synth/tier2n.py` implements the triple and freezes the measured baselines in `MEASURED_CONFOUND_BASELINES`; `src/tbox_finder/eval/tier2n_probe.py` admits only eligible variants; `src/tbox_finder/infernal.py` is the single `cmsearch` caller. Unit tests pin each leg and were verified **by sabotage**.
- **P2-09** — round-0 mining reads the probe set through `build_probe_set`; a sub-min-N set is `ROUND_INADMISSIBLE`, never a silent pass.
- **P5** — the synthetic-Tier-2N spike-in recovery (§12 detection-power floor) inherits the triple and the provisional status.
- **P6** — re-derives eligibility against the real (b) architecture detector; if the two disagree materially, the P6 result supersedes and this amendment is re-signed.
- **ADR-0006 D9** — unchanged as a rule; this amendment supplies the operational proxy D9's row-5 routing needs before its (b) backend exists.
- No gate **value** moves; `MIN_REAL_HOMOLOG_N` is untouched.

## Amendment A5 — the synthetic class-II set supplies power, not independence (P2-08, 2026-07-19)

- **Status:** **Accepted (user sign-off 2026-07-19, "both approved"; CLAUDE.md §7 item 2).**
- **Label note:** `A5`, not `A4` — `A4` is P2-07's Tier-2N eligibility rule. The pre-existing **duplicate-`A3` collision** (P0-28 and P2-03 both carry `A3`) is untouched here and still needs reconciling at the P2 exit gate.

**Trigger.** A1 records the natural independent-of-Actinobacteria class-II set at **N = 0** and rests the anti-mimicry sub-arm on "the P2 construction-powered synthetic-class-II recovery set (above min-N by construction) + the D9 within-phylum-homology control". P2-08 measured that **no construction available in this corpus manufactures phylogenetic independence** — only record count and, under ADR-0004 A3, block count. Every viable parent pool is either 22 records / 4 orders (training-fold) or dominated by Actinobacteria (held-out: 1,160 of 1,172 eligible parents). A1's fallback therefore does not close as written.

**Amendment.** The synthetic recovery set is pinned as supplying **min-N and block-level power, not phylogenetic independence**. Consequently the **within-phylum-homology control is promoted from a side check to the load-bearing discriminator** of the sub-arm: with the parent pool dominated by a single phylum, recovery *unaccompanied by a passing within-phylum control* is **not** evidence of anti-mimicry, and must not be reported as such.

**How the control is operationalized (measured, P2-08).** **Zero Actinobacteria records of any class are in the nested training fold** — the whole phylum is held out — so within-Actinobacteria memorization *by direct exposure* is impossible. The residual risk is **homology-mediated**: a held-out Actinobacteria record close enough to a trained non-Actinobacteria one. Each variant therefore carries its parent's identity-to-nearest-training-fold bin, computed by `power.compute_heldout_identities` + `power.bin_identity` — the **same metric as the ADR-0004 D2 histogram**, reused rather than re-derived, so the control and the histogram cannot disagree. Measured strata over the 1,172 parents: **`50-70` = 1,019 · `<50` = 61 · `no_neighbour` = 92**. The two low-homology strata each independently clear `MIN_REAL_HOMOLOG_N = 20`, so the control is powered, not merely present. Recovery concentrated in the high-identity stratum is memorization; recovery holding in the `<50` and `no_neighbour` strata is not.

**Block power is reported at both resampling units.** **103 clusters / 25 orders** (both ≥ 20; a parent with a null `resolved_order` contributes no order block — counting nulls had inflated this to 26). Reporting only the flattering unit is how an underpowered arm reads green, so `build_report` grades at the **weakest** unit and emits both.

**What the construction is — and what it deliberately is not.** The set is **presentation variants** (window-phase offset, reverse complement) of real held-out class-II corpus records; it asserts no new biology. The construction the step's name suggests — a class-I→class-II **regulatory-platform swap**, which would have supplied 8,281 parents across 16 phyla — was put through the §10.1 evidence gate and **refused**:

- Platform modularity with respect to the sensing core is **contested**, and the operative clause is contradicted by **two independent labs**: Stem I architecture systematically covaries with class (long Stem I + IDTM motif in transcriptional T-boxes; mostly intermediate/"ultrashort" and IDTM-less in translational ones — Suddala & Zhang 2020, DOI:10.1002/wrna.1600), and class-II Stem I domains "lack several highly conserved elements that are essential for interaction with the tRNA ligand in other T box RNAs" (Sherwood et al. 2015, PMID:25583497).
- **No published functional T-box platform-swap chimera could be verified.** The one review-level assertion of such a construct (PMID:31206978) cites a source that does not contain it. Precedented instead is **specifier-level** re-engineering (PMID:9098057; Brill et al. 2011, PMID:21233158) — a different claim.

Encoding that premise beneath the headline anti-mimicry pillar is what §10.2/§10.3 forbid. *(All literature accessed 2026-07-19; Consensus was quota-exhausted, so the §10.1 ladder ran Scholar Gateway → PubMed → Europe PMC full text.)*

**Also recorded (no decision change).** TBDB's class-II set was built by running its class-II covariance model on **Actinobacteria only** (TaxID:1760 — Marchand et al. 2021, DOI:10.1093/nar/gkaa1060, accessed 2026-07-19), and the class-II model was additionally applied only to class-I hits predicted to have a truncated Stem I. TBDB therefore **cannot** detect a class-II T-box outside Actinobacteria by construction. The observed Actinobacteria-restriction of class II is an **ascertainment artifact, not evidence of absence**, and must never be cited as a distribution baseline in a model/dataset card, `paper/manuscript.qmd`, or a gate rationale. This is a live confound for the §1/§13 expansion-of-distribution claim — and equally an opening, since a phylum-agnostic class-II search is unexplored.

**Not amended.** D9's Stage-1-only scoring and naive-checkpoint requirement, `MIN_REAL_HOMOLOG_N = 20` (A1), the D4/D5 block-resampled CI, and the D6 reported-not-gated → §7 stop-and-ask rule are all unchanged.

**Cross-reference impact.** PRD §2.3 (GATE-1 anti-mimicry clause — the "above min-N by construction" phrase now reads *block*-min-N, and independence is explicitly not supplied), §5 mechanism 3, §7.1, §11; ADR-0004 D7 + **A3** (the parent-fold relaxation this amendment depends on); P2-11 (the naive checkpoint this set is graded on), **P4** (anti-mimicry grading — must stratify by the carried control bin, not pool), P2 exit gate (report the arm at both block units).

## Amendment A6 — mineability requires coordinates independent of the positives (P2-10b, 2026-07-20)

- **Status:** **Accepted (user sign-off 2026-07-20; CLAUDE.md §7 item 2), "Approve as drafted."**
- **Label note:** `A6`. `A4` = P2-07, `A5` = P2-08. The pre-existing duplicate-`A3` collision (P0-28 and P2-03) is untouched and still needs reconciling at the P2 exit gate.

**Trigger.** D14 makes the union-prior locus mask the first guard between a Stage-1 false positive and the mined pool. P2-10b measured that the guard could not run at all: `classify_candidate` returned `refused_no_coordinates` for **100 %** of every pool named in `MINEABLE_POOLS`, and `decoys_report.json` reported `total_pool_records_masked: 0`. Giving the pools coordinates is necessary but not sufficient — three further measurements:

1. **Namespace.** Every `LocusIndex` key is unversioned (**0 of 12,041**); every external coordinate source is versioned (2,999/3,000 Rfam headers; 23,535/23,535 flank-context rows). Raw intersection: **0 of the 2,750** distinct accessions carried by the 2,999 coordinate-bearing Rfam records (2,751 across all 3,000 headers, the extra being the non-replicon URS below); **269** after version-stripping. Shipping parsed-but-unnormalised coordinates converts a *visible* no-op into an *invisible* one — non-null columns, clean run, still zero masked.
2. **`dinuc_shuffled` has no independent coordinates.** Its only possible coordinates are its source positives'. Carrying them masks 100 % of the pool against its own parents; leaving them null makes `refused_no_coordinates` its permanent steady state.
3. **`structured_rna` has 0 true overlaps** with the union prior (nearest record 5 nt away, caught only by the flank). It cannot demonstrate the mask fires.

**Amendment.**

- **`dinuc_shuffled` is removed from `MINEABLE_POOLS`.** Mineability requires coordinates *independent of the training positives*; a composition null has none by construction, so mining it is a category error, not a missing feature. It **remains a PRD §9.1 training negative** — only its mineability is withdrawn — and its parent link is now recorded explicitly as `source_record_id` (content-addressed corpus identity, replacing a corpus row index embedded in `decoy_id` text that any re-sort silently re-pointed).
- **`genomic_window` is added** (`src/tbox_finder/mining/pool.py`): real windows carved from the P2-00 `flank_context` regions, each with an exact `(accession, locus_start, locus_end, strand)` on a replicon that hosts a known T-box. **Not a fifth PRD §9.1 negative class** and **not annotation-verified 5′UTRs** — it is the D14 loop's candidate substrate, written to its own artifact so the §9.1 pool sizes, the ~10:1 seed ratio, and the committed decoy golden digest are untouched.
- **Accession-namespace normalisation is pinned at the shared masking boundary** (`masking.normalize_accession`, applied on both index build and query), not in each producer, so a future pool cannot silently re-introduce the empty-intersection no-op. Masking is an over-approximation (PRD §9.1 already widens every locus by a flank), so a stale version masking slightly too much is the correct error direction.
- **Non-vacuity is gated on a designed control, not on the natural rate.** Locus-centred windows overlap a known T-box *by construction* and must mask at **100 %**; anything less is a coordinate-frame defect, not a biological result. A natural rate of zero stays a legitimate outcome and can never be the gate, because it cannot distinguish "nothing overlaps" from "the mask is structurally dead". Controls are tagged `is_designed_control` and excluded from the natural rate. Overlap (flank 0) and proximity (flank 50) are reported separately.

**Measured on the shipped artifacts.** Designed control **500/500** masked (500/500 true overlaps); natural **85/46,091** (0.184 %, 69 true overlaps); namespace **0 as-is → 11,447 normalised**. `structured_rna` coordinate coverage **2,999/3,000** (the 1 residual is an RNAcentral URS with an interval but no replicon — refused rather than given a coordinate that addresses no genome). The decoy golden digest is unchanged (`13061e3e…`).

**Not amended.** D14's spare rule and its three model-independent disjuncts, the retained-`leader_decoy` rule, the Tier-2N probe and its per-round halt/rollback, `MIN_REAL_HOMOLOG_N = 20`, and A4/A5 are all unchanged. `gc_background` remains non-mineable for the reason it always was (emitted i.i.d.; exists in no genome).

**Deferred, explicitly (P2-10b′).** Re-sourcing a *true* 5′UTR/tRNA-adjacent `leader_decoy` pool (N = 8 today, opaque surrogate ids, no recoverable accession) needs CDS/tRNA coordinates: the repo has **zero** GFF/GBK, both NCBI fetchers are `rettype=fasta`, and there is no `tRNAscan-SE`/`hmmer`/aaRS resource — 3 data acquisitions + 2 env amendments, overlapping P2-10c's own env stop. ADR-0006 D12's per-class joint false-pass estimate rests on those 8 records and is **under-powered until P2-10b′ lands**.

**Cross-reference impact.** PRD §9.1 (the four pools stay four; the mining substrate is additional and separately named); **ADR-0006 D11** — the masking key's accession comparison is now version-normalised, a semantics change to a D11-pinned key; by user decision (2026-07-20) A6 records it here and ADR-0006 is amended at the **P2 exit gate**, batched with the ADR-0005 duplicate-`A3` reconciliation and the D12 under-power note. P2-10c (spare-rule backend, consumes this substrate); **P2-10e** (the mining rounds — `MINEABLE_POOLS` is what it iterates, and N is still unpinned); P4/P5 (any report quoting `total_pool_records_masked` must now read `coordinate_coverage` + `control_gate` alongside it).

## Amendment A7 — the §9.1 seed ratio is a stream share, its dominant class is real genomic background, and the leader pool is a label collision (P2-10d′-b, 2026-07-21)

- **Status:** **Accepted (user sign-off 2026-07-21; CLAUDE.md §7 item 2).** Signed in three parts, in the order the evidence arrived: the four contested pins (per-class shares, `leader_decoy`, the junction-control rule, class weighting) were each selected from drafted alternatives; **pin 7 was then rebuilt** on the user's decision "do 1" once the junction was measured **visible** and the original reading retracted (see the correction below); and **pin 6a** — the decoy's own §9.2 fold check, raised by review after the first sign-off — was signed last, explicitly, as new ADR content. Amends **D14** with the *training-mix composition* D14 left open, and re-scopes one D14 clause (the retained leader pool). Pins no existing value: D7's 100:1 benchmark decoy prevalence, `MIN_REAL_HOMOLOG_N = 20`, and A4/A5 are untouched. Consumed by **P2-10e** (this run is its round-0 parent) and by **P4/GATE-1**. Committed atomically with `data/embedding.py`, `data/window_dataset.py`'s symmetric augmentation, `eval/junction_probe.py`, `scripts/measure_junction_control.py`, their unit + generator-sabotage + ml gates, and `reports/p2/junction_control.json`.
- **Label note:** `A7`. `A4` = P2-07, `A5` = P2-08, `A6` = P2-10b. The pre-existing duplicate-`A3` collision (P0-28 and P2-03) is untouched and still needs reconciling at the P2 exit gate.

**Trigger.** PRD §9.1's "~10:1 seed ratio" **was never delivered** — not merely its iterative-mining clause. `reports/p2/train_stage1_production.json`'s `diagnostics.config` carries **zero** `negative_*` keys (they were added by P2-10d, *after* P2-09) and `load_corpus_records` loads positives only, so the P2-09 production checkpoint trained on 8,303 positive records and **no negatives at all**. That is a departure from §9.1's *training-ratio* paragraph, and it lands on **GATE-1 (recall@matched precision)**, not GATE-4. P2-10d built the injection hooks; P2-10d′-a built the substrate (15,708 injectable 1,024-nt windows, union-prior-clean and `nested_train`-parented). What a "§9.1 negative" **is** at W = 1024 remained undefined: all 7,008 decoy records are locus-length (57–550 nt) and `background_record` refuses a length mismatch by design (`negatives.py`), so three of the four §9.1 classes had no referent at the training window. The user chose reading **R2** (2026-07-20) — embed the decoy at a random phase inside a real background window — which makes the matched junction control mandatory.

**Measured before adopting R2 (k-mer logistic regression, 5-fold CV AUROC, n = 2,500/arm, seed 20260721; exploratory).** The objection R2 has to answer is this repo's own ruling in `workflow/rules/data.smk`'s `p2_flank_context` docstring: "a 0th-order background splice would hand the model a trivially separable boundary cue and inflate GATE-4/GATE-1" — the reason P2-00 spent 23,535 NCBI requests sourcing real flank. R2 reintroduces two junctions **in negatives only**, so a readable junction would be perfectly class-correlated.

| arm | k = 4 | k = 6 |
|---|---|---|
| **null** — two disjoint sets of *unspliced* real windows | 0.493 | 0.520 |
| **junction alone** — background→background, same two junctions, no decoy | 0.487–0.494 | 0.505–0.519 |
| junction alone, **local** 128-nt window centred on the junction | 0.487–0.510 | — |
| decoy content given the junction — `structured_rna` | 0.715 | 0.876 |
| … `dinuc_shuffled` | 0.844 | 0.812 |
| … `gc_background` | 0.846 | 0.809 |
| … `leader_decoy` (8 distinct inserts) | 0.934 | **1.000** |

> **⚠ CORRECTION (same step, 2026-07-21, before merge). The first reading below was WRONG, and this amendment's junction rule was rebuilt because of it.** The exploratory table said the junction sat on the null. It does not. That measurement was **underpowered**: the pin-6a leakage fix shrank the embedded arm 4,999 → 3,701 and grew the unspliced arms 2,804 → 4,102, and the added power revealed a small, reproducible cue. On the shipped substrate the k=6 junction arm reads **0.5328** against a null of 0.4947, and across **four independent draws** it is **0.5217 / 0.5264 / 0.5281 / 0.5328** against nulls of 0.4947–0.5042 — above its null every time, one-directionally. A splice junction is a **chimera of two genomes**, and a 6-mer model can see it; the original single draw (0.5058) was the lucky one. Pin 7 is therefore **not** "the junction is invisible, here is the bound" but "the junction is visible, so the cue is removed by construction". The original sentence is struck through rather than deleted, because the retracted claim is the point.

Three readings, each of which changed a pin. **(i)** ~~The junction sits **on the null**, globally and locally.~~ **RETRACTED — see the correction above.** The `data.smk` ruling was about a *0th-order synthetic* splice, and R2 never abuts synthetic against real DNA at a labelled boundary; but a real-DNA-into-real-DNA junction does **not** leave "no compositional trace" — it leaves a small one (~0.52–0.53 AUROC at k=6), and while only negatives were spliced that trace was perfectly class-correlated. **(ii)** The embedded windows are separable at 0.71–1.00 anyway — and it is **not** host/insert GC mismatch: matching host GC to insert GC cut mean |ΔGC| from **0.101 to 0.001** (100×) and separability **did not fall** (it rose slightly, 0.821 → 0.840 for `dinuc_shuffled`). The shortcut is the insert's own higher-order composition, so a GC-matched-host pin would buy nothing and **is not adopted**. **(iii)** The mined `genomic_window` records carry no such shortcut — they *are* the null arm. The easy negatives are the synthetic ones.

**Verified directly against the corpus (P2-10d′-b, not from a summary).** Of the 8 `leader_decoy` records, **2 are exact substrings of training-corpus positives**: `neg_tbdb_-1baL_Tx` (197 nt) inside 2 of the 23,535 `master_clean_v0` records, and `neg_tbdb_GwZsgp7s` (61 nt) inside 2. The other 6 are also T-box-derived — `neg_anchor_6UFM` / `neg_anchor_6UFG` are strand 0 of PDB **6UFM** / **6UFG**, which this repo's own `tests/fixtures/pdb_element_extents/README.md` classifies as **class-II ultrashort translational T-box riboswitches**, and the remaining four are TBDB v1 T-box records drawn by tboxevo's `draw_ultrashort_negatives`. They were negatives relative to *tboxevo's IDTM covariance model*, never relative to "is a T-box".

**Pinned rules.**

1. **`negative_fraction = 10/11` is a share of the emitted STREAM, not a pool ratio.** §9.1's "~10:1" reads f = 10/(10+1); `negative_draw_count` emits `round_half_up(n_pos·f/(1−f))` = exactly **10·n_pos** draws per epoch, integer-exact, **no tolerance knob**. At the measured n_pos = 8,303: **83,030** negative draws, stream **91,333**, **14,270** optimiser steps at 10 epochs (11.06× P2-09's 1,290).
2. **A §9.1 negative at W = 1024 is one 1,024-nt real genomic window, optionally carrying one embedded decoy.** The splice is an **equal-length replacement** of `[p, p+L)`, `p ~ U[0, 1024−L]` inclusive (flush placements stay reachable, so "exactly two junctions" is not an invariant of the class), so the window stays exactly 1,024 nt and `background_record`'s length rule is honoured unchanged. Inserts are normalised **U → T** before embedding (`structured_rna` carries 1,784 U); `N` is retained.
3. **Per-class shares are pool-proportional by construction.** The admitted negative records are pooled and drawn **uniformly with replacement**; shares therefore follow admitted pool sizes and **no per-class weight knob is introduced**. §9.1's class 1 ("random windows matched to T-box GC + length — dominant real-world negative") is served by the mined `genomic_window` records, which are real DNA carved from the *same replicons as the loci* — better matched than a bootstrap — and they are the dominant share as §9.1 requires.
4. **`gc_background` takes share 0 in training.** It is a seeded 0th-order i.i.d. composition null built at P0 *because no genome panel existed then* (`decoys.py`'s own docstring). One does now. The pool is **untouched** and remains the D7 GATE-1 benchmark denominator; only its *training* role is retired.
5. **`leader_decoy` takes share 0, and D14's retention clause is re-scoped.** Embedding it would label verbatim positive sequence as background — a direct label contradiction and a breach of §9.1's own masking rule ("all known T-box loci … masked from every negative/decoy pool") — and would hand the model an AUROC-1.000 memorisation target (8 distinct inserts across 83,030 draws/epoch). D14's "the 5′UTR / tRNA-adjacent leader pool is **retained**" now reads **"the leader pool as re-sourced at P2-10b′"**. The non-delivery is disclosed, not absorbed.
6. **Host = the P2-10d′-a admitted pool only**, through the *same* admission filter the plain arm uses (`negatives.admit_pool_rows`, promoted out of `negative_records_from_rows` at this step). An embedded negative's §9.2 provenance is its host's and cannot be weaker than a plain negative's. Host **reuse** across the plain and embedded arms is deliberate: repetition inside the negative class creates no positive-vs-negative cue, and it is what makes the decoy the only difference between the two.
6a. **A decoy's OWN §9.2 provenance is checked, not just its host's (added 2026-07-21, review finding).** Pin 6 originally claimed an embedded negative's provenance "cannot be weaker than a plain negative's". It was. `dinuc_shuffled` records are Altschul–Erikson permutations of *corpus positives*, sampled by `decoys.py` from all 23,535 records rather than from the training fold, and `decoys.py`'s own docstring calls the relationship "leakage-adjacent". **Measured: 1,298 of 2,000 (64.9 %)** `dinuc_shuffled` decoys have a parent **outside** `nested_train`. Embedding one puts a permutation of held-out DNA into the training stream, and the CI §8.2 gate is structurally blind to it. So a decoy carrying a `source_record_id` must have that parent in the training fold, and if no fold set is supplied the row is refused as *unverifiable* rather than admitted. Decoys with **no** parent (`structured_rna` from Rfam, `gc_background` i.i.d.) have no such relationship and are unaffected — the discriminator is the presence of the column, which `decoys.py` populates precisely to keep the link reconstructable.

7. **The junction cue is removed by CONSTRUCTION, not bounded by measurement (amended 2026-07-21, after the correction above).** Positive windows receive the *same* background→background splice, from the *same* insert-length distribution, at a rate **derived** from the negative arm — `rate = n_chimeric_negatives / n_negative_records` = **3,701 / 19,409 = 0.19069**, never a configured number. P(chimeric | positive) then equals P(chimeric | negative) and "this window is a chimera" carries no class information. The splice overwrites only positions **already background-labelled and already real**, equal-length, so it cannot move a single per-nucleotide target, and it draws from an RNG **salted apart** from the lead/strand stream so enabling it does not re-phase any prior run's windows.
   *The replacement gate is arithmetic, not statistics* (`eval/junction_probe.junction_symmetry_clauses`): the rate is re-derived from recorded counts; the splice moved **zero** labels over a non-empty checked set; realized positive and negative chimeric rates over an emitted epoch agree within 3 SE of their difference; and — the must-fire half — with the augmentation **disabled** that agreement clause must be **FALSE**, or the measurement cannot tell a working augmentation from a broken one reporting zeros everywhere.
   *Retained and deliberately NOT gated:* `junction_within_null_band` is still measured and recorded in `reports/p2/junction_control.json`, **failing at k=6**, flagged `junction_is_visible_at_k6: true`. Dropping a failing clause is otherwise indistinguishable from loosening a bar, so the number stays where a reader will find it. The gated clauses are `arms_are_matched` and `probe_can_discriminate` (still must-fire: "the junction is invisible" and "the probe sees nothing" produce identical numbers otherwise).
   *Tier 2, post-run, model-side, unchanged:* the trained checkpoint scores the same arms and must satisfy `AUROC(decoy vs control) > AUROC(control vs plain)` — the decoy must beat its own junction null. Threshold-free on purpose: pinning an absolute bar is how P2-07 certified a perturbation whose effect was smaller than its own null.

8. **Round 0 is a COLD start.** §9.1 orders "seed ratio … **then** iteratively mine", so the seed ratio is a property of the *initial* training: `init_from_checkpoint: null`, and `warm_start`'s absence is **asserted**, not assumed. P2-10d's warm-start machinery is retained unchanged and is **P2-10e's** mechanism (round *k* from round *k−1*). The P2-09 positives-only checkpoint is the **pre-mix baseline**, not this run's parent.
9. **Class weights are computed over the realized mixed stream.** §9.1's focal loss + class weighting correct "residual per-nucleotide imbalance", and the imbalance being corrected is the mixed-stream one; weights fitted to a positives-only stream would describe a distribution the model never sees. This is already the behaviour — `compute_class_counts` scans the dataset *after* the negatives are appended — so the pin is **asserted rather than rebuilt**, and both the mixed and positives-only counts are recorded so the change is visible.

**Measured on the shipped artifacts (`reports/p2/junction_control.json`, seed 20260721, real pools at `029b432`, after pin 6a).** Arms: **3,701** embedded / **4,102** per unspliced arm, from **15,606** unique admitted windows (**102** byte-identical duplicate windows dropped — the pool carries the same DNA under distinct `candidate_id`s, and a duplicate spanning two arms leaks across CV folds exactly like a shared host would).

| k | null | junction | decoy content | SE_H0 | `junction_within_null_band` |
|---|---|---|---|---|---|
| 4 | 0.5244 | 0.5018 | 0.7111 | 0.0064 | true |
| 6 | 0.4947 | **0.5328** | 0.8068 | 0.0064 | **false — the finding; see pin 7** |

Realized composition after pin 6a: **15,708** plain + **3,701** embedded (`structured_rna` 2,999, 1 masked; `dinuc_shuffled` **702** of 2,000, the rest refused as out-of-fold) = **19,409** admitted negative records → shares **80.9 % / 15.5 % / 3.6 %**. Refusals: 2,008 by pins 4–5, **1,298 by pin 6a**, 1 masked. At f = 10/11 that is ≈ **4.28** draws per record per epoch, and the junction-symmetric rate pin 7 derives from it is **0.19069**.

**Scope limits, disclosed rather than buried.**
- **The single-null-draw band was the wrong instrument, and that is why it is no longer a gate.** Estimated from one pair of unspliced arms, it inherits that pair's noise: across four draws the null moved 0.4947–0.5042 while the junction moved 0.5217–0.5328, and the verdict flipped on which draw you took (1 of 4 passes at k=6; 3 of 4 at k=4). A rule whose verdict depends on the draw cannot certify anything in **either** direction — which is why the response was a construction guarantee, not a wider band.
- **The k-mer probe is a *composition* instrument.** It dilutes a single-position discontinuity that a convolutional or state-space receptive field could read, so ~0.53 is a **lower** bound on what the model might see — one more reason to remove the cue rather than bound it. Tier 2 tests the model actually trained.
- **The junction-symmetric splice does not fire on every drawn window.** A locus that fills its window leaves no admissible all-background interval, so the *realized* positive rate is at or below the configured one; the gate measures the realized rate for exactly that reason and never trusts the configured number.
- **At small n the probe overfits.** At n = 90 the 256-column featuriser separated two *unspliced* host sets at 0.576 while the null draw landed at 0.504. The CI fixture runs at n = 600 for that reason, recorded in the test rather than tuned silently.
- **The embedded negatives remain compositionally separable** (0.74–0.76 on the shipped arms). They are *easy* negatives. That is what a **seed** ratio is for under §9.1 — "a fixed moderate ratio for tractability" — and the hard ones are P2-10e's job. It is stated here so no later report can read the seed mix as evidence the model learned structure.
- **Diversity of the negative arm is not characterised.** Distinct parent loci, homology clusters and taxonomic orders of the 15,708 are **NOT REPORTED** by any committed artifact, and the mined pool carries no cluster or taxonomy column. No claim about clade coverage of the negatives is licensed by this amendment.
- **`negative_fraction` compounds with `steps_per_epoch: null`.** The 11.06× step increase is a *consequence* of the pinned f, not an independent choice, and the P2-06 sweep selected γ = 0.5 / α = 0.5 in the positives-only regime where the foreground share of labelled positions was 21.3 %; at f = 10/11 it falls to ≈ 1.9 %. Those hyperparameters are **not** re-swept here (that is outside this step's carve) and the regime change is recorded as a known caveat on any comparison to P2-09.

**Compute, pre-registered here rather than charged to a budget that does not cover it.** ≈ **18–21 GPU-h / ≈ 2.2–2.6 h wall** on 8× A4000 (P2-05 basis 1.6129 × 11 = 17.7; P2-09 actual 1.89 × 11 = 20.8). **ADR-0003 A2's ≤ 90 GPU-h is a *sweep* bound** — it prices 36 grid points and the string "mining" appears **0×** in ADR-0003 — so it neither authorises nor forbids this production retrain. `train_production.sbatch`'s `--time` is raised from 01:00:00 (which would have killed the run mid-training) to 05:00:00; poll cadence 30 min.

**Not amended.** D14's spare rule and its three model-independent disjuncts; the Tier-2N probe and its per-round halt/rollback; `MIN_REAL_HOMOLOG_N = 20`; D7's pinned 100:1 benchmark decoy prevalence and its 10:1→10⁴:1 sweep (an *evaluation* operating point, distinct from this training ratio — §9.1 says so twice, and A7 does not blur them); A4, A5, A6. The four §9.1 pools **stay four** in `decoys_v0.parquet`; A7 changes only which of them the *training stream* draws from, and A6's "the ~10:1 seed ratio untouched" invariant is superseded by pin 1 making that ratio concrete. **N, the mining-round count, remains unpinned** — P2-10e still owns it.

**Cross-reference impact.** PRD **§9.1** — the training-ratio paragraph now has an operative reading at W = 1024, and its four-class enumeration is a *pool* enumeration, not a per-round training composition; **D14** — its leader-pool retention clause is re-scoped (pin 5) and its mining loop now starts from a checkpoint that has actually seen the seed ratio; **P2-10e** — this run is its round-0 parent, and its warm-start path is unchanged; **GATE-1 / P4** — any recall@matched-precision figure computed on a checkpoint trained *before* this amendment (i.e. P2-09's) is a positives-only-trained number and must be labelled as the pre-mix baseline; **ADR-0006 D12** — unaffected, but its per-class joint false-pass estimate still rests on the 8 `leader_decoy` records this amendment removes from training, so P2-10b′ now closes two things at once; **P2 exit gate** — README + `paper/manuscript.qmd` owe this step a line, batched there per §6.4.

## Amendment A8 — the union-prior mask is fail-open off-catalogue, so the GTDB/RefSeq negative substrate is gated by a model-independent, shard-aware cmsearch pre-scan whose versioned report carries same-artifact must-fire controls; the reported number is a removal/detection YIELD that lower-bounds true contamination (a low value is NOT a clean-pool claim), not a residual (P2-10c′-d, 2026-07-23)

- **Status:** **Accepted (user sign-off 2026-07-23; CLAUDE.md §7 item 2 — all three P2-10c′ masking/env decisions signed off by the lead, bioedca).** Amends **D14**: its "*all known T-box loci … masked from every negative/decoy pool*" clause is satisfied on the union catalogue by `masking.py` but is a **silent no-op on an independent genome** (every GTDB/RefSeq accession is off-catalogue), so this amendment adds the catalogue-independent gate D14 presumes but does not deliver, and re-scopes the union-prior mask to its on-catalogue role. Pins no existing value: A7's `negative_fraction = 10/11` stream share and its "four pools stay four" invariant, D7's 100:1 benchmark decoy prevalence, `MIN_REAL_HOMOLOG_N = 20`, **ADR-0005 D3's freeze of the production Stage-1 threshold at the phase gate [ADR-0005:41]** (this ADR's D3 — the scan-decision-machinery rule — *not* ADR-0004 D3, the cluster-crossing leakage rule), and A4/A5/A6/A7 are untouched; the ADR-0004 positive-split / no-leakage rules are untouched — **this amendment governs negative/background windows only.** Consumed by **P2-10c′** (the GTDB/RefSeq fetch it gates) and **P2-10e** (the mining rounds that draw from the gated substrate). Committed atomically with `src/tbox_finder/mining/substrate_prescan.py` (the shard-aware batched cmsearch pre-scan wrapper + per-shard removal-rate estimator + builder/validator-shared clause set + per-shard→aggregate reducer), `slurm/p2/substrate_prescan.sbatch` (the SLURM-array driver writing per-shard `SHARD_OK`), `scripts/make_substrate_prescan_control.py` (the designed spike/null control-fixture generator), the committed golden fixture under `tests/fixtures/substrate_prescan/` (minted by a real local cmsearch run — §8.7), and their unit + generator-sabotage + ml gates (`tests/unit/test_substrate_prescan.py`, `tests/ml/test_substrate_prescan_control.py`). **No substrate is fetched or scanned by this step** (§10.3): the aggregate `reports/p2/substrate_prescan.json` is produced downstream at P2-10c′/P2-10e behind pin-5's admissibility clauses; this step ships the machinery + tests + the ADR only.
- **Label note:** `A8`. `A4` = P2-07, `A5` = P2-08, `A6` = P2-10b, `A7` = P2-10d′-b. The pre-existing duplicate-`A3` collision (P0-28 and P2-03) is untouched and still needs reconciling at the P2 exit gate.

**Trigger.** The P2-10c′ scope decision ("A + B together", 2026-07-20) made one GTDB/RefSeq fetch serve **both** the D7 model-independent homolog-search target **and** P2-10e's unbounded 1,024-nt negative-window substrate; the ρ-pilot (P2-10c′-a/b/c, PR #72–#75) sizes that fetch and measured **ρ ≈ 0.25–0.59 Stage-1 candidates/Mbp** (max 0.594) over 100 genomes / 287.76 Mbp [`data/processed/pilot/rho_pilot_report.json`] — i.e. the independent substrate is candidate-bearing under the Stage-1 *neural* caller (the mining rationale; **not** a claim about cmsearch yield: ρ is a Stage-1 neural density, a different detector, and a Stage-1 candidate may be a Tier-2N leader cmsearch misses by definition — see scope limits). That substrate is an **independent genome set**, and the union-prior mask (A6/A7) that protected the on-catalogue P2-10d′-a decoy substrate is **fail-open off-catalogue by construction**: `matches_interval_exactly` / `is_masked` return `False` for any accession not keyed in `union_prior.parquet` and for a missing/NaN accession, and the code names the semantics — an unmatched candidate is classified **minable, not protected** (`masking.py`). Independent genomes contain **real, uncatalogued T-boxes**; drawing them as unmasked background would label a true T-box as a negative and directionally train the production scanner to reject the flagship discovery class — the exact harm D14's spare rule exists to prevent [ADR-0005:129-131].

**Measured (from committed artifacts; no new substrate-scan measurement is asserted).** The union mask is fail-open on any off-catalogue or missing accession (`masking.matches_interval_exactly`, `masking.is_masked`); an unmatched window is classified minable. The union prior's accessions are stored **unversioned** — `masking.normalize_accession`'s docstring records "measured P2-10b: **0 of 12,041** union-prior accessions carry a `.N` suffix" — while every GTDB/RefSeq accession is off-catalogue *and* version-suffixed, so a naive cross-accession join against the unversioned catalogue would intersect 0 of N and read as "clean"; this repo already shipped exactly that versioned-vs-unversioned zero-intersection no-op (`masking.accession_namespace_report` exists because of it), and the batched pre-scan below carries a within-namespace join of its own, guarded the same way (pin 5(v)). The only in-repo catalogue-independent T-box detector is cmsearch/`RF00230.cm`, which **misses ~27.6 %** of catalogued T-boxes (`parent_miss_rate = 0.276` [`reports/p2/tier2n_probe.json`]) and misses **Tier-2N by definition** (`n_natural = 0`; the probe substitutes 45 synthetic architecture-departure records as a lower-bound proxy). The union denominator (`len(union_prior.parquet)` ≈ 24,160) lives only in code and is **meaningless off-catalogue**; `decoys.py:720-728` closes the dead-loader hole with a **two-armed** cross-check (a maskable-but-empty raise **and** a strict loaded-vs-expected-inequality raise) re-derived from loci actually loaded, and this amendment's report reproduces that full two-armed check for its own *scanned-window* count, per shard and in aggregate. The **git-frozen must-find genome set** already exists (the P2-10c′-a selection manifest `pilot_genomes_v0.parquet` + its provenance/audit sidecars; the production selection manifest for the full fetch), and the prior committed fetch report (`pilot_fetch_report.json`) is the clause-(viii) diff baseline — never a floor (the ncbi-refetch-429 lesson: a cold re-fetch can 429-drop genomes with every gate green).

**Pinned rules.**

1. **The union-prior mask is retired as the *sole* gate on the independent-genome substrate; it stays only in its on-catalogue role — and this SUPPLEMENTS, does not replace, the PRD §9.1 residual report.** On GTDB/RefSeq it masks ≈ nothing and its union-denominator residual is meaningless, so it may **not** be reported as the contamination gate for that substrate. Its existing on-catalogue role (A6/A7) is unchanged. **PRD scope clarification, signed as part of A8 (CLAUDE.md precedence, PRD > ADR):** PRD:190 mandates "*the residual contamination rate against that union denominator is reported*" for every negative/decoy pool; that report is **retained wherever the pool is on-catalogue**, and this substrate estimator (pin 4) **supplements** it for the off-catalogue substrate PRD §9.1 did not contemplate — it does **not** override PRD:190. Recorded as a one-line PRD §9.1/§9.2 supplement note (PRD is the local-only scope doc; the note lands there, ADRs are the cross-machine fallback).

2. **The independent-genome substrate is gated by a *model-independent* cmsearch pre-scan, at a recall-favouring threshold, and an overlapping window is REFUSED — not labelled background.** Detector = cmsearch/`RF00230.cm` on **`infernal` 1.1.5**, run from `envs/infernal.yml` (or `envs/rscape.yml`, which co-carries `infernal` 1.1.5 via A11's real co-install) **as a separate scan step**. This amendment does **not** co-pin `infernal` with `hmmer` in one env: bioconda `hmmer` and `infernal` each ship their own `esl-*` Easel miniapps into `bin/`, a genuine clobber A11's *vendored* R-scape never faced — cmsearch stays in its own env. A window overlapping a hit (+ the D14 flank) is dropped from the negative/mining pool. This is a **D14 spare rule** — protective, so an error is a bounded sensitivity cost, never a novelty claim. The gate is **never the production Stage-1 scanner** (masking negatives with the model's own detector removes exactly its would-be false-negatives — circular; it inflates GATE-1 [ADR-0006]). **Direction pinned now, value at the phase gate:** the inclusion threshold is recall-favouring (relaxed relative to `--cut_ga`, so the gate errs toward *refusing* windows), pinned as a *rule* now with the numeric cutoff frozen at the P2/P6 phase gate — mirroring ADR-0006 D7, ADR-0005 D3's production-threshold freeze [ADR-0005:41], and the P2-10c′-c-i recall-favouring caller. **No numeric threshold is pinned by this amendment (§10.3)** — `substrate_prescan.scan_shard` runs cmsearch without `--cut_ga` and `score_threshold` is a keyword-required parameter with no default. An optional corroborating `nhmmer`-against-T-box-seed pre-scan becomes admissible only once the separate ADR-0002 `hmmer`/`blast` env amendment (A12) lands — and that amendment, not this one, owns the `hmmer`↔`infernal` clobber verification.

3. **The honest instrument is a triple** — a removal/detection YIELD (a point estimate, NOT a residual), the one-sided **lower** bound this yield places on the true removable-contamination fraction (`removal_rate ≤ true contamination-window fraction`, so a low yield is NOT a clean-pool claim), and the retained **D14 Tier-2N halt**. Against this detector the post-gate residual is ≈ 0 by construction (every detected window is refused); the reported number is the instrument's *removal/detection yield*, not a residual-risk quantity — **low or zero means "few cmsearch-detectable loci here" (an admissible, common case)**, not "clean" *or* "dirty". **Detector liveness is a separate question, answered only by the per-shard spike control (pin 5(ii)/(iv)) and the per-invocation CM-identity check (pin 5(vii)), never by the production removal level.** The residual RISK that remains is one-sided **lower**-bounded, because cmsearch misses ~27.6 % and misses Tier-2N by definition, so `removal_rate` can only *understate* the true fraction. The two legs do **not** compose into any upper bound; the D14 Tier-2N probe + per-round recall-drop halt is powered only by 45 synthetic records with an empty natural arm, so it too is directional. Stated plainly: **residual Tier-2N contamination of the negative pool is UNMEASURED (not bounded above).** The only guarantee is directional — the per-round halt stops mining from training the scanner to reject the Tier-2N class at the probe's current power. Neither leg is claimed sufficient, alone or together.

4. **The estimator is a *removal/detection yield on the PRODUCTION substrate itself* (`substrate_removal_rate`), not a residual against the union denominator; the compute model is SHARD-AWARE — no per-window fast path AND, at ρ-scale, no single global invocation.** `substrate_removal_rate = (production windows with ≥ 1 cmsearch self-hit at the pinned threshold) / (production windows actually scanned, summed over shards)`. A genome-scale substrate (0.7–68 GB) cannot be scanned one window per call, nor in one process (the ρ-pilot already ran across `n_shards = 4`; the production fetch shards far wider), so the scan is **sharded across SLURM array tasks** (CLAUDE.md §9.3). **Each shard runs ONE batched `cmsearch --tblout`** over a multi-record FASTA of *its* windows **plus its own folded-in spike and null control arms in the same invocation**, then writes a per-shard `SHARD_OK` marker and a segment carrying consumed/expected counts, per-arm removal, **the sha256 of the `RF00230.cm` that invocation loaded** (pin 5(vii)), and a per-shard `invocation_id` **re-derived from that single tblout's own data rows** (`substrate_prescan.tblout_identity`), NOT a builder-stamped string — so all three arms carry the same id **because they were parsed from one tblout** (pin 5(vi)). The reducer AGGREGATES the segments; the rate is over the **production** subset only. Hits are grouped back to windows by cmsearch's reported target name — a **within-namespace exact-name join**, guarded always (pin 5(v)); each window carries a **unique, whitespace-free name** (`infernal.write_fasta` enforces it), because cmsearch names a target by the header up to the first whitespace, so a naive header would collapse every window on a contig to one name and **silently deflate** the rate (the pin-2/pin-3 harm). **The production denominator is the count of production windows ACTUALLY CONSUMED — never a manifest/config row count** — but it is cross-checked, per shard and in aggregate, against a **FASTA-independent** expected count derived from the fetched substrate's recorded per-genome window totals and the pinned tiling geometry (`window_nt = 1024` / `stride_nt = 512`), NOT re-counted from the FASTA the scan reads. That expected count is FASTA-independent but **not** fetch-independent (a silent re-fetch shrink moves both sides), which is why genome completeness is separately gated against the git-frozen selection manifest (clause (viii)). **Three disjoint window sets:** the report records `n_production_windows`, `n_spike_windows`, and `n_null_windows` as three separate keys per shard and in aggregate; the rate is computed over production only. **A low or zero production removal rate is EXPECTED and ADMISSIBLE** and is NOT a dead-scan flag; liveness is guaranteed only by clauses (ii)/(iv)/(vi)/(vii). Every reported value carries the pin-3 lower-bound disclosure (`lower_bound_disclosure`).

5. **The gate is a VERSIONED set of SAME-ARTIFACT must-fire clauses re-derived from recorded counts inside `reports/p2/substrate_prescan.json` (per-shard segments + aggregate), whose `overall_pass` is FALSE BY CONSTRUCTION on a dead scan, a dead shard, a degraded CM, or a shrunken fetch** — the A7 `junction_symmetry_clauses` shape [ADR-0005:436] + the `decoys.py:720-728` inline raise, **not** a detached fixture. This repo has shipped a control that passed as a copy of the positive arm with both alignments deleted (the P2-10c matched-control finding), gates that stayed green on zero evidence, and a silently-dead backend that reported "no signal" identically to a genuine null — so each clause below is re-derived from the scan's own counts by a **builder/validator-shared function** (`substrate_prescan.derive_clauses`, called by both `build_report` and `validate_report`; never read back from config), guards emptiness, and carries a must-fire half.
   - **Clause-set completeness is itself gated (`CLAUSE_SCHEMA_VERSION`; A7 precedent).** `overall_pass = all(...)` is over the **REQUIRED, enumerated, versioned** `REQUIRED_CLAUSES` — never `all()` over whatever keys are present. `validate_report` asserts the version equals the pinned constant and **every** required key is present and boolean; a **MISSING clause key is a HARD FAIL**, never silently dropped. A new clause bumps `CLAUSE_SCHEMA_VERSION`; an older report then re-validates FALSE unless every current key re-derives present ([[new-gate-clause-invalidates-old-reports]]). There are no committed substrate_prescan reports yet, so v1 is authoritative.
   - **(i) PRODUCTION denominator — strict `consumed==expected` EQUALITY *and* emptiness, per shard AND aggregate** (reproduces the FULL `decoys.py:720-728`, BOTH raises): re-derived from the production window stream actually consumed, not a config value; `expected` is the FASTA-independent contig×geometry count. A FASTA-write truncation, a failed array shard's dropped windows, or a single-window off-by-one turns this FALSE even though the rate over the *scanned* subset looks healthy. `Σ consumed ≥ MIN_SCAN_N` is also asserted. Backed by clause (viii) for the fetch-shrink case.
   - **(ii) The cmsearch detector code path is LIVE in EVERY shard — per-shard spike-arm removal > 0.** The spike arm embeds KNOWN cmsearch-detectable canonical loci, so a live detector MUST recover > 0 per shard; with the scan disabled / binary off PATH / wrong CM in a single array task, that shard's spike removal is 0 → FALSE (the A7 "augmentation disabled ⇒ clause FALSE" analogue). Being per-shard, it catches one dead shard among many. This — not the production removal level — is the dead-scan flag.
   - **(iii) Power control — matched spike/null arms, each over a min-N-guarded set, folded into EVERY shard.** A committed matched pair generated by `scripts/make_substrate_prescan_control.py` — a spike arm of known T-box loci embedded into background, and a length- and composition-matched null (a composition-preserving shuffle of the spike window) — is embedded into each shard's invocation and run through the byte-identical estimator. Per shard, `n_spike_windows ≥ MIN_SPIKE_N (20)` and `n_null_windows ≥ MIN_NULL_N (20)`, and matchedness (equal window count, equal length multiset, identical base composition) is asserted from recorded arm metadata; deleting/emptying either arm drops it below its min-N floor → FALSE. **No absolute recall number is pinned as the floor** (the 0.73 / 0.276 references are provenance, not the operating floor); the operative admissibility is clause (iv).
   - **(iv) Power control — STRICT separation of spike from matched null, per shard.** `removal(spike) − removal(null) > MARGIN` — **strict `>`, MARGIN = 0.0** — re-derived from recorded counts at the operating threshold, never inherited from the reference sensitivities. This distinguishes **"no power" (control fails → estimator inadmissible) from "no signal" (control fires + low production removal → genuinely clean at the detector's sensitivity)**. Copying the spike arm into the null slot makes the difference 0 ≤ MARGIN → FALSE; a dead shard (0 in both arms) → 0 ≤ MARGIN → FALSE.
   - **(v) Hit→window mapping totality + unique naming, per shard, all three arms.** `n_distinct_target_names == n_windows_scanned` (unique names, so grouping cannot collapse windows on a contig) and `n_hits_reported == n_hits_joined` (every reported hit maps to a scanned window). The join is on the **exact** name (no normalisation), so mutating a `.N` suffix on one side drops that hit's join → `reported > joined` → FALSE — the versioned-accession no-op guard, per shard (verified by a build-path sabotage test).
   - **(vi) SHARD completeness + one-tblout co-invocation, re-derived from the recorded segments.** `n_shards_recorded == n_shards_expected`, every shard carries `SHARD_OK`, every shard records `n_tblout_files == 1`, and each shard's production `invocation_id` EQUALS its spike- and null-arm ids (which holds because all three parsed one tblout). A missing/failed array task, an omitted shard, `n_tblout_files ≠ 1`, or a split id → FALSE. This makes a per-shard dead scan turn `overall_pass` FALSE **on the cluster runtime report** — the report the substrate is admitted on — not merely in CI. Co-invocation alone cannot mask a degraded production CM: clause (vii) pins the loaded CM independently.
   - **(vii) CM IDENTITY — the production scan loaded the pinned `RF00230.cm`, re-derived per invocation.** Each shard records the sha256 of the CM its invocation loaded; the clause asserts it equals the pinned `RF00230.cm` sha256 `082616efe0616479dc9b9f565b5bcab7648d7ea1d629430940319f247af52cea` [`reports/p2/tier2n_probe.json:cm_sha256`] in **every** shard. A truncated/swapped/wrong-path CM changes the recorded sha256 → FALSE, and — crucially — this fires **independently of co-invocation (vi)**: a production window scanned against a degraded CM that recovers ≈ no T-boxes is caught here, closing the hole where (i), (ii)/(iv), and (vi) all pass while genuinely T-box-bearing windows escape refusal.
   - **(viii) GENOME COMPLETENESS — every SELECTED genome scanned, re-derived against the GIT-FROZEN selection manifest, plus an enumerated within-genome no-shrink clause.** `n_genomes_scanned == n_genomes_selected` and the scanned accession set **equals** the set read from the committed selection manifest (`pilot_genomes_v0.parquet` for the pilot; the production selection manifest for the full fetch) — a git-frozen must-find set, never the mutable fetch output. Additionally the per-genome consumed-window totals are diffed against the **prior committed fetch report** as an enumerated must-fire clause: **no genome's window total may shrink** (pinned tolerance: exact, `scanned[acc] ≥ prior[acc]`), catching the failure clause (i) cannot — where `expected` shrinks to match `consumed` (both from the shrunken fetch) so `consumed == Σ expected` still holds while the substrate is silently missing genomes. **Maiden-fetch behaviour is pinned:** on the first production fetch there is no prior report, so the diff leg is satisfied by the git-frozen set-equality alone, and the first fetch report is itself committed as the baseline **before any scan is admitted** (so the second run's shrink leg has a baseline to diff against). A silent 429-drop of *k* genomes makes `n_genomes_scanned < n_genomes_selected` → FALSE. This is the git-frozen backstop to (vi)(a) and the reason (i)'s expected count is FASTA-independent, not fetch-independent.

6. **Version, CM identity, denominator, schema, and thresholds are frozen / asserted, not assumed (A1/A11 precedent).** The cmsearch binary version (`infernal` 1.1.5) is asserted against the running binary; the covariance-model identity is pinned and asserted per invocation (`RF00230_CM_SHA256`); the estimator's production denominator is windows actually consumed, cross-checked against the FASTA-independent expected count, with genome completeness re-derived against the git-frozen manifest; and the phase-gate-frozen threshold, the pinned CM sha256, `CLAUSE_SCHEMA_VERSION`, `MIN_SCAN_N`, `MIN_SPIKE_N`, `MIN_NULL_N`, and `MARGIN`, once pinned, are **code-frozen with no CLI/config override** [ADR-0005:222 A1 precedent], so no committed report can silently contradict the pin or the clause set.

**Scope limits, disclosed rather than buried.**
- **This resolves the *masking* problem, not the PRD §9.2 order-level-negatives question.** The gate is a **locus-level** filter (like the union mask): it removes windows overlapping a *detectable* T-box, not windows drawn from a genome that merely *hosts* a held-out-order T-box. No sentence in PRD §9.2 forbids a background window from a held-out-order host from entering training under leave-one-order-out. This amendment **neither closes nor widens** that gap; it stays governed by locus-level masking only (that question is the separately-signed §9.2 a1+b2 decision, encoded in ADR-0004 + PRD §9.2 + `tests/ml/test_no_leakage.py`).
- **The residual RISK is lower-bounded, permanently, and the Tier-2N leg is UNMEASURED above.** `removal_rate` can only understate the true contamination-window fraction: even a maximally-relaxed cmsearch cannot recover an architecture-departed Tier-2N T-box; the 45-synthetic-record D14 probe is a directional halt, not a bound on true off-catalogue Tier-2N contamination, which is unmeasured. Pin 3's D14 backstop is load-bearing, not decorative — but it is not a sufficiency claim.
- **The production removal level is a yield, not a liveness or cleanliness verdict.** A low or zero production removal rate is admissible and expected (few CM-detectable loci) and must never be read as a dead-scan flag; liveness is clauses (i)/(ii)/(iv)/(vi)/(vii); cleanliness against Tier-2N is explicitly unmeasured. The report's naming (`substrate_removal_rate`, never "residual") is designed to prevent the "high = bad / low = clean" category error.
- **ρ does not license any cmsearch expectation.** ρ is a Stage-1 *neural* candidate density; it establishes only that the substrate is candidate-bearing for the mining rationale and sizes the fetch. It does **not** imply a nonzero cmsearch removal, because a Stage-1 candidate may be exactly a Tier-2N leader cmsearch misses by definition.
- **No contamination value is asserted here.** The substrate is not yet fetched/scanned; the number is measured at P2-10c′/P2-10e behind pin 5's admissibility clauses (§10.3). The scan runs under SLURM as a sharded array, sized by the ρ-pilot; **no compute is charged to this design pin.**

**Not amended.** D14's spare rule and its three model-independent disjuncts; **D14's Tier-2N probe and per-round halt/rollback (inherited unchanged as pin 3's directional backstop)**; `MIN_REAL_HOMOLOG_N = 20`; D7's 100:1 benchmark decoy prevalence and its 10:1→10⁴:1 sweep; A7's `negative_fraction = 10/11` and "four pools stay four"; A4, A5, A6, A7; the union mask's on-catalogue role (A6/A7); **ADR-0005 D3's freeze of the production Stage-1 threshold at the phase gate [ADR-0005:41]**; and the ADR-0004 positive-split / no-leakage rules — this amendment introduces **no** taxon/order-level negative exclusion, and does **not** co-pin `infernal` with `hmmer` (that is the pending ADR-0002 A12 scope). **N, the mining-round count, remains unpinned** — P2-10e still owns it.

**Cross-reference impact.** PRD **§9.1 / §9.2** — the "masked from every negative/decoy pool" clause now has an operative gate on an off-catalogue substrate (a model-independent, sharded cmsearch pre-scan), which **supplements** (does not replace) the PRD:190 union-denominator residual report wherever the pool is on-catalogue (pin 1); PRD **§9.2** is explicitly *not* amended (the order-level-negatives gap is disclosed, not closed); **D14** — its masking clause is delivered for independent genomes and its Tier-2N halt re-affirmed as the directional (not sufficient) backstop; **ADR-0005 D3** — its recall-favouring Stage-1-threshold-freeze pattern is mirrored, its frozen value untouched; **ADR-0004** — its locus-level-only scope for negatives is re-stated, not changed; **ADR-0006 D7/D2** — the masking gate is model-independent and never the RF00230-confirmation or Stage-1 path (anti-circularity), and the recall-favouring threshold mirrors D7's "rule now, value at the phase gate"; **ADR-0002** — this amendment runs cmsearch from the existing `envs/infernal.yml` / `envs/rscape.yml` and does **not** co-pin `hmmer`; the pending `hmmer`/`blast` env (A12) enables the optional `nhmmer` corroboration and owns the genuine `esl-*` clobber verification; **`reports/p2/tier2n_probe.json`** — supplies the pinned `RF00230.cm` sha256 the CM-identity clause (vii) asserts against; **CLAUDE.md §9.3** — the sharded scan writes per-shard `SHARD_OK` markers and zero-byte `.err`, verified artifact-based; **P2-10c′-a** — its committed selection manifest is the git-frozen must-find set clause (viii) re-derives against, and the prior committed fetch report is its diff baseline; **P2-10c′** — this gate is a prerequisite of admitting the fetched GTDB/RefSeq substrate, and ρ both sizes the fetch and supplies the deterministic FASTA-independent expected-window geometry the equality guard consumes; **P2-10e** — its mining pool draws only from the gated substrate and its Tier-2N halt is the backstop; **GATE-1 / P4** — any recall@matched-precision figure trained on the gated substrate must cite this gate as the negative-substrate provenance; **P2 exit gate** — README + `paper/manuscript.qmd` owe this step a line, batched there per §6.4.

## Amendment A9 — Hard-negative mining round count N + mining GPU-h budget + the provisional mining-internal detection criterion (P2-10e, 2026-07-26)

- **Status:** **Accepted (user sign-off 2026-07-26; CLAUDE.md §7 item 2, bioedca).** Amends **D14**.
- **Label note:** `A9`. `A4` = P2-07, `A5` = P2-08, `A6` = P2-10b, `A7` = P2-10d′-b, `A8` = P2-10c′-d. The pre-existing duplicate-`A3` collision (P0-28 and P2-03) is untouched and still needs reconciling at the P2 exit gate.

**Why.** D14 pins the mining spare-rule + Tier-2N per-round halt/rollback but pins neither a round count N nor a mining GPU-h budget; A6/A7/A8 each record verbatim *"N, the mining-round count, remains unpinned — P2-10e still owns it."* This amendment closes that, and resolves the circular-gate-ordering blocker P2-10e inherited.

**Pin 1 — round count N = 4 (fixed).** Four §9.1 hard-negative-mining rounds off the A7 cold-start round-0 production checkpoint. The D14 per-round Tier-2N halt/rollback governs **early stop at k < 4** (a per-round recall regression vs the running best halts and rolls back to the best round). N is chosen on **training/mining-side criteria only** — the halt/rollback guard, diminishing hard-negative yield, and the GPU-h budget — and is **NOT** tuned against D3's Stage-1/Stage-2 operating thresholds (those are frozen only downstream at the §13.1 phase gate). The PRD does not constrain N (§9.1 is criterion-terminated, *"iteratively mine"*), so this pins a number the PRD leaves open — no §2.3 precedence carve-out fires.

**Pin 2 — mining GPU-h budget ≈ 135 GPU-h (a NEW cost centre).** Per-round ≈ **34 GPU-h**, measured-anchored:
- Leg 1, scan the 3,630,683-window a2 negative substrate with the current checkpoint = 3,630,683 / 67.68 win/s/GPU / 3600 = **14.9 GPU-h** (throughput measured, ρ-pilot job 687, forward-only, same 1024/512 tiling).
- Leg 2, collect FPs + apply spare rule ≈ **0 GPU-h** (torch-free numpy on the Leg-1 logits; ~2–3 h CPU wall, overlappable).
- Leg 3, retrain from round k−1 ≈ **19 GPU-h** (measured P2-10 production retrain, job 680, 2.4 h × 8 A4000); drifts up modestly with round index as accumulated FPs enlarge the stream.
- N = 4 → **≈ 135 GPU-h**. Conservative ceiling **≈ 145–290 GPU-h** disclosed as headroom (only if the forward-only inference speedup does not hold and the scan runs at the 18.8 win/s/GPU training floor).

This budget is a **cost centre distinct from** ADR-0003 A2's ≤ 90 GPU-h *sweep* bound (which prices the 36-point P2-06 config grid; the string *"mining"* appears **0×** in ADR-0003) and from D6's P5 scan ceiling. It is pinned **here**. A paired ADR-0003 note may record the new cost centre for compute-governance completeness. Per-round **SLURM submit-ack is still required** (§9.3) — this pins the budget, not a blanket submit authorisation.

**Pin 3 — the provisional mining-internal detection criterion (resolves the circular gate-ordering blocker).** P2-10e's per-round Tier-2N gate needs `recovered_ids`, which needs a Stage-1 detection criterion (posterior threshold + along-sequence locus construction: minimum span, gap-merge) that `scan.py` defers (*"still a later step"*) and D3 freezes only at the §13.1 phase gate — genuinely **downstream** of these rounds. Resolution:

- Pin a **provisional** Stage-1 detection criterion (posterior threshold + min-span + gap-merge) used **only** to compute the per-round Tier-2N recall (and, with the same operator, to collect the model's false positives on the negative substrate).
- Held **identical across all N rounds** (cross-round-constant), so its absolute value **cancels** in `round_decision`'s best-round-so-far *relative* comparison — the halt measures the model's change, not the criterion's value.
- Declared **explicitly non-binding** on D3's §13.1 phase-gate freeze and the P5 production operating point. It does **not** become the D3 value; D3's freeze is untouched.
- Requires implementing the locus-construction step `scan.py` flags as deferred (posteriors → called loci → `recovered_ids`).
- **Degenerate-criterion guard:** sanity-check the provisional criterion resolves the probe set at a non-degenerate recall (not ≈1.0 which never fires the halt, not ≈0 which halts immediately) before the first round's decision is trusted.

Rationale for provisional-over-reorder: reordering (pinning the final D3 operating point before mining) would freeze a **binding, test-set-facing** threshold on a **pre-mining** model, then change the model under it — a circularity/validity risk against the project's #1 constraint, and it fights D3's *"frozen at the phase gate … never tuned on the test set"* design. A fully criterion-free path does not exist (nothing maps posteriors → `recovered_ids` without *some* criterion), so the honest move is to pin one explicitly and lean on the shipped relative baseline to keep its value non-load-bearing.

**As encoded (P2-10e ENCODE).** The provisional criterion lives in `src/tbox_finder/eval/mining_criterion.py` (`PROVISIONAL_THRESHOLD` / `PROVISIONAL_MIN_SPAN` / `PROVISIONAL_GAP_MERGE`), which **reuses** `infer/call.py::call_candidates` (the D3 operator, value-unpinned for the ρ-pilot) rather than re-deriving locus construction, defines `sequence_is_recovered` / `recovered_ids` (→ `eval/tier2n_probe.probe_recall`), and implements the degenerate guard as an **identity** check `0 < |members ∩ recovered| < |members|` (`guard_non_degenerate`, a §7 stop on a degenerate round-0 recall).

**Not amended.** D3's §13.1 phase-gate threshold freeze (untouched); `MIN_REAL_HOMOLOG_N = 20`; D7's 100:1 benchmark prevalence; A7's `negative_fraction = 10/11`, cold-start round-0, and the four-pools invariant; A4/A5/A6/A8.

**Cross-reference impact.** **P2-10e** (consumer; round-0 parent = the A7 cold-start `stage1_production` md5 `91bd49dd…` / `stage1.pt` sha256 `09931a22…`) — encodes the criterion (`eval/mining_criterion.py`), the round driver (`mining/mine_round.py`), and `slurm/p2/mine_round.sbatch`; its **N=4 RUN is blocked** at the D14 round-readiness gate until a protective spare-rule backend is MSA-producible (ADR-0006 **D7/A1** per-candidate homolog-search + de-novo MSA supply — a downstream step), the fail-closed behaviour recorded in ADR-0006 **A2**; **D14** — the round count / budget / criterion slot it left open is filled, its spare rule + Tier-2N halt inherited unchanged; **D3** — its recall-favouring "rule now, value at the phase gate" pattern is mirrored, its frozen value untouched (the provisional criterion is non-binding on it); **P4/P5** grade the final checkpoint; **ADR-0003** compute reconciliation — mining is a new cost centre, not under A2; **P2 exit gate** — README + `paper/manuscript.qmd` owe this step a line, batched there per §6.4.

## Amendment A10 — The per-candidate covariation-producer mining-time compute is a NEW CPU cost centre (measure-first budget + decoupled SLURM-array orchestration); corrects A9's ≈0 Leg-2 pricing (P2-10e-msa-producer, 2026-07-27)

- **Status:** **Accepted (user sign-off 2026-07-27; CLAUDE.md §7 item 2, bioedca).** **Amends A9** (Pin 2's Leg-2 pricing). No ADR-0006 pin is touched (Pin 3).

**Context.** A9 Pin 2 priced the round's spare-rule leg as *"Leg 2, collect FPs + apply spare rule ≈ 0 GPU-h (torch-free numpy on the Leg-1 logits; ~2–3 h CPU wall, overlappable)."* That pricing predates the covariation producer (`homolog_msa.py`; "mlocarna" appears 0× in ADR-0005) and is wrong: populating each FP's `SpareRuleEvidence.any_helix_rscape` requires a per-candidate **3-conda-env pipeline** (nhmmer homolog-search vs the A1 2,500-genome FM-index → mlocarna Sankoff align → R-scape), ~120–180 s/candidate (job-766 whole-pipeline prose ≈ 2 min; per-stage split unrecorded), over an FP-per-round count that is **unmeasured** (only the ρ-pilot proxy ρ∈[0, 0.594]/Mbp, flagged `provisional_grids_bind_nothing=True`). At the round sbatch's own 5 h wall the serial crossover is ~150 candidates; at the ρ-proxy top, ~5–9 days/round — 20–40× the A9 per-round GPU budget. This is a NEW cost centre A9 under-counted.

**Pin 1 — Measure-first, two-phase budget.** The producer's mining-time cost is pinned in two phases, not estimated from the binding-nothing ρ-proxy (§10.3):
- **Phase 1 (measure).** In round 0, after leg (a) scans the a2 negative substrate (660 genomes / 3,630,683 windows) with the A7 cold-start checkpoint under the provisional detection triple (τ=0.9 / min_span=50 / gap_merge=10, A9 Pin 3), record the **real FP-candidate count N₀**, then run the per-candidate producer on a **bounded deterministic sample of K ≥ 50** accession-sorted FP candidates on the cluster, capturing per-stage wall (nhmmer / mlocarna / R-scape) and homolog depth per candidate. Report the measured per-candidate wall distribution + the extrapolated full-round envelope. These are §9.3-verified artifacts, not estimates.
- **Phase 2 (pin).** With N₀ and the measured per-candidate wall, pin the decoupled-array sizing (array width, cpus-per-task, est CPU-core-h, est wall) and record the re-priced per-round producer envelope as an A10 addendum, disclosed alongside A9's ≈135 GPU-h. The N=4 RUN's producer cost is the Phase-2-pinned envelope.

**Pin 2 — Orchestration: a decoupled SLURM-array over FP shards.** The producer runs as its own SLURM **array job between leg (a) and leg (d)**, CPU-only (`partition=gpu`, **no `--gres`** — CPU tasks on node `two`'s 96 cores; the `compute` partition = `zero` remains forbidden, §9.2), each array task processing a shard of the round's FP candidates through the 3-env pipeline (mlocarna `--threads` ≤ 2–3, LocARNA scaling caveat) and writing per-candidate covariation status to a table keyed by `candidate_id`; the GPU retrain leg (d) consumes the merged table. This **scores every FP** (full mined-negative yield — no cap, no subsample) and removes the 5 h-wall ceiling from the producer while retaining the whole-8-GPU-node allocation for legs (a)+(d). Each `MiningCandidate` (coordinates only) is re-resolved to nucleotides from the A1 genome FASTA before search.

**Pin 3 — Governance: A10 amends A9 only.** Because every FP is scored (no subsample that would mine an unscored candidate), A2's fail-closed sparing semantics are untouched (unscored / None-MSA → `unavailable` → **spared, never mined**). The change is purely the mining cost centre A9 owns → **ADR-0005 A10 only**; ADR-0006 D7/A1/A2/A3 thresholds, the `TOTAL_ACROSS_HELICES` operator, and `min_sequences=20` are unchanged.

**Scope guard (§10.3).** A10 pins the measurement plan, the orchestration architecture, and the governance locus. It does **not** itself pin the budget number (Phase 2, on measured N₀ + wall), does **not** change any detection threshold / verdict operator / power floor / sparing decision, and does **not** authorize a submit — Phase 1's round-0 scan + producer-sample is a downstream §9.3 submit-ack.

**§10.1 evidence gate:** N/A — A10 is a compute-orchestration/budget-governance decision (ordinary engineering); it asserts no biological/statistical fact. (The inherited `min_sequences=20` power floor was §10.1-cleared in ADR-0006 A2.)

**As encoded (P2-10e-msa-producer ENCODE).** The producer is `src/tbox_finder/mining/covariation_producer.py` — the per-candidate `resolve → search → align → score` composition of the `homolog_msa` primitives, split into one shard-level subcommand per env (`search_shard` / `align_shard` / `score_shard`, promote-don't-duplicate) plus `merge_status_tables` (per-shard tables → one `candidate_id → status` table). The wiring is `mining/mine_round.py`: `window_candidates_to_mining(..., covariation_status=…)` replaces the default all-`unavailable` `SpareRuleEvidence()` with the merged table's per-candidate status (an id absent from the table resolves to `unavailable` ⇒ spared — a dropped shard cannot fail open), `apply_spare_rule` is leg (d)'s status→mined/spared bridge, and `scan_admissible_genomes` / `load_admissible_accessions` are leg (a). Orchestration: `slurm/p2/mine_round.sbatch` (legs a–d; the decoupled leg-(c) array is `mine_round_producer.sbatch`, the retrain is `mine_round_retrain.sbatch`) and the Phase-1 measurement `slurm/p2/mine_round_measure.sbatch` (a distinct §9.3 submit-ack). `MSA_SUPPLY_AVAILABLE` stays `False` until Phase-2 pins the budget addendum.

## Sign-off
☑ recorded 2026-07-27 (bioedca), CLAUDE.md §7 item 2 — per-candidate covariation-producer mining-time compute pinned as a new CPU cost centre: measure-first 2-phase budget + decoupled SLURM-array orchestration (scores every FP); amends A9's ≈0 Leg-2 pricing; no ADR-0006 pin changed.

### A10 Phase-2 pin — the per-round producer budget, measured (P2-10e-msa-producer, 2026-07-28)

- **Status:** **Accepted (user sign-off 2026-07-28; CLAUDE.md §7 item 2, bioedca).** Fills the Pin-1 Phase-2 slot on the §9.3-verified round-0 measurement (job 816). Amends no other pin. **Documented, not exercised** — the N=4 RUN is deferred (§7 decision below), so this envelope is *disclosed, not spent*.

**Measured inputs** (`reports/p2/mine_round_measure.json`, job 816; §9.3-verified — `squeue` empty · `.err` 0 bytes · DONE marker · `n0_false_positives` > 0 · `k_sample` ≥ 50):
- **N₀ = 941** round-0 FP candidates over the 660 a2-admissible genomes, at the A9 Pin-3 provisional detection triple (τ = 0.9 / min_span = 50 / gap_merge = 10).
- **K = 50** accession-sorted producer sample: per-candidate wall **mean 16.92 s / median 9.01 s / max 114.22 s**. Stage split: search[nhmmer] mean 9.43 / **max 114.22 s** (the right-skew tail), align[mlocarna] mean 7.37 / max 47.53, score[R-scape] mean 0.11. Homolog depth median 20 / mean 21.4 / max 73. Covariation status {passed 16, failed 10, unavailable 24}.

**Pin — decoupled-array sizing (the A10 Pin-2 architecture, priced on the measured wall):**
- **Per-round producer cost ≈ 4.42 CPU-core-h** (sum-faithful: 941 × mean 16.92 s), **not** the report's median-based `est_core_h = 2.35`. The median understates by **1.88×** because the nhmmer search stage is right-skewed; §10.3 requires sizing on the sum, not the median.
- **Array width = 48, cpus-per-task = 2** for `slurm/p2/mine_round_producer.sbatch` on one 96-core gpu-partition node (**no `--gres`**, CPU-only; `compute` = `zero` stays forbidden, §9.2). All 48 tasks run concurrently (48 × 2 = 96 cores, no queueing), giving each nhmmer/mlocarna 2 threads for the search tail. **Per-round wall ≈ 7 min** (mean 5.6 / worst-case 7.3 min; the worst-case guarantees ≥ 1 full 114 s nhmmer tail per round). Width 96 (1 core/task) halves the wall but leaves no threading headroom for the tail — **48 is the pinned width**.
- **0 GPU-h.** The producer is CPU-only and orthogonal to A9's ≈ 135 GPU-h scan budget; against that envelope the ≈ 4.4 core-h / < 8-min producer is negligible and never the bottleneck. Over N = 4 rounds ≈ **17.7 core-h** total.

**Deferral — the §7 structural-0-yield decision (2026-07-28, bioedca).** The N = 4 RUN is **skipped/deferred**. Verified from code this session (2 independent traces + an adversarial refuter, 0 errors): mining a candidate requires **all three** model-independent disjuncts to run-and-`fail` (`is_mining_excluded`, `mining/spare_rule.py`), but `relaxed_architecture` and `downstream_aaRS_synteny` have **no backend** (`candidate_evidence` sets only `any_helix_rscape`; the other two default `unavailable`), so all 941 FPs are spared — the producer is **1 of 3** backends: it makes `mining_round_readiness` return `ready = True` but yield is structurally **0** (each round's retrain skips on `n_mined = 0`). This is the signed fail-closed conservatism (ADR-0006 A2 / D9 row 5 / D11; ADR-0005 D14) and the D14 diminishing-yield halt at k = 0. `MSA_SUPPLY_AVAILABLE` stays **False**; ~0 GPU-h spent. Real mining yield is gated on building the two remaining backends (a new step).

**Scope guard (§10.3):** records the measured envelope only; pins no threshold / operator / power-floor / sparing change; authorizes no submit; changes no code; flips no flag.
**§10.1 evidence gate:** N/A — compute-budget arithmetic on measured wall (ordinary engineering); asserts no biological/statistical fact.
**Sign-off (A10 Phase-2):** ☑ recorded 2026-07-28 (bioedca), CLAUDE.md §7 item 2 — per-round producer budget pinned on the measured round-0 numbers (N₀ = 941; ≈ 4.42 CPU-core-h/round; array width 48 × cpus-per-task 2; ~7 min wall/round; 0 GPU-h). The N = 4 RUN is deferred (structural-0-yield; `MSA_SUPPLY_AVAILABLE` stays False); envelope documented, not exercised.

## Amendment A11 — Stage-1 temperature scaling: one shared scalar T on the per-nucleotide 8-class logits, fitted by an exact convex 1-D solve, applied **before** the D3/A3 reconciliation operator; the P2-13 reliability read is per-class one-vs-rest, **non-gated**, and fitted/read on cluster-disjoint halves of `selection_val` because the D11 calibration split does not exist until P3-02 (P2-13, 2026-07-30)

- **Status:** **Accepted (user sign-off 2026-07-30; CLAUDE.md §7 item 2, bioedca — "sign A11 and land the whole step now", with the fold and T-form options taken as drafted).** Extends **D11** to Stage 1. Pins no new numeric gate default and moves no gate.
- **Label note:** `A11`. `A9` = P2-10e, `A10` = P2-10e-msa-producer. The pre-existing duplicate-`A3` collision (P0-28 and P2-03) is untouched and still owed at the P2 exit gate.

**Why.** D11 pins the recalibration stack order (*train → temperature-scale on a disjoint calibration split → prior-shift*), the binned-ECE estimator (**15 equal-mass bins, debiased**), and the gated object (the *named posterior*, pre-prior-shift, graded at **P3 exit**) — but it pins them **"on the positive-class posterior"**, i.e. for Stage-2's binary head (`ADR-0005:111`; ADR-0001 D1 gives Stage 2 the *"calibrated binary"* head). Stage 1 emits **per-nucleotide 8-class logits** (`ADR-0001:31`). Four things were therefore genuinely open for P2-13, each verified absent from `PRD.md` and all six ADRs:

1. the **calibration axis** for a per-nt multi-class segmenter (`PRD.md` has no sentence on it; `isotonic`, `Platt`, `reliability diagram` = 0 hits in both PRD and ADRs);
2. the **parameterisation** — one shared scalar T vs per-class (vector) T (`single-T`, `vector scaling` = 0 hits);
3. the **fit objective/optimiser** (`LBFGS` = 0 hits; `imp.md`'s *Notes* suggest PyTorch LBFGS — a suggestion, not a pin);
4. **which fold** the P2-13 fit uses — D11's *"disjoint calibration split"* is the **only** occurrence of that phrase in the PRD (`PRD.md:248`), ADR-0004's fold vocabulary contains **no** calibration fold, the committed split table has **no `calib` column**, and `imp.md` assigns that carve to **P3-02** (*"a disjoint calibration subset carved from the training folds, whole-cluster-assigned"*).

**Pin 1 — axis: the per-nucleotide 8-class logit vector; the read reduces one-vs-rest per class.** T is fitted on the same axis the loss trains (`PRD.md:236`, per-nucleotide cross-entropy). The reliability read calls the **frozen** `metrics.binned_ece` (15 equal-mass, debiased) **once per class** on the one-vs-rest posterior, and reports the **per-class table** plus the **worst (max) over the three ADR-0004 D6 core elements** {Stem I, Specifier, Antiterminator}. **No cross-class mean** is reported — mirroring GATE-4's *"no cross-unit mean"* discipline (`PRD.md:246`); the only macro-average the PRD pins for ECE is **over held-out orders** (`PRD.md:247`), a P3/P4 statistic, not this one. `binned_ece` is **called, never forked**; P2-13 promotes the bin table out of it as `metrics.reliability_bins` so the plotted curve and the reported number are one arithmetic, and the committed hand-computed expectations in `tests/ml/test_eval_gate.py` (reproduced **bit-exactly** across that refactor) remain the non-tautological guard.

**Pin 2 — parameterisation: ONE shared scalar T > 0, applied as `logits / T`.** The method of the paper D11 already cites (arXiv:1706.04599), whose vector/matrix-scaling variants underperform it. Per-class (vector) scaling is **not** adopted; adopting it later is its own amendment, because it is the variant that can move an arg-max at will and would therefore re-open GATE-4's graded quantity.

**Pin 3 — fit: an exact, deterministic 1-D convex solve in β = 1/T; not SGD, not LBFGS.** With `β = 1/T`, `NLL(β) = Σ_i [ logsumexp_c(β·z_ic) − β·z_{i,y_i} ]` is **convex in β** (log-sum-exp composed with a linear map, less a linear term) and `dNLL/dβ = Σ_i [ Σ_c p_ic·z_ic − z_{i,y_i} ]` is monotone increasing, so the fit is a **bracketed bisection on a monotone derivative** — exactly reproducible, **no seed, no learning rate, no optimiser state**, and importable in the bare-CI tier (numpy only), so the unit gate is **not** torch-gated (CI installs no torch). **Fail-closed, in both degenerate directions, each guarded separately:** the exact `β → ∞` limit `Σ_i (max_c z_ic − z_{i,y_i})` is 0 **iff** every position is already its own arg-max (no minimiser exists; T → 0) — checked exactly because the *sampled* gradient underflows to 0.0 there and a `|grad| ≤ tol` rule would read that flat tail as stationarity and certify an arbitrary T; and a sign check at each bracket endpoint refuses a minimiser lying outside `T ∈ [1e-3, 1e3]`. Neither returns an endpoint dressed as a solution.

**Pin 4 — where T sits relative to the frozen reconciliation operator: BEFORE it, and the arg-max consequence is MEASURED, not assumed.** The D3+A3 operator consumes **per-window logits** and internally takes `log_softmax` per window before the coverage-normalised average (`ADR-0005:284`), so the calibrated deployment order is `logits / T → reconcile → posterior`. Two consequences, one counter-intuitive:

- a single T **preserves each window's own arg-max exactly** (strictly monotone);
- it **does not necessarily preserve the *reconciled* arg-max** where coverage ≥ 2, because the operator averages *probabilities* across overlapping windows and re-tempering the summands can change which class wins that average.

So temperature scaling is **not** a priori GATE-4-neutral, and the P2-12 flag (*"check whether temperature scaling stabilises the Antiterminator min-core-F1 under the frozen D3 arg-max operator"*) is an **empirical** question, not one answerable by invariance. The P2-13 artifact therefore reports `n_positions_argmax_changed`, `n_positions_coverage_ge_2`, and the per-class F1 / boundary-IoU delta. **GATE-4 at P2-14 is graded on the uncalibrated posterior** unless a later signed decision says otherwise; A11 changes no gated quantity.

**Pin 5 — the P2 fold, and what the P2-13 numbers may and may not be called.** No D11-disjoint calibration split exists at P2. Rather than invent one or silently reuse a selection fold:

- **Fit and read on cluster-disjoint halves of the P2-06a `selection_val` inner rung** (830 records / 469 clusters): a **whole-cluster** seeded halving (`CALIB_SPLIT_SEED = 20260730`, `fraction = 0.5`, drawn to a record-count target exactly as `selection_val_cluster_ids` draws its rung) — T fitted on half A, reliability read on half B — so the read is **out-of-sample for T**. Because two ~equal halves make a swapped fit/read wiring invisible to every count in the artifact while turning the read in-sample for T, the halves are validated by **identity**: the report carries the cluster→size mapping and its validator **re-cuts** the fit half from it, rather than reading back a boolean.
- **Mandatory disclosures, shipped as report constants (not prose):** (i) `selection_val` is the fold the P2-06 sweep **selected on**, so it is **NOT** the D11 disjoint calibration split; (ii) **no T from P2-13 is shipped or consumed by any gate** — GATE-2's T is re-fitted at **P3-02/P3** on the `calib` carve; (iii) the report carries `gated: false`, `is_science: false`, and every ECE with a **cluster-blocked** bootstrap CI; (iv) the CI is a **200-replicate** percentile interval (the frozen stdlib estimator costs ~0.6 s per call at this N, so the D5 default of 2000 would be hours of pure-Python sorting for a non-gated read) over ~235 clusters, and is disclosed as coarse; (v) the fit objective is the **per-window** NLL while the read is on the **reconciled** posterior — a deliberate mismatch (convexity on one side, the deployment object on the other), with both reported; (vi) the per-nt class mix is background-dominated, so the pooled-NLL fit is background-dominated.
- **The prior-shift stage is refused fail-closed at P2.** The artifact stops at the *named posterior*; the Saerens/Elkan correction is P5. The stack-order record P2-13 owes is a machine-checked constant + validator clause, not a sentence.

**Alternatives rejected.** (a) **Pull P3-02's `calib` carve forward** — re-opens the committed, git-LFS split table and its closed schema and forces a full-corpus no-leakage re-run (§8.5) to serve a **non-gated** read, duplicating a step `imp.md` assigns elsewhere. (b) **Fit and read on all of `selection_val`** — the read becomes in-sample for T and flatters the machinery it exists to check. (c) **Machinery-only on synthetic fixtures** — `imp.md`'s own P2-13 gate asks for a reliability read on real Stage-1 per-class posteriors, and the measured local cost is ≈46 s of laptop GPU, so withholding it would be a non-delivery without a cost justification.

**Not amended.** D11's gated object, threshold (**ECE ≤ 0.05**), estimator (15 equal-mass debiased bins) and **P3-exit** grading point; D11's stack order; D13/A2's distinct small-N-robust OOD estimator and `OOD_ECE_MIN_N = 20`; D3/A3's frozen reconciliation operator (**consumed**, not modified); D6/GATE-4's definition and its P2-14 grading on the uncalibrated posterior; ADR-0004's fold vocabulary and the committed split-table schema (**untouched** — A11 adds no column).

**§10.1 evidence gate.** The one methodological claim carrying a citation is that post-hoc **single-parameter temperature scaling** is the appropriate multi-class recalibration form here — arXiv:1706.04599 (Guo et al. 2017), already cited by D11 and by `power.CITATIONS["calibration_modern"]`, which introduces it and reports vector/matrix scaling as weaker on the same benchmarks; the binned-estimator lineage is PMID:25927013 (`power.CITATIONS["calibration_binning"]`). The convexity of `NLL(β)` and the strict monotonicity of `logits/T` are mathematical facts derived in Pin 3/Pin 4, not empirical claims, and the arg-max behaviour **through** the averaging operator is left as a measurement rather than asserted.

**Cross-reference impact.** **P2-13** (this step — `src/tbox_finder/calib/temperature.py`, `metrics.reliability_bins`, `reports/p2/calibration.json`, `figures/calib/`, `tests/unit/test_temperature.py`); **`imp.md` P3-02** — must **re-fit** T on the `calib` carve; A11's T is a machinery demonstration and is **not** inherited; **P3 exit / GATE-2** — grades D11 unchanged, on the P3-02 split, with this estimator; **P2-14 / GATE-4** — graded on the uncalibrated posterior, with `n_positions_argmax_changed` making any future decision to grade the calibrated one an explicit choice rather than a drift; **P2-12** — its flagged Antiterminator IoU fragility is addressed here **as a measurement**, and is not closed by single-T invariance (Pin 4); **ADR-0001 D1** — its *"P2-trained, graded GATE-4/GATE-2"* wording (`ADR-0001:35`) reads as if a calibration gate lands at P2, while D11 (`ADR-0005:105/:109`) and `PRD.md:56/:358` put the ECE gate at **P3 exit**; A11 follows the PRD/D11 reading and flags the ADR-0001 phrasing for reconciliation at the P2 exit gate alongside the duplicate-`A3` cleanup; **P2 exit gate** — `README.md` + `paper/manuscript.qmd` owe this step a line, batched there per §6.4.

## Amendment A12 — The P3 hard-negative re-mining round is satisfied by a round that **decides**; the shipped Stage-1 checkpoint remains the **P2-trained** production scanner, and the "P3 supersession" it was measured against was an `imp.md` construction the PRD never made (P3-15′-k reconciliation, 2026-08-13)

- **Status:** **Accepted (user sign-off 2026-08-13; CLAUDE.md §7 item 2, bioedca), "Ratify — sign ADR-0005 A12".** Amends **D14** (what a *mining round* is required to deliver) and **discharges the A10 Phase-2 §7 deferral** ("the hard-negative-mining round did NOT run … deferred, not cancelled", ADR-0005:185, :580). Pins **no** numeric value; moves **no** gate; changes **no** sparing semantics. Consumed by **P3-16**, **P4 GATE-1**, and the **P5 scan**, each of which now has a named Stage-1 checkpoint.

**Context — what actually happened.** ADR-0005 D14 phase-conditions the fourth spare-rule disjunct on "the **P3 re-mining round** once Stage-2 exists" (ADR-0005:131; PRD §9.1 in the same words). At the Phase-2 exit that round was a structural no-op: 1 of 3 evidence backends existed, sparing is a disjunction so mining is a conjunction, and yield was **0** for every candidate whatever Stage-2 scored — recorded as a §7 deferral in A10 Phase 2 (:580) and disclosed rather than ticked in `docs/model_card.md` §8. The P3-15′ arc built the missing backends (a) covariation supply, (b) relaxed architecture, (c) downstream-aaRS synteny, (d) the per-candidate Stage-2 posterior producer, plus the Tier-2N probe **id** writer (P3-15′-i). **P3-15′-k then ran the round** (2026-08-13, LOCAL, 0 GPU-h) and published `reports/p3/remine_round_report.json` (sha256 `c8aa02cf…`, `leg: "apply-spare-rule"`, `problems: []`):

> **941 candidates → 3 mined · 0 masked · 938 spared · 0 refused**, at `stage2_threshold = 0.9`.

**⚠ The artifact this amendment ratifies is now `e733744d…`, not the `c8aa02cf…` cited above.** The digest above is correct for what P3-15′-k published; this amendment corrects that report's citation fields and regenerates it, so a reader of a fresh clone computes `e733744dc83b2a854dafe915146854752271e54c67e0556bb9555a328345abd6` and should find it named here (ADRs are the cross-machine record — `PRD.md`, `imp.md` and `TODO.md` are gitignored and absent from any clone). Schema **1.1**. No measured value differs between the two.

On the recorded §7 call of 2026-08-12 ("DECISION-ONLY ROUND, NO RETRAIN") the retrain leg was not run and `P3-15′-j` (the mined-window pool builder) is deferred, so `retrain.checkpoint_superseded` reads **false**.

**Trigger.** The published round report states its own unmet obligation as *"PRD §18.1 states that `stage1_production.ckpt` supersedes the P2 checkpoint … Reconciling PRD §18.1 with ADR-0005 is owed and needs user sign-off."* Re-derived from the primary text, **that attribution is wrong**, and correcting it is what makes the reconciliation tractable rather than a live conflict between two governing documents.

---

**Pin 1 — A mining round's D14 obligation is to *decide* every candidate; producing a retrained checkpoint is a P2-located obligation the PRD does not repeat at P3.**
D14 pins three things: the union-prior mask, the spare rule with its named disjuncts, and a Tier-2N probe evaluated each mining round with a recall drop halting/rolling back the iteration. It requires no retrain, names no checkpoint artifact, and asserts no supersession — and neither does any other ADR-0005 decision or amendment, nor ADR-0001/0002/0003/0004/0006.

**Both readings of the source text are recorded here, because the run-only reading is a decision, not a discovery.**
- **Run-only (adopted).** PRD §18.1 puts the **§9.1 hard-negative-mining loop in P2** — the P2 row reads *"**P2 — Stage-1 training** | Train/calibrate the genomic segmenter … **§9.1 hard-negative-mining loop + Tier-2N probe**"* — while the P3 row asks only that the *"**P3 hard-negative re-mining round** (§9.1)"* occur, and P3's exit gate carries **no Stage-1 segmentation metric** (it is two-stage precision + in-distribution ECE). PRD **§2.1** states *"The shipped **production scanner** is **one checkpoint**"* (`PRD.md:33`) and §10.1 that *"Two Stage-1 checkpoints are trained, **both in P2**"* — so a P3-retrained Stage-1 checkpoint would be a third artifact the PRD never enumerates and never assigns a gate.
- **Pro-retrain (rejected, on the §7 call of 2026-08-12 and the yield).** PRD §9.1 says the spare rule exists so mining *"cannot directionally **train the production scanner** to reject the flagship Tier-2N class"* — mining is *what trains the scanner* — and D14's *"per-round recall drop … **halts/rolls back** the iteration"* presupposes a model state that changes between rounds. On this reading a round that never retrains is a round whose probe halt has nothing to halt. **That reading is not refuted by text; it is declined on the measurement:** the round's yield is **3 windows** against A7's 19,409-item negative pool, i.e. a training-set perturbation too small to move any graded quantity, and the retrain would carry a whole-node DDP job plus a GATE-4 re-grade and a Tier-2N re-gate for it.

A round that evaluates every disjunct for every candidate, publishes the per-candidate decision, and declines the retrain on a recorded §7 call **satisfies D14 under the adopted reading**. The P3-15′-k round did that: `problems: []`, every disjunct backed, the outcome reproducible from the recorded supplies by the publisher's own replay check.

**Pin 2 — The canonical shipped Stage-1 checkpoint is the P2-trained production scanner, unchanged by the P3 round.**
Named concretely, so no future step has to infer it: **`data/processed/checkpoints/stage1_production/stage1.pt`**, sha256 `09931a22…b380940`, DVC-tracked at `data/processed/checkpoints/stage1_production.dvc` (`md5 91bd49dd…dir`), produced by **P2-10d′-b** (`slurm/p2/train_production.sbatch :: train_stage1`, git SHA `5e29197e`, seed 42) — the artifact `docs/model_card.md` already lists as *the shipped scanner*. A file literally named `checkpoints/stage1_production.ckpt` **does not exist, is produced by nothing in the repo, and is not owed**; the `.ckpt` path is an imp.md invention (`imp.md:166`) that no PRD or ADR text uses. **P3-16, P4 GATE-1 and the P5 scan consume the P2-trained production scanner**, and every recall@matched-precision / precision-at-matched-recall / FDR / ECE figure downstream is a figure on that checkpoint. Three consequences are pinned with it:

- **(i) GATE-4's P2 grade stands and is not re-graded.** CLAUDE.md §8.5's re-grade trigger is "after any training-config change"; there was none. No re-grade is claimed and none is owed. imp.md P3-15's "re-mined production scanner **re-holds GATE-4**" gate is **not applicable**, not skipped.
- **(ii) D14's per-round Tier-2N halt/rollback is *vacuously* satisfied, and is recorded as vacuous.** No checkpoint changed, so no probe recall could drop; the round measured no probe recall and none is reported. Separately, the round's probe **exclusion** is `n_excluded = 0` for a **structural namespace** reason (the 45 probe ids are `tier2n:*`; the 941 substrate ids are `GCA_…`/`GCF_…:c<contig>:…` across 76 accessions (63 `GCA_` + 13 `GCF_`, re-counted from the artifact) ⇒ no shared namespace token), recorded in the report as `reading: "structural_namespace_disjoint"` and re-derived by its checker — a different zero from "the probe set was empty" and from "the exclusion ran and removed nothing".
- **(iii) The 3 mined windows enter no training pool.** P3-15′-j stays deferred, not scheduled; it becomes live only if a retrain is separately commissioned.

**Pin 3 — The "supersedes the P2 checkpoint" claim is retired at its source: it is an `imp.md` construction, and the PRD does not carry it.**
Re-derived from the primary text (not from any restatement):

- **PRD.md contains none of the substrings "supersede", "stage1_production", "ckpt", "re-mined", or "P2 checkpoint", anywhere** (case-insensitive, whole file) — the roadmap claim has no PRD sentence behind it.
- **PRD §18.1's P3 row** lists the goal as *"RiNALMo re-ranker + aux heads; end-to-end two-stage; calibration; **P3 hard-negative re-mining round** (§9.1)"* and the exit gate as *"Two-stage beats Stage-1-only precision; **in-distribution ECE ≤ 0.05** gated on the pre-prior-shift posterior (§12)"*. Neither half mentions a checkpoint.
- **PRD §10.1's checkpoint → phase → gate → release map** opens *"Two Stage-1 checkpoints are trained, **both in P2**"* and already assigns the P2-trained production scanner every downstream role: *"graded by **GATE-4** (P2), **GATE-2** (P3 ECE / P5 FDR), **GATE-1 arms a–c** (P4), and used for the **P5 scan / GATE-3**" — "Shipped? **yes**"*. The PRD's own map is therefore **already consistent** with Pin 2; it is imp.md that departed from it.
- **PRD §9.1** requires the mining curriculum and phase-conditions the Stage-2 disjunct on the P3 round; it names no checkpoint and states no supersession.
- The "final; **supersedes the P2 checkpoint**" wording lives **only in `imp.md`** — `:166` (the checkpoint map row), `:170-173` (the "reconciler sequencing fix #1" note), `:1584`, `:1897`, `:1909`, `:1968`, `:2166` — and `:172` / `:1968` attribute it to *"PRD §10.1"*, which says the opposite.

**Why the vacuum existed.** PRD §18.3 **delegates** the *"anti-mimicry checkpoints + checkpoint → phase → gate → release map"* to **ADR-0006**, and ADR-0006 never wrote it — the string "checkpoint" does not occur in that ADR at all. With the delegated map unwritten, `imp.md`'s own map (`:166`) became the de-facto authority and drifted from PRD §10.1's. Booking that gap is a separate decision (see the cross-reference list); this amendment does not write the delegated map.

**Therefore PRD §18.1 is not unmet: it never carried the claim.** What was unmet is imp.md's own derived roadmap claim, and this amendment retires it. `imp.md` is corrected in the same commit (local-only, CLAUDE.md §1); the published report's `retrain.prd_18_1_note` / `prd_18_1_supersession_status` fields carry the same misattribution and are corrected at their producer.

**Pin 4 — What this decision leaves owed, disclosed rather than closed.**

- **(a) The shipped scanner never saw the P3 round's hard negatives.** Three windows against A7's 19,409-item negative pool is inert in expectation, but the permitted claim is *"the P3 round evaluated 941 candidates, found 3 minable, and the retrain was declined"* — never *"the shipped scanner is hard-negative-mined at P3"*.
- **(b) 79 of 941 (8.4 %) are spared by `high_stage2_posterior` and nothing else** — the project's own model is their only protection (85 / 79 / 61 at threshold 0.5 / 0.9 / 0.99). D14 calls the other three disjuncts *model-independent* for exactly this reason; any statement about model-independent protection of the negative pool must be able to subtract this 79.
- **(c) The 938 split is a fact at an unpinned operating point.** The **mined set is invariant** across `stage2_threshold` 0.5/0.9/0.99 (the same three ids; posteriors 5.2 × 10⁻⁶ … 2.5 × 10⁻⁴), so the unpinned §13.1 value does not decide the yield — but the **sparing composition is not** invariant (724/214 · 718/220 · 700/238). `stage2_threshold_pinned` stays **false**; **this amendment pins no threshold** (§10.3), and every published split carries its operating point.
- **(d) 220 of the 938 are spared only by an unavailable backend.** Fail-closed sparing is **unchanged**. Re-reading `unavailable` as non-protective would raise the ceiling by ≈ 220 candidates and is a D14 semantics change this amendment explicitly does **not** make.
- **(e) Whether the 941 are false positives at all is open.** **639 of them (67.9 % of the manifest)** satisfy at least one **orthogonal** T-box criterion — `downstream_aaRS_synteny` 546 · `relaxed_architecture` 226 · `any_helix_rscape` 167, counted with overlap — which sharpens, and does not settle, the P3-15′-g-iv threat. ⚠ **Not 718 / 76.3 %:** that is the count spared by *any* of the **four** disjuncts, and it includes the 79 of Pin 4(b) whose only support is the project's own Stage-2 posterior. Pin 4(b) requires that 79 to be subtractable, so this pin subtracts it — 718 − 79 = 639. The `76.3 %`-as-*orthogonal* form was carried by the P3-15′-k dev-log stanzas and by `paper/manuscript.qmd`'s §sec-remine **title**; both are corrected in this commit (the manuscript's body sentence, which says *"spared because a criterion actually passed"*, was already right for 718). The manuscript reports the round as a measurement of the manifest, not as a curriculum step.

---

**Not amended.** D14's spare rule and its three model-independent disjuncts; D14's union-prior mask; the Tier-2N probe and its per-round halt/rollback (retained in full — Pin 2(ii) records only that it is vacuous on a round with no retrain); `MIN_REAL_HOMOLOG_N = 20`; A1, A2, A3, A4–A11; **A9's round count N and its ≈135 GPU-h mining budget** (unspent — no round was retrained); **A10's measured ≈4.42 core-h/round producer envelope** (its Phase-2 §7 *deferral* is discharged; its *pin* is untouched); ADR-0005 D3's phase-gate freeze of the production Stage-1 threshold; ADR-0006 D7/D11/A1/A2/A3 and the `TOTAL_ACROSS_HELICES` operator; ADR-0004's split / no-leakage rules; **GATE-4's P2 grade and the checkpoint it was graded on**.

**Cross-reference impact.** **PRD §9.1 / §18.1 / §10.1** — no PRD text changes; §18.1's P3 goal *"P3 hard-negative re-mining round (§9.1)"* is **delivered** by a round that decided, and §10.1's map is **confirmed** (both Stage-1 checkpoints trained at P2, the production scanner shipped). **`imp.md`** — `:166`, `:170-173`, `:1584`, `:1897`, `:1909`, `:1968`, `:2166` corrected; P3-15's `Outputs` and `Validation gate` re-scoped to the decision the round took (local-only file, no PR). **`reports/p3/remine_round_report.json`** — `retrain.prd_18_1_note` / `prd_18_1_supersession_status` are corrected at their producer (`src/tbox_finder/mining/remine_publish.py`) and the artifact's committed digest pin is updated in the same commit; **no measured number moves**. **`analyses/phase3_log.qmd`** (the P3-15′-k stanza), **`slurm/p3/stage1_remine.sbatch:57,:195-196`**, and — the carrier this list first omitted — **`tests/unit/test_remine_publish.py`**, which on `main` *enforced* the misattribution twice (`assert … prd_18_1_supersession_status == "UNMET"` at `:374` and `:683`). That is the complete committed carrier set; all are corrected here. ⚠ The test tier mattering most: it is where a wrong citation stops being prose and becomes a green assertion. **`docs/model_card.md` §8** — its Phase-2 statement ("not executed as a training round … 1 of 3 backends") remains historically accurate **as a Phase-2 disclosure** and is superseded at the **P3 phase-exit** by a Phase-3 section recording that the round ran, decided, and declined the retrain. **`README.md` + `paper/manuscript.qmd`** — the manuscript's `{#sec-remine}` paragraph already states *"no re-training was performed and the Stage-1 checkpoint is unchanged"*, and `docs/model_card.md` Limitation 8 already states the shipped checkpoint carries no mined hard negatives — **no outward-facing artifact asserts the supersession as achieved**, so this amendment invalidates none of them; README owes the P3 headline line at the phase-exit gate (§6.2). **P3-16 / P4 GATE-1 / P5 scan** — each consumes the **P2-trained** production scanner and must cite this amendment as the provenance of that choice. **P3-15′-j** — remains deferred; reinstating the retrain reopens Pins 1–3. **ADR-0006** — the PRD §18.3-delegated *checkpoint → phase → gate → release map* is **absent** from it; that gap is **booked, not closed here** (writing it is an ADR-0006 amendment needing its own §7 sign-off).

**Scope guard (§10.3).** This amendment pins no numeric value, moves no gate, changes no detection threshold / verdict operator / power floor / sparing semantics, re-grades nothing, authorizes no submit, flips no flag, and adds no training data. It records what a round is required to deliver, which checkpoint is canonical, and where a mis-attributed roadmap claim came from.

**§10.1 evidence gate:** N/A — a roadmap/governance reconciliation over in-repo document text plus a §9.3-style artifact-verified round outcome; it asserts no biological or statistical fact. (The biological reading of the 941-candidate manifest is P3-15′-g-iv's open threat, explicitly not settled here.)

## Sign-off
☑ recorded 2026-08-13 (bioedca), CLAUDE.md §7 item 2 — the P3 hard-negative re-mining round is satisfied by a round that DECIDES; the P2-trained production scanner stays the canonical shipped Stage-1 checkpoint; the "supersedes the P2 checkpoint" claim is retired at its `imp.md` source; no value pinned, no gate moved, no sparing semantics changed. Presented against reinstating the retrain (3 mined windows) and against revisiting D14's fail-closed `unavailable` semantics (ceiling ≈ +220).
