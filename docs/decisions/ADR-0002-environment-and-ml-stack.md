# ADR-0002 — Environment & ML stack

- **Status:** Accepted (user sign-off recorded 2026-07-08, CLAUDE.md §7 item 2)
- **Date:** 2026-07-08
- **Deciders:** bioedca (project owner)
- **Phase:** P0 (seed ADR)
- **Delegated from:** PRD §3.1 (CLAUDE.md), §10.1–10.3, §15, §18.3
- **Supersedes / superseded by:** none
- **Related:** ADR-0001 (architecture & aims — pins the D5 swapped-backbone integrity clause and the D6 branch-B3 backbone-fallback route this ADR supplies the *values* for); ADR-0003 (cluster/scan ops, compute ceilings); ADR-0004 (GATE-4 default); ADR-0005 (GATE-1/GATE-2 machinery + the RNA-FM-swap ECE/recall margins for conditions (c)/(d)); ADR-0006 (validation rollup)

This is the **environment & ML-stack ADR.** It pins the **per-env conda-lock policy + CI image**, the **load-bearing ML/data/infernal version stack** (with a named version + rationale for every load-bearing pin), the **CUDA/sm_86 kernel pins + pure-PyTorch fallbacks**, the **RiNALMo commit + parity numerics** (the numeric parity margin **N**, its magnitude rationale, and the fallback phasing), the **RNA-FM-swap re-clearing conditions** (the swapped backbone re-clears the *absolute* GATE-1/GATE-2 gates and re-runs the P4→P5 go/no-go), and the **Caduceus prokaryotic-transfer two-part go/no-go + backbone fallbacks**. Every version fact herein was verified against authoritative sources (PyPI JSON, `download.pytorch.org`, GitHub release asset lists, the Caduceus HF config/modeling files, the RiNALMo paper) on **2026-07-08** — the assistant knowledge cutoff is Jan 2026, so *no version was taken from memory* (CLAUDE.md §3.3). The **empirical sm_86 wheel/build-availability confirmation** is deferred to **P0-06**, which amends this ADR with the observed result (PRD §10.3).

---

## Context

The stack is **load-bearing and never-silently-bumped** (CLAUDE.md §3.1): the reproducibility of every training/eval/scan artifact — and therefore the defensibility of the GATE-1 generalization claim — depends on an exactly-pinned, mutually-compatible environment that also *runs on the cluster's actual hardware*. The runtime target is fixed and non-negotiable (PRD §15):

- **Hardware:** NVIDIA RTX A4000 = **Ampere, compute capability `sm_86`, 16 GB VRAM**; CUDA 12.x toolkit; NVIDIA driver **590.x** (CUDA-13-capable, hence forward-compatible with any 12.x runtime).
- **No container runtime on the cluster** (no apptainer/singularity/docker/podman) → environments are **conda-lock** on the existing user miniconda3, one lockfile per env (CLAUDE.md §3.1). The CI **Docker image is the archival/portability artifact, not the cluster runtime.**
- **Two model backbones drive the stack**, and each carries a *hypothesis-to-validate* rather than an assumption: **Stage-1 Caduceus** (BiMamba/MambaDNA, RC-equivariant, single-nucleotide DNA; `trust_remote_code=True` from `kuleshov-group/caduceus-*`; **human-reference pretrained** → prokaryotic-transfer go/no-go) [arXiv:2403.03234; PMID:40567809], and **Stage-2 RiNALMo-giga** (650M RNA LM, FlashAttention-2; accessed via the transformers-native `multimolecule` mirror; **no published T-box result** → parity + transfer gates) [PMID:40593636; DOI:10.1038/s41467-025-60872-5; PMC12219582].

Two facts about the current package ecosystem (2026-07-08) shape every pin below and are the reason "pin the newest of everything" is **wrong** here:

1. **A `trust_remote_code` ceiling on `transformers`.** Caduceus is loaded as Hub remote code; `transformers` 5.0 removed `is_torch_fx_available` (PR #37234), which broke a broad class of remote-code modeling files (issue #44561). The Caduceus + `mamba_ssm` remote path is unvalidated on v5. **`transformers` is capped at the last 4.x release, 4.57.5**, which cascades the whole HF stack down to the Jan-2026 4.x epoch (`huggingface_hub` < 1.0, `tokenizers` ≤ 0.23.0, era-matched `accelerate`/`peft`/`deepspeed`/`datasets`).
2. **An `sm_86`-wheel ceiling on `torch`.** The three CUDA-kernel packages the backbones need — `mamba-ssm`, `causal-conv1d` (Caduceus), `flash-attn` (RiNALMo) — do **not** all publish prebuilt Ampere `sm_86`/CUDA-12 wheels at the same `torch` version, and the newest `mamba-ssm` (2.3.x = Mamba-3) makes a Hopper/Blackwell-oriented kernel stack a *hard* runtime dependency of unverified Ampere-import-safety. The `torch` anchor is therefore set by the **kernel ecosystem**, not by `torch` itself (D3).

The **`pytorch-cuda` conda metapackage is dead** — PyTorch stopped publishing its Anaconda `pytorch` channel after 2.5 (issue #138506). PRD §15's "GPU env pins `pytorch-cuda` matching driver 590 / CUDA 12.x" describes *intent* (a CUDA-12.x GPU PyTorch matching the driver); the **mechanism** is now a pinned `pip:` block against the CUDA-12.x wheel index inside the conda-lock env (D1/D3). This is a mechanism update within PRD §15's stated intent, not a scope change, and is flagged here per the CLAUDE.md precedence rule.

---

## Decision

### D1. Per-env conda-lock policy (locked)

- **Five environments, one lockfile each** (CLAUDE.md §3.1): `envs/{data,infernal,ml,viz,app}.yml`, each with its own `envs/<name>.conda-lock.yml` solved `-p linux-64` — **never** a single aggregate lock. Built by P0-05.
- **Python 3.12** across all five envs. This is **forced, not stylistic**: `numpy` 2.5.1 and `scipy` 1.18.0 both raise `requires-python >= 3.12`, so 3.11 would strand the data/viz stack on an older numpy/scipy *and* desync its numpy ABI from the `ml` env; the `ml` kernel wheels cover `cp312`; the infra floor (`snakemake`) is 3.11. 3.12 satisfies every pin and keeps one numpy ABI across envs. (3.13 is unnecessary bleeding edge.)
- **GPU-PyTorch mechanism (supersedes the literal `pytorch-cuda` wording of PRD §15).** The `pytorch-cuda` metapackage is deprecated; the Anaconda `pytorch` channel ended at 2.5. In the `ml` env, keep Python/BLAS/numpy/etc. as conda deps and pin `torch` **and the three CUDA-kernel packages** in the env's `pip:` block against the CUDA-12.x PyTorch wheel index (`--extra-index-url https://download.pytorch.org/whl/cu128`); `conda-lock` captures pip deps in the lock. This guarantees the kernel wheels **ABI-match the exact `torch` build** (all `manylinux_2_28` = C++11-ABI TRUE; the kernel wheels must be the `cxx11abiTRUE` variants). Pulling `torch` from conda-forge instead silently breaks this ABI match — do not.
- **Adding a dependency later** = edit the yml, regenerate that env's lock, re-run the affected golden/eval test, commit yml+lock together (CLAUDE.md §3.1). Never ad-hoc `pip install`/`conda install`; never `conda env update`/`pip install -U`.

### D2. The pinned stack (locked; every load-bearing pin named + rationalized)

Versions are the **latest mutually-compatible** stable releases as of 2026-07-08, chosen for compatibility over recency. `numpy 2.5.1` is pinned **identically** in the `data` and `ml` envs (one numpy-2 ABI shared by torch/multimolecule/pandas/scipy/sklearn/biopython — never let any dep pull a numpy-1 build).

**`ml` env (GPU; training / eval / scan) — the load-bearing ML stack.** GPU/kernel pins are D3.

| Package | Version | Source | Rationale (load-bearing) |
|---|---|---|---|
| `python` | 3.12 | conda-forge | numpy/scipy floor; kernel wheels cover cp312 |
| `torch` | **2.7.1 (+cu128)** | pip (cu128 index) | anchor set by the kernel ecosystem — see **D3** and **Considered options** |
| `mamba-ssm` | **2.2.6.post3** | pip (source-built) | Mamba-2, no Hopper deps — **D3** |
| `causal-conv1d` | **1.6.2.post1** | pip (prebuilt `cu12torch2.7`) | Caduceus depthwise-conv accelerator — **D3** |
| `flash-attn` | **2.8.3.post1** | pip (prebuilt `cu12torch2.7`) | FA2 = the Ampere line — **D3** |
| `transformers` | **4.57.5** | conda-forge | **ceiling** — last 4.x; Caduceus `trust_remote_code` unvalidated on v5 (config declares 4.38.1; v5 removed `is_torch_fx_available`, issue #44561). 4.57.0 was yanked → .5 |
| `tokenizers` | 0.22.2 | conda-forge | newest **stable** ≤ transformers' `<=0.23.0` cap (0.23.0 was skipped; 0.23.1 exceeds it) |
| `huggingface_hub` | 0.35.3 | conda-forge | last 0.x; transformers 4.57.5 pins `<1.0,>=0.34.0` (hub 1.x = the v5 line) |
| `safetensors` | 0.7.0 | conda-forge | era-matched to the 4.57 epoch (>=0.4.3 required; 0.8.0 is the v5 era) |
| `accelerate` | 1.12.0 | conda-forge | FSDP `FULL_SHARD` + DeepSpeed ZeRO-3 plugins for the §10.3 full-FT fallback; era-matched, no transformers cap |
| `peft` | 0.18.1 | conda-forge | `LoraConfig(target_modules="all-linear")` + grad-checkpointing (§10.3 LoRA default); era-matched, no transformers cap |
| `deepspeed` | 0.18.5 | conda-forge | ZeRO-3 full-FT fallback (states shard to ~1.3 GB/GPU ×8); JIT-builds ops vs local CUDA 12.x (needs in-env nvcc) |
| `datasets` | 4.5.0 | conda-forge | era-matched; coupled to HF only via `huggingface_hub` (satisfied) |
| `multimolecule` | **0.0.9** | pip | the RiNALMo/RNA-FM transformers-native mirror; **era-matched to transformers 4.57.5** (avoid 0.1.0/0.2.0 which may assume v5 internals) — **D5** |
| `wandb` | 0.28.0 | conda-forge | offline mode (`WANDB_MODE=offline`) + login-node `wandb sync`; pulls `pydantic>=2.6` |
| `hydra-core` / `omegaconf` | 1.3.2 / 2.3.0 | conda-forge | run-config driver; `omegaconf<2.4` cap (do **not** use the 1.4.0/2.4.0 dev pre-releases) |
| `numpy` | 2.5.1 | conda-forge | shared numpy-2 ABI (identical to `data` env) |
| `cuda-nvcc` + `ninja` + `packaging`/`setuptools>=61` | CUDA 12.8 line | nvidia/conda-forge | toolchain to source-build `mamba-ssm` for `sm_86` and JIT DeepSpeed ops (D3/D4) |

**`data` env (CPU DAG / ingest / labels / splits / metrics).** `python` 3.12; `numpy` 2.5.1, `scipy` 1.18.0, `pandas` 2.3.3 (**P0-05 amendment**, was 3.0.3 — see Amendments), `polars` 1.42.1, `scikit-learn` 1.9.0, `biopython` 1.87 (all conda-forge, numpy-2 native); `snakemake` 9.23.1 + `snakemake-executor-plugin-slurm` 2.7.1 (bioconda; the SLURM executor is used for **CPU DAG bookkeeping only** — GPU jobs are hand-authored sbatch, PRD §16); `dvc` 3.67.1 + `dvc-ssh` 4.3.0 (conda-forge; `dvc-ssh` 4.x is **sshfs/asyncssh**-based, not paramiko — install via the `dvc[ssh]` extra so the two stay `>=4,<5` in lockstep); `hydra-core` 1.3.2 / `omegaconf` 2.3.0.

**`infernal` env (baseline + folding).** `infernal` **1.1.5** (bioconda) — the `cmsearch`/`cmbuild`/`cmcalibrate` suite that is the **Stage-0 baseline the GATE-1 recall claim is measured against**; its version string is load-bearing for the recall-vs-`cmsearch` metric — do not float it. `viennarna` 2.7.2 (**bioconda only** — not on conda-forge; SWIG Python bindings). `biopython` 1.87, `numpy` 2.5.1.

**`viz` / `app` envs (non-load-bearing toolchains).** The plotting/Quarto (`viz`) and Svelte-atlas build/serve (`app`) toolchains are **not** part of the load-bearing ML-reproducibility surface; their exact pins are settled at **P0-05** build time. Where they touch numerical data they share the **`numpy` 2.5.1** pin for ABI consistency.

**Pin-coupling notes (watch when bumping):** `snakemake` 9.23.1 ↔ `snakemake-executor-plugin-slurm` 2.7.1 both target the `snakemake-interface-executor-plugins` 9.x line and co-resolve — re-check they still land on one 9.x interface release if either is bumped. **bioconda over-cap (found at P0-05):** the *bioconda* build of `snakemake-executor-plugin-slurm` 2.7.1 declares `pandas >=2.2.3,<3.0` — a stale recipe cap that contradicts the package's own upstream PyPI metadata (`pandas <4,>=2.3.3`) and forces the `data` env off pandas 3.x (see Amendments). Watch for a corrected bioconda build (or a conda-forge listing) as the trigger to revisit the pandas pin. `hydra-core` needs `omegaconf<2.4` (also pulled transitively unpinned by `dvc`) — keep `omegaconf<2.4` on any bump. Channel skew: PyPI is ahead of conda-forge for `hydra-core` (1.3.4) / `omegaconf` (2.3.1); for a pure-conda lock use the conda-forge versions above.

### D3. Torch/CUDA anchor + `sm_86` kernel pins & pure-PyTorch fallbacks (locked; empirical check → P0-06)

**Anchor: `torch` 2.7.1, CUDA-12.x build (`cu128`), C++11-ABI TRUE (`manylinux_2_28`).** CUDA 12.8 is the safe 12.x choice on driver 590.x (Ampere `sm_86` is in `torch`'s default `TORCH_CUDA_ARCH_LIST` for every 12.x build; driver 590 is forward-compatible to CUDA 13). torchvision/torchaudio are **not required** (both backbones are token-sequence models); add only if a later step demonstrably imports them, at the co-released version from the same index.

**Kernel pins (all `sm_86`-capable on CUDA 12.x):**
- **`flash-attn` 2.8.3.post1** — prebuilt Ampere wheel exists: `flash_attn-2.8.3.post1+cu12torch2.7cxx11abiTRUE-cp312-linux_x86_64.whl` (Dao-AILab GitHub release; `sm_86` covered by the always-compiled `compute_80` gencode). **FA2 is the correct line for Ampere** — `flash-attn` 3.x (SM90/Hopper-only) and the `fa4` betas (Blackwell/Hopper) do **not** target `sm_86` and merely auto-fall-back to FA2 there.
- **`causal-conv1d` 1.6.2.post1** — prebuilt `cu12torch{2.6..2.10}` Ampere wheels; **optional** accelerator (Mamba falls back to `nn.Conv1d` without it).
- **`mamba-ssm` 2.2.6.post3** — the **last pre-Mamba-3 release**, keeping the classic CUDA `selective_scan` kernel (gencode `compute_80`/`sm_80` + `sm_87` = native Ampere) with minimal deps. Its prebuilt `cu12` wheels cap at `torch2.5`, so at the `torch` 2.7 anchor it is **source-built** (nvcc 12.x via in-env `cuda-nvcc`, `ninja`, `--no-build-isolation`; compiles `sm_86` cleanly). This one source build is captured/verified at P0-06 and baked into the CI image (D4) for reproducibility. **Mamba-3 (`mamba-ssm` 2.3.x) is rejected** — it makes `quack-kernels` (nvidia-cutlass-dsl CuTe-DSL), `tilelang`, and `apache-tvm-ffi` **hard** runtime deps of unverified Ampere-import-safety, a load-bearing liability for **zero Caduceus benefit** (Caduceus is Mamba-2).

**Pure-PyTorch fallbacks (confirmed in source; P0-06 records the throughput cost, PRD §10.3):**
- **Mamba selective-scan:** `Mamba(use_fast_path=False)` → `selective_scan_ref`/`mamba_inner_ref` in `mamba_ssm/ops/selective_scan_interface.py`, skipping the fused kernel; `causal-conv1d` degrades to `nn.Conv1d`. So a missing kernel is a **speed cost, not a capability loss**. The measured genome-scan throughput cost of the fallback is recorded in this ADR at P0-06.
- **FlashAttention-2:** `torch.nn.functional.scaled_dot_product_attention` (SDPA)/eager. **Important:** the *reference* `lbcb-sci/RiNALMo` repo has **no** SDPA fallback (it calls flash-attn unconditionally), but the **`multimolecule` HF port we use provides the SDPA/eager path** — so accessing RiNALMo through the `multimolecule/rinalmo-giga` mirror (D5) makes flash-attn an **accelerator, not a hard requirement**. This is an additional reason to prefer the mirror.

**Empirical gates:** the **`sm_86` wheel/build-availability check is P0-06** (folded back into this ADR as an amendment with the observed wheel tags + the source-build result); the **kernel import/forward smoke gate is P1** (PRD §10.3). If P0-06 finds the `mamba-ssm` source build unreliable on the cluster, the **pre-registered fallback is Considered-option A** below (torch 2.5, all-prebuilt wheels) — an ADR-0002 amendment, not a silent switch.

#### Considered options for the anchor (the sign-off decision)

- **Option B — `torch` 2.7.1 + cu128 (RECOMMENDED, adopted above).** Prebuilt `sm_86` wheels for `flash-attn` + `causal-conv1d`; **one** source build (`mamba-ssm` 2.2.6.post3); clean Mamba-2 kernels with no Hopper deps; modern torch; **all three kernel packages have exact, confirmed, named versions**.
- **Option A — `torch` 2.5.1 + cu124 (pre-registered fallback).** **Zero source builds** — prebuilt `sm_86` wheels for all three (`flash-attn` 2.8.3.post1 `cu12torch2.5`, `mamba-ssm` 2.2.6.post3 `cu12torch2.5`, `causal-conv1d` **1.5.x** `cu12torch2.5`). Marginally more reproducible / no cluster-nvcc dependency, but `torch` is ~20 months old and the `causal-conv1d` 1.5.x exact patch must be reconfirmed at env-lock. **Adopted only if** P0-06 shows the Option-B `mamba-ssm` source build is unreliable on the cluster.
- **Option C — `torch` 2.10 + `mamba-ssm` 2.3.x (REJECTED).** All-prebuilt at the newest torch, but drags the Hopper/Blackwell `quack-kernels`/`tilelang`/`apache-tvm-ffi` hard-dep stack of unverified Ampere-import-safety into a load-bearing `sm_86` env — unacceptable risk for zero benefit.

### D4. CI Docker image (locked)

`docker/Dockerfile` builds a **CUDA-12.8 + PyTorch** image in CI (GHCR), from a `nvidia/cuda:12.8.*-cudnn-devel-ubuntu` base (the `-devel` base supplies nvcc for the `mamba-ssm` source build), installing the `ml` env from its committed `conda-lock.yml`. It is the **archival/portability/reviewer artifact and the path to a future apptainer/udocker run — not the cluster runtime** (no container runtime on the cluster, D1). Built on schedule/release (CLAUDE.md §8.6), not per-push.

### D5. RiNALMo commit + parity numerics (locked; the delegated margin **N** is authoritative here, D4/§2.3 carve-out)

- **Commit to RiNALMo, parity-gated (PRD §10.2).** Stage-2 is **RiNALMo-giga (650M)**, accessed via the transformers-native mirror **`multimolecule/rinalmo-giga`** (`multimolecule` **0.0.9**); the official `lbcb-sci/RiNALMo` weights (Zenodo 15043668) are the fallback. RiNALMo ingests **sequence only** (no structure-input channel). A **P1 parity step** confirms the mirror reproduces RiNALMo's published **ArchiveII nine-family leave-one-family-out (inter-family) secondary-structure generalization** on the same LOFO splits **before** committing.
- **Parity criterion + the numeric margin N.** Primary criterion (PRD §10.2) = "mirror inter-family F1 within the **published SD** on the same LOFO splits." **The RiNALMo paper reports no per-split SD** (only per-family mean F1 — the exact values live in **Supplementary Table S2, `non-weighted` column**, of which main-text Fig. 4a is the bar-chart rendering; Fig. 4b is per-structure box-plots), so the primary criterion is **not directly usable** and the pinned **±N pp fallback governs**:
  - **N = 2 pp (±0.02 F1)**, applied to the **mean inter-family F1 (0.72)** and to **each of the eight stable held-out families** (5S 0.88, SRP 0.70, tRNA 0.93, tmRNA 0.80, RNase P 0.80, Group I intron 0.66, 16S 0.74, 23S 0.85).
  - **Telomerase carve-out:** the **telomerase-RNA** family (published F1 **0.12**, near the failure floor, high-variance — the acknowledged outlier) is **excluded from the strict parity gate** (or given a widened ±5–10 pp band). A same-weights mirror can swing several pp there on benign seed noise; gating parity on telomerase alone would manufacture false failures. Parity is decided on the **mean + the eight stable families at ±2 pp**.
  - **Magnitude rationale (documented per D4/§2.3, blinded-frozen at P0).** The published values are printed at 0.01 (1 pp) resolution → 1 pp is the irreducible reporting granularity; the mirror loads the **same** weights, so the only expected divergence is fine-tuning-run stochasticity (head init, shuffle seed) + reimplementation numerical drift (tokenizer mapping, attention/precision path) — ~1 pp on a family-*mean* F1 (which averages over many structures and is far tighter than the wide per-structure box-plot IQRs). 2 pp cleanly covers 1 pp rounding + ~1 pp noise **without admitting a meaningful accuracy gap**. The huge 0.12–0.93 across-family spread is **biological family-difficulty variation, not mirror noise**, and must not set N. A >2 pp shift on the mean or on multiple stable families is treated as a **real reimplementation discrepancy to investigate**, not accepted. Source: RiNALMo, `PMID:40593636; DOI:10.1038/s41467-025-60872-5; PMC12219582` (**Supp. Table S2, `non-weighted` column** — the exact values; main-text Fig. 4a renders them, Fig. 4b is per-structure box-plots), accessed 2026-07-08.
- **The `transformers`-4.57.5 ceiling ↔ `multimolecule` tension (flagged for sign-off).** Caduceus caps `transformers` at 4.57.5 (D2); `multimolecule` pins **no** `transformers` version, so 0.0.9 is era-matched to that epoch and the **P1 parity gate runs under exactly `transformers` 4.57.5 + `multimolecule` 0.0.9**. **Contingency:** if a *required* future `multimolecule` hard-requires `transformers ≥ 5` while Caduceus still needs ≤ 4.57.5, Stage-1 (DNA) and Stage-2 (RNA) can no longer share one `transformers` in a single `ml` env → the pre-registered resolution is an **env split** (`envs/ml-dna.yml` + `envs/ml-rna.yml`), an ADR-0002 amendment under sign-off — **not** a silent pick.
- **P1 forward-throughput probe = advisory-only (PRD §10.2).** A RiNALMo forward-throughput probe (candidates/sec/GPU on the pretrained checkpoint) runs alongside the parity gate to surface the latency risk early, but the **binding** latency decision is frozen at the **P5 sizing gate** (condition (b) below); a crude pre-P3 "clearly-hopeless" reading is a **CLAUDE.md §7 stop-and-ask, not an auto-switch**.

### D6. RiNALMo → RNA-FM fallback phasing + swapped-backbone re-clearing (locked; (c)/(d) margins delegated to ADR-0005)

Stage-2 falls back to **RNA-FM** (`multimolecule/rnafm`, ~100M; drop-in via the same `multimolecule` interface, so sunk cost is bounded) **only** on a phase-assigned trigger (PRD §10.2):
- **(a) [P1 parity gate]** the D5 parity gate fails;
- **(b) [VRAM at P1 / §10.3; genome-scale latency frozen at the P5 sizing gate]** RiNALMo-giga cannot meet the A4000 16 GB-VRAM + genome-scale-latency budget after PEFT/bf16;
- **(c) [P3–P4]** post-calibration leave-clade-out ECE is worse than RNA-FM's by a **pre-registered numeric margin — pinned in ADR-0005**, not here;
- **(d) [P4]** RiNALMo transfers worse on the primary discovery metric (leave-clade-out recall@matched-precision / AUPRC) by a **pre-registered margin — pinned in ADR-0005**, even if ECE matches.

**Swapped-backbone integrity (mirrors ADR-0001 D5).** **If the swap fires, the substituted RNA-FM Stage-2 must clear the *absolute* GATE-1 (+10 pp point / +5 pp CI-floor) and GATE-2 (in-distribution ECE ≤ 0.05) thresholds and re-pass the P4→P5 go/no-go on its own P3/P4 numbers before entering the GATE-3 scan** — the shipped backbone's gates are **never inherited from RiNALMo**. The covariance model is kept as an **orthogonal cross-validator, never the sole Stage-2** (PRD §10.2). (The absolute GATE-1/GATE-2 threshold *values* are authoritative in ADR-0005; this ADR pins only that the swapped backbone must re-clear them and re-run the go/no-go.)

### D7. Caduceus prokaryotic-transfer two-part go/no-go + backbone fallbacks (locked)

The public Caduceus checkpoints are **human-reference pretrained**, while the discovery scan is fully prokaryotic/archaeal/MAG — so Stage-1 cross-domain transfer is a **hypothesis to validate, symmetric with RiNALMo's parity gate** (PRD §10.1). A **two-part P1 go/no-go runs before P2 commits training compute:**
1. **Cheap frozen-embedding binary linear-separability pre-filter** — held-out bacterial/archaeal T-box vs GC-matched prokaryotic background (an advisory screen: is there above-chance linear separability in the frozen embeddings?).
2. **Binding per-nucleotide fine-tune segmentation smoke F1** on held-out prokaryotic loci, matching the GATE-4-graded task — because a binary probe can pass while the embeddings lack the positional structure for segmentation, **or** fail while full fine-tuning succeeds. **This part is binding**; the pre-filter is not.

**Go/no-go rule.** *Go* iff both are positive: the pre-filter shows above-chance separability **and** the segmentation smoke recovers the three core elements (Stem I, Specifier, Antiterminator) at per-nt F1 **clearly above a background-only baseline** on held-out prokaryotic loci — a directional "the backbone can learn prokaryotic T-box boundaries" signal, **not** the GATE-4 bar (that is graded at P2 on the full split). A **borderline** read is a **CLAUDE.md §7 stop-and-ask**, not an auto-decision (the exact small-N smoke threshold is set with the P1 fixture, since it is a go/no-go and not a blinded-frozen gate).

**Fallbacks on weak transfer, in order (PRD §10.1/§10.3):** (1) full fine-tuning (already the Stage-1 default); (2) an **optional GTDB-prokaryotic continued-pretraining pass** before T-box fine-tuning (budgeted SLURM, probe-triggered); (3) a **prokaryotic/multispecies DNA backbone** (e.g. **Nucleotide-Transformer multispecies**) as a Stage-1 ablation/fallback. A **transfer-driven** (not merely capacity-driven) **GATE-4 failure at P2 re-triggers these same continued-pretraining / NT-multispecies fallbacks** — this is the concrete backbone-fallback ladder that **ADR-0001 D6 branch B3** routes to before any terminal pivot (a capacity/transfer method-gate failure is *not* auto-routed to the GATE-1-failure deliverable).

### D8. Never-silently-bump discipline + amendment procedure (locked)

- The `torch`/kernel/`transformers`/`multimolecule`/RiNALMo-stack pins are **load-bearing** (CLAUDE.md §3.1, §13). No `conda env update`, no `pip install -U`, no silent bump. A bump requires **user approval + an ADR-0002 amendment listing which steps re-validate** (CLAUDE.md §3.1).
- **Two bellwethers to watch** (bump *triggers*, not licenses to bump silently): (i) **`flash-attn`** is the binding torch-ceiling package — a `flash-attn` release adding `torch>2.10` prebuilt Ampere wheels is the trigger to *consider* re-anchoring the whole GPU group; (ii) the **`transformers`-4.57.5 ↔ `multimolecule`** tension (D5) — a required `multimolecule` needing `transformers ≥ 5` triggers the env-split contingency.
- **Re-validation on any bump** (CLAUDE.md §8.5): re-run the kernel import/forward smoke (P1), the RiNALMo parity gate (D5), and the affected golden/eval gates before the amended lock is committed.

### D9. Delegations (values NOT pinned here)

- **ADR-0005:** the RNA-FM-swap ECE/recall trigger **margins** for conditions (c)/(d) (D6); the **absolute GATE-1/GATE-2 threshold values** the swapped backbone must re-clear.
- **ADR-0004:** the GATE-4 per-element F1 default (0.80) the P2 segmentation is graded on.
- **ADR-0003:** the per-corpus/aggregate compute ceilings + training/sweep GPU-hour budget that bound the continued-pretraining pass (D7) and the scan.
- **P0-06 (amends this ADR):** the empirical `sm_86` wheel/build-availability result + the measured pure-PyTorch-fallback throughput cost + the Option-A/B anchor confirmation.

---

## Consequences

- **Positive.** Every training/eval/scan artifact is reproducible from an exactly-pinned, mutually-compatible, `sm_86`-runnable stack; the two ecosystem ceilings (`transformers`←Caduceus, `torch`←kernels) are made explicit so no downstream step silently bumps into a broken combination; the RiNALMo parity margin, its magnitude rationale, and the RNA-FM fallback phasing are pre-registered and blinded-frozen; the swapped backbone can never inherit RiNALMo's gates; the Caduceus go/no-go and backbone-fallback ladder give ADR-0001's B3 branch concrete values.
- **Costs / constraints.** The `transformers`-4.57.5 ceiling forgoes v5 features and creates a latent env-split risk (D5) if `multimolecule` moves to v5. The `torch`-2.7 anchor requires **one source build** (`mamba-ssm`) with an in-env CUDA toolchain, with Option A (all-prebuilt, older torch) as the pre-registered fallback if the build is unreliable. The never-bump discipline trades late flexibility for reproducibility integrity.
- **Empirical items still open (by design).** The `sm_86` wheel/build availability (P0-06), the kernel import/forward smoke (P1), the RiNALMo parity result (P1), and the Caduceus transfer go/no-go (P1) are confirmed at their named gates; this ADR pins the *targets, decision rules, and fallbacks*, not their empirical outcomes.

## Cross-reference impact list

- **PRD:** §3.1 (env policy in CLAUDE.md), §10.1 (Caduceus backbone + transfer go/no-go + checkpoint map), §10.2 (RiNALMo commit/parity + RNA-FM conditions a–d), §10.3 (fine-tuning footprint + `sm_86` kernels/fallbacks), §15 (compute/env; `pytorch-cuda`-mechanism update flagged), §18.1 (P0/P1 exit gates), §18.3 (ADR index).
- **Sibling ADRs:** ADR-0001 (D5 swapped-backbone integrity + D6 branch-B3 backbone fallbacks — this ADR supplies their values); ADR-0003 (compute ceilings bounding continued-pretraining + scan); ADR-0004 (GATE-4 default); ADR-0005 ((c)/(d) RNA-FM margins + absolute GATE-1/GATE-2 values); ADR-0006 (rollup).
- **imp.md:** P0-04 (this step); P0-05 (builds the five envs from these pins); P0-06 (empirical `sm_86` check + fallbacks — amends this ADR); P0-07 (CI Docker image); P1 (kernel smoke, Caduceus go/no-go, RiNALMo parity + throughput probe).
- **CLAUDE.md:** §3.1 (conda-lock + never-bump), §3.2 (rule=env), §3.3 (context7 before pinning), §7 item 2 (this sign-off) + item 4 (borderline go/no-go stop-and-ask), §8.5 (re-validate on bump), §10.1 (parity citation carried).
- **Cards / paper (release-bound):** model card (backbone versions, LoRA/FSDP config, parity result, Caduceus transfer outcome); `docker/Dockerfile` + `envs/*.conda-lock.yml` (the reproducibility artifacts); `paper/manuscript.qmd` (methods: pinned stack + `cmsearch` 1.1.5 baseline).
- **Phase-0 exit (2026-07-12):** the pinned envs remain the reproducibility artifacts; the phase-0 dev-log renders via the **`tbox-viz`** env (`envs/viz.yml` → `name: tbox-viz`; `quarto`+`typst`, no LaTeX in the stack). No ML-stack claim enters the P0 manuscript paragraph (deferred to the P1 backbones/parity paragraph); no pin changed.

## Amendments

- **A1 — `data` env `pandas` 3.0.3 → 2.3.3 (P0-05, 2026-07-08; user sign-off recorded).** When P0-05 built the `data` env lock, the two D2 pins `pandas` 3.0.3 and `snakemake-executor-plugin-slurm` 2.7.1 proved mutually unsatisfiable **via conda**: the *bioconda* build of the plugin (`2.7.1-pyhdfd78af_0`) over-caps `pandas >=2.2.3,<3.0`, whereas the same version's **upstream PyPI** metadata allows `pandas <4,>=2.3.3` (and `snakemake` core 9.23.1 has no pandas constraint). So the conflict is a **bioconda recipe defect, not an inherent stack incompatibility**. Resolution (of the two options weighed — pure-conda downgrade vs. pip-sourcing the plugin to keep 3.0.3): pin `pandas` **2.3.3** (the max 2.x; satisfies both the bioconda cap and upstream) in the `data` env, keeping the env pure-conda and preserving the load-bearing **numpy-2.5.1 ABI**. pandas-3.0 features (CoW / PyArrow-string defaults) are **not load-bearing** here (polars 1.42.1 is the dataframe workhorse; pandas is convenience/interop). The non-load-bearing `viz` env's pandas (a P0-05-delegated pin, not fixed in this ADR) was aligned to 2.3.3 for repo-wide consistency. **Re-validation:** the amended `data` lock solves cleanly (305 pkgs, all other D2 pins intact) and installs + import-smokes locally. Bump-trigger recorded in the D2 pin-coupling notes.

- **A2 — sm_86 wheel/build-availability probe result + two D3 corrections (P0-06, 2026-07-08; probe `scripts/check_sm86_wheels.py`; user sign-off recorded).** The empirical probe (PyPI JSON + GitHub Releases REST; verified by a Range-GET download probe and a 3-agent adversarial pass) finds **all three ml-env CUDA kernels have a prebuilt Ampere-`sm_86` `cu12torch2.7` cp312 `linux_x86_64` wheel** — `flash-attn` 2.8.3.post1 (abiTRUE+FALSE), `causal-conv1d` 1.6.2.post1 (abiTRUE), **and `mamba-ssm` 2.2.6.post3 (abiTRUE+FALSE)**. `sm_86` rides the always-compiled `compute_80`+`compute_87` gencode (verified in each `setup.py`); torch 2.7.1 Linux is C++11-ABI-TRUE (`manylinux_2_28`, PyTorch's Nov-2024 wheel-platform switch), so the **abiTRUE** wheels are the correct pins. Option A (torch 2.5) fallback matrix: `flash-attn` ✓ and `mamba-ssm` ✓ have torch2.5 wheels, but `causal-conv1d` 1.6.2.post1 has **no** torch2.5 wheel (its wheels start at torch2.6) — Option A would need `causal-conv1d` 1.5.x, as D3 already noted. **Correction C1:** D3's "[`mamba-ssm`] prebuilt cu12 wheels cap at torch2.5, so at the torch 2.7 anchor it is source-built" is **superseded** — the torch2.7 wheel exists (533 MB, 8,877 downloads on the non-draft/non-prerelease `v2.2.6.post3` release). **Option B therefore requires zero source builds**, conditional on `ml.yml` pinning the exact **GitHub release-asset wheel URLs** (these wheels are absent from PyPI, which serves only the sdist; a bare version pin would fetch the sdist and invoke nvcc). Consequences, applied/recorded in this amendment: the `cuda-nvcc`/`ninja` toolchain in `ml.yml` is **no longer required for `mamba`** (retained only for DeepSpeed's JIT-built ZeRO ops); `ml.yml` now pins the three kernels by release-asset URL (updated same commit); the **P0-05 ml-lock deferral is reversed** — with URL-pinned prebuilt wheels the ml env installs with no nvcc and no GPU, so `envs/ml.conda-lock.yml` is now producible on the laptop (subject to conda-lock resolving pip-URL wheels + the cu128 torch index) and the actual lock is the next focused step; the D4 CI-image `-devel` base is needed only for DeepSpeed JIT. **Correction C2:** D3's "pure-PyTorch fallback — `Mamba(use_fast_path=False)` → `selective_scan_ref`/`mamba_inner_ref` … a missing kernel is a speed cost, not a capability loss" is **factually wrong** for the packaged `Mamba` module in `mamba-ssm` 2.2.6.post3: `mamba_ssm/modules/mamba_simple.py:13` bare-imports `selective_scan_fn`/`mamba_inner_fn` and `mamba_ssm/ops/selective_scan_interface.py:20` bare-imports `selective_scan_cuda`, so importing `Mamba` **fails** without the compiled kernel; `use_fast_path=False` calls `selective_scan_fn` (CUDA) at `mamba_simple.py:189`, never `selective_scan_ref` (present but unreached; `mamba_inner_ref` also asserts `causal_conv1d_fn is not None`). The genuine optional-degradation is `causal_conv1d` only (`mamba_simple.py:168-169` → `nn.Conv1d`, speed-only). **The true no-kernel path is: rely on the now-confirmed prebuilt wheel (primary), or hand-wire `selective_scan_ref` / a Triton backend (a code change, not a flag).** Since C1 supplies the sm_86 kernel this is low practical risk; **P1's import/forward smoke must assert `import selective_scan_cuda` succeeds.** **Confirmation K1:** the RiNALMo SDPA/eager fallback (D3) holds — `multimolecule` 0.0.9 RiNALMo is a transformers-native `PreTrainedModel` using HF pluggable attention (`eager_attention_forward` default, `sdpa` auto-selected; `_supports_sdpa`/`_flash_attn`/`_flex_attn`=True), so flash-attn is a selectable accelerator, not a hard import (unlike reference `lbcb-sci/RiNALMo`, which imports `flash_attn` unconditionally). Config caveat: leave `attn_implementation` unset or pin `"sdpa"`/`"eager"`; passing `"flash_attention_2"` without flash-attn raises ImportError. **Validation gate: PASSED** (each kernel `wheel-present` at Option B; C1/C2 are honesty corrections within a passing gate). **Re-validation:** `tests/unit/test_check_sm86_wheels.py` (10 cases) locks the classifier + the mamba-torch2.7-wheel finding as a regression fixture; the live import/forward smoke remains P1 (cluster A4000). Verification: probe + Range-GET download-confirm + 3-agent adversarial pass + direct raw-source read of `mamba-ssm` v2.2.6.post3 (C2).

- **A4 — `ml` env split into `ml-dna` + `ml-rna`; the cu128 stack locks only via a full URL-pinned closure (P0-06b + P0-06c, 2026-07-08; user sign-off recorded).** This amendment folds together two P0-06b/P0-06c findings and **supersedes the uncommitted P0-06b A3 draft** (never merged) by incorporating its conda-lock finding here.

  **(1) The env split (D5's pre-registered contingency, now realized).** D5 (and the D2 table) recorded `multimolecule` 0.0.9 as "era-matched to transformers 4.57.5" — this premise is **disproven**. `multimolecule` 0.0.9 was uploaded **2026-02-02**, *after* transformers **5.0.0** (2026-01-26); at import it needs transformers 5.x (`multimolecule` → `danling` imports `transformers.initialization`, a 5.0.0 module absent in 4.57.5, and `RiNALMoModel` fails to build on 4.57.5). Stage-1 (Caduceus) still caps transformers at **4.57.5** (v5 dropped `torch.fx`/`is_torch_fx_available`, breaking `trust_remote_code`, issue #44561). So Stage-1 (DNA) and Stage-2 (RNA) can no longer share one `transformers` in a single `ml` env → **D5's pre-registered resolution (env split) fires**. `envs/ml.yml` is **deleted** and replaced by **`envs/ml-dna.yml`** (Stage-1 DNA: transformers 4.57.5 + Caduceus, **no** `multimolecule`) + **`envs/ml-rna.yml`** (Stage-2 RNA: transformers **5.13.0** newest + `multimolecule` 0.0.9 + RiNALMo). This is architecturally clean: the two-stage pipeline (ADR-0001) hands off via DVC artifacts, never a shared Python process, so two envs cost nothing at the seam. **D4 updated:** the CI image installs **both** `ml-dna.conda-lock.yml` + `ml-rna.conda-lock.yml` (not the retired single `ml.conda-lock.yml`); Snakemake GPU rules declare `ml-dna` or `ml-rna` per stage (CLAUDE.md §3.2). D8's never-bump discipline applies to **both** closures + the shared torch anchor.

  **ml-rna transformers-5 epoch pins (verified vs the conda-forge 5.13.0 recipe run-deps + PyPI `Requires-Dist` + the transformers v5.0.0 release notes; context7 §3.3 consulted).** transformers 5.13.0 pins `huggingface_hub<2.0,>=1.5.0` / `tokenizers<=0.23.0,>=0.22.0` / `safetensors>=0.8.0`; v5 mins are `accelerate>=1.1.0`, `peft>=0.18.0`, `python>=3.10`; hub 1.x is the v5 line (httpx backend + `hf_xet`, a major bump from ml-dna's 0.35.3). Resolved: `huggingface_hub` **1.22.0**, `safetensors` **0.8.0**, `tokenizers` **0.22.2** (0.23.0 is **absent** on conda-forge — it jumps 0.22.2 → 0.23.1 — and 0.23.1 exceeds the `<=0.23.0` cap, so 0.22.2 is the newest in-cap, same as ml-dna). **Minimize-divergence design:** ml-rna = ml-dna with **exactly three forced HF-core changes** (transformers 4.57.5→5.13.0, huggingface_hub 0.35.3→1.22.0, safetensors 0.7.0→0.8.0) plus `multimolecule` added — everything else is byte-identical, because `accelerate` 1.12.0 (`hub>=0.21.0`), `peft` 0.18.1 (`transformers>=4.36.0`, `hub>=0.25.0`) and `datasets` 4.5.0 (`hub>=0.25.0,<2.0`) already admit hub 1.x + transformers 5.x with no upper cap. `multimolecule` 0.0.9 pulls `chanfig`/`danling[torch]`/`torchmetrics`/`torchdata` from PyPI (danling's torch extra satisfied by the URL-pinned torch).

  **(2) The cu128 URL-closure (folding in the retired A3 draft).** Producing the lock on the laptop surfaced two hard **conda-lock 4.0.2** (the *latest* release) limitations: **(a)** it feeds every `pip:` entry to the PEP 508 requirement parser, so the D1-prescribed `- --extra-index-url https://download.pytorch.org/whl/cu128` line raises `InvalidRequirement` *before any solve*; and **(b)** its wheel-tag matcher's `MANYLINUX_TAGS` set omits `manylinux_2_27` and matches tag **strings exactly** (not by glibc version), so torch 2.7.1+cu128's transitive `nvidia-*` deps (`nvidia-cublas-cu12`, `nvidia-cudnn-cu12`, `nvidia-curand`, `nvidia-cusolver` … which ship **only** as `manylinux_2_27` on both PyPI and the pytorch index) match nothing → `Unable to find installation candidates for nvidia-cublas-cu12`. Neither `--find-links`, the cluster, nor a torch-only URL pin reaches (b) — the *tag* is the wall, not the *source*. **Fix (Option D, user-approved):** pin **torch and its entire cu128 pip closure by exact wheel URL** — torch + all **15** transitive deps (14 `nvidia-*` CUDA-runtime libs + `triton`, exact versions read from torch 2.7.1+cu128's own METADATA `Requires-Dist`) + the three A2 kernel wheels — in **both** `ml-dna.yml` and `ml-rna.yml`; a direct-URL (`name @ url`) pin is conda-lock-supported and skips both version-index lookup and tag matching, so the graph closes. All **16** URLs verified live (HTTP 206) 2026-07-08. The D1 "pin `torch` … against the CUDA-12.x wheel **index** (`--extra-index-url`)" wording is **superseded** by this URL-closure mechanism (a mechanism update within PRD §15 intent, as D1 already flagged for the dead `pytorch-cuda` metapackage). The closure is **frozen with the torch anchor under D8** — no piecemeal edits; a torch bump re-derives the whole closure from the new METADATA and re-verifies every URL.

  **Operational note — `CUDA_HOME` at runtime.** Both envs bundle `cuda-nvcc` 12.8 for DeepSpeed's JIT-built ZeRO ops (D2/D4). DeepSpeed probes `CUDA_HOME` **at import**, and on the RNA side `import multimolecule` transitively imports `deepspeed` (`multimolecule` → `danling` → `deepspeed`), so **any process using either env must set `CUDA_HOME=$CONDA_PREFIX`** (the env supplies the toolchain that satisfies the probe; without it deepspeed raises `MissingCUDAException` at import). Snakemake GPU rules + sbatch bodies export it.

  **Validation gate: PASSED.** Both `conda-lock lock` solve (ml-dna 52 s, ml-rna 72 s) + `conda-lock install` reproduced the env locally with **no GPU and no nvcc** (prebuilt wheels + URL closure). Lockfile pin audit clean for both: torch **2.7.1+cu128**, the three kernels, the 14 `nvidia-*` libs + `triton` all at their pinned URLs, **no `--extra-index-url` leak**. CPU import-smokes: **ml-dna** — torch 2.7.1+cu128 CPU matmul, transformers **4.57.5**, `CaduceusConfig` loaded via `AutoConfig(trust_remote_code=True)` (d_model 256, n_layer 16, bidirectional); **ml-rna** — transformers **5.13.0**, `transformers.initialization` importable (the 4.57.5 blocker), and **`import multimolecule` + `from multimolecule import RiNALMoModel` + instantiate — the exact P0-06b blocker, now cleared** (`_attn_implementation=eager`, SDPA-capable per A2 K1). The GPU kernel import/forward smoke (incl. `import selective_scan_cuda`, A2 C2) and the RiNALMo parity gate (D5) stay **P1** (cluster A4000). **Re-validation:** `tests/unit/test_env_manifest.py` extended — `EXPECTED_ENVS` → `data/infernal/ml-dna/ml-rna/viz/app`, both ml locks required, and the torch-direct-URL + no-`--extra-index-url` guard applies to **both** ml specs. Verification: artifact-based — conda-lock solve/install logs + lockfile pin audit + 16-URL live-asset probe + the two CPU smokes.

- **A5 — live `sm_86` kernel import/forward smoke gate + measured pure-PyTorch selective-scan fallback throughput cost (P1-01, 2026-07-12; smoke `src/tbox_finder/kernels/kernel_smoke.py`, SLURM job 462; user sign-off recorded).** The D3/D9-pre-registered fallback throughput number is now **measured on a real cluster RTX A4000** (Ampere `sm_86`, driver 590.48.01, torch 2.7.1+cu128). **Gate: import + forward green.** `import selective_scan_cuda` succeeds (the A2-C2 load-bearing assertion), as do `causal_conv1d_cuda`, `mamba-ssm` 2.2.6.post3, `causal-conv1d` 1.6.2.post1, `flash-attn` 2.8.3.post1. A forward on a seeded random-DNA-derived tensor (fp32; D=512, N=16, L=512, B=2) runs through both the CUDA kernel and the pure-PyTorch reference. **Parity (kernel ≈ reference):** `selective_scan_fn` vs `selective_scan_ref` max-abs-diff **1.53e-5**; `causal_conv1d_fn` vs `causal_conv1d_ref` **9.5e-7** — both ≪ the fp32 `atol` 1e-2 (no `atol` is ADR-pinned; it is an implementer choice and the raw diff is always recorded, §10.3). **Measured fallback throughput cost (the pre-registered deliverable):** the CUDA `selective_scan` kernel is **≈871×** faster than the hand-wired pure-PyTorch `selective_scan_ref` (12.46 M vs 14.3 K tokens/s) — the genome-scan cost of the C2 no-kernel path (the packaged `Mamba` block never reaches `selective_scan_ref`, so the fallback is `selective_scan_ref` invoked directly, per C2). `causal_conv1d` (the D3 *optional* accelerator) shows a **0.56×** ratio at this small size — its CUDA depthwise-conv kernel carries launch overhead the torch `F.conv1d` reference avoids at B=2/L=512; it is speed-relevant only at scan scale, and its correctness (parity 9.5e-7) is what the gate needs. **Quantitatively confirms C2:** a missing `selective_scan_cuda` is not a "modest speed cost" — an ≈871× slowdown makes a genome-scale scan on the pure-PyTorch fallback non-viable, so the prebuilt `sm_86` wheel is load-bearing (as A2 concluded). **Env-install finding (durable, not a pin change):** the cluster `tbox-ml-dna` env's first `conda-lock install` (detached on the login node, reaped before completing) left **`causal-conv1d` partially installed** — an orphan `causal_conv1d_cuda.so` with no package dir / `dist-info` (`pip show` → not found), which crashed the first smoke (job 460). Repaired by reinstalling the **exact pinned wheel** from `envs/ml-dna.yml`; no `.yml`/lock change. Carry forward: verify a *complete* install (`pip show` + import) after any cluster env creation, and prefer a robust/foreground install over a reap-prone detached one. **Validation gate: PASSED** (`reports/p1/kernel_smoke.json`, `gate.overall_pass=true`). **Re-validation:** `tests/unit/test_kernel_smoke_report.py` (14 cases; the real-report tier asserts `overall_pass` + `is_sm86` + the `selective_scan_cuda` import); the smoke was hardened to record kernel-import failures in-report rather than crash. **Verification:** artifact-based (SLURM job 462, `sacct` off) — `squeue` empty + `reports/p1/kernel_smoke.DONE` present + zero-byte `.err`; git_sha `8ba3880`, timestamp 2026-07-12T10:54:26Z.

- **A6 — binding per-nucleotide segmentation smoke go/no-go PASSED (GO); Stage-1 Caduceus prokaryotic transfer confirmed for P2 (P1-07, 2026-07-12; harness `src/tbox_finder/train/stage1_smoke.py`, SLURM job 464, git_sha `1213883`; user sign-off recorded).** The **D7 two-part transfer go/no-go is now complete and both parts are positive**, so Stage-1 cross-domain transfer (human-reference-pretrained Caduceus-PS → prokaryotic discovery) is **confirmed** and **no backbone fallback is triggered**. **Part (i) — advisory pre-filter (P1-05):** frozen-embedding binary linear separability PASSED (AUROC 0.992, non-binding). **Part (ii) — BINDING per-nucleotide segmentation smoke (this step):** a seeded (seed 42), deterministic full fine-tune of the P1-04 `Stage1Segmenter` (Caduceus-PS backbone → `concat` RC-combine → 8-class seg head) on the **P1-06 held-out prokaryotic smoke set** (300 leave-clade-out loci, 93,764 valid positions), per-nucleotide **focal cross-entropy** (γ=2, PRD §11), 20 epochs, on a cluster **RTX A4000 (Ampere `sm_86`)**. **Measured: min per-nt F1 over the three core elements {Stem_I, Specifier, Antiterminator} = 0.99990** (Stem_I the min; Specifier = Antiterminator = 1.000; focal loss → 0.0010) vs the **background-only baseline 0.0 → GO**. **Go/no-go operationalization (the D7 "set-with-the-P1-fixture" threshold, finalized here — D7 pins no blinded-frozen number).** Directional rule: **GO iff min-core-F1 ≥ 0.30** (clearly above the 0.0 background-only baseline, and well below the 0.80 GATE-4 floor which P2 owns on the full split); **NO-GO iff min-core-F1 ≤ 0.10** (at/near background); the band **(0.10, 0.30) → CLAUDE.md §7 stop-and-ask**. The observed 0.99990 is decisively GO — **non-marginal** (any threshold in [0.1, 0.8] yields the same verdict). These are the directional go/no-go values, **NOT** the GATE-4 gate (the 0.80 floor stays frozen in `power.py`/`metrics.py`, unweakened). **Interpretation (honest scope, §10.3).** The smoke fine-tunes **and** grades on the same 300 held-out loci — a **learnability / expressivity** transfer probe answering D7's "can the backbone learn prokaryotic T-box boundaries" (disclosed in the report as `data.eval_split="fine_tune_set"`, validator-enforced). The ≈1.0 fit shows the human-reference-pretrained Caduceus-PS embeddings carry the positional structure for per-nt prokaryotic T-box segmentation (a non-transferring backbone could not fit the rare 3-nt Specifier codon across 300 diverse held-out loci). It is **not** a generalization measurement — that is graded at **P2 GATE-4** on the full homology-clustered split; the loci are held out from the P2 training corpus (§9.2), so this GO does not contaminate P2. **Reproducibility (feeds P1-08).** Deterministic (seed 42, TF32/cudnn off, `PYTHONHASHSEED=0`); the committed `conf/train/stage1_smoke.yaml` + git_sha `1213883` + env-lock hash pin the run; **P1-08** re-runs it and confirms the go/no-go metrics reproduce within the pinned tolerance. **Consequence:** D7's binding go/no-go PASSES → **Stage-1 Caduceus is confirmed for P2 full fine-tuning**; the fallback ladder (full-FT default → GTDB continued-pretraining **P1-11** → NT-multispecies P1-10) is **not triggered**. **Re-validation:** `tests/ml/test_transfer_gonogo.py` (30 cases; the bare Tier-1 validator + go/no-go adjudication + the F1-over-3-core harness, the torch tier the focal loss + head path). **Verification:** artifact-based (SLURM job 464, `sacct` off) — `squeue` empty + `reports/p1/seg_smoke.DONE` present + a self-validated `reports/p1/seg_smoke_gonogo.json`; `.err` benign (wandb-offline notices only); provenance git_sha `1213883`, seed 42, inputs/outputs hashed.

- **A7 — P1-08 Stage-1 smoke reproducibility tolerance (pinned a priori; the "smoke run reproducible" P1 exit-gate metric) (P1-08, 2026-07-12/13; harness `src/tbox_finder/train/stage1_smoke.py` unchanged; gate module `src/tbox_finder/train/repro.py`; SLURM job 466; a-priori sign-off + P1-08 scope re-sign-off recorded).** PRD §18.1's P1 exit gate requires *"smoke run reproducible"* — a seeded re-run of the A6 binding go/no-go must reproduce its go/no-go metrics. The re-run is **not** bit-exact: the Caduceus Mamba `selective_scan_cuda` kernel (A2 C2) registers no deterministic algorithm, so `set_determinism` uses `torch.use_deterministic_algorithms(True, warn_only=True)` — every RNG seed pinned and TF32/cudnn autotune disabled (§8.3; PRD §11), but the fused scan may accumulate floating-point differently run-to-run. The tolerance is therefore **metric-level, not bitwise**, and is pinned **before** the re-run so the gate is pre-registered, not fit to the observed diff (§8.3/§10.3).

  **Gate (two-level), over the committed A6 report (`reports/p1/seg_smoke_gonogo.json`) vs the P1-08 re-run report (`reports/p1/seg_smoke_repro.json`), same seed 42 / `PYTHONHASHSEED=0` / committed `conf/train/stage1_smoke.yaml` (only the output paths overridden via Hydra `report_path=`/`checkpoint_dir=`) / same pinned backbone revision:**
  1. **Primary (load-bearing — the exit-gate meaning of "reproducible"):** the **go/no-go verdict reproduces** (re-run = `GO`, identical to A6). The min-core-F1 clears the 0.30 GO threshold by ~0.70, so the P2-gating transfer decision is robust to any drift short of a bug-level collapse.
  2. **Secondary (determinism health-check), τ = 1e-3 (0.001 absolute F1):** `max|Δ| ≤ τ` over the **go/no-go-decision metrics** — `min_core_f1` (the min over the 3 core elements the verdict rests on, ADR-0004 D6), the three core `per_element_f1` {Stem_I, Specifier, Antiterminator_Tbox_seq}, and the `macro_f1`/`micro_f1` aggregates (6 scalars). The remaining eight `per_class_f1` are reported as a **determinism diagnostic** (their max|Δ| is surfaced), **not** τ-gated. **All 14 reported metrics must still be present + finite** in both reports (a structural fail-closed floor: a truncation, a symmetric reduction, or an extra/renamed key voids the gate); only the numeric τ comparison is scoped to the decision metrics.

  **Scope decision (P1-08 re-sign-off, 2026-07-13).** τ = 1e-3 was pinned **a priori over all 14 metrics**. The measured re-run reproduced the verdict (GO) and every go/no-go-decision metric within τ (**gated max|Δ| = 2.90e-4**; `min_core_f1` Δ = 7.6e-5), but **one rare non-core per-class metric — `Stem_III` — drifted 1.67e-3 > τ**. Per the pre-registered §7 stop-and-ask (the tolerance is *not* weakenable), the gate was **not loosened**; instead the secondary τ was **scoped to the decision metrics** and the per-class table reported as a diagnostic. Justification: the go/no-go decision is *defined* on the 3 core elements + verdict (ADR-0004 D6), not the 8-class table; the rarer non-core classes span far fewer positions, so a single argmax flip under the irreducibly-nondeterministic Mamba kernel (§8.3, no deterministic variant) moves their F1 more than the core elements' (~1e-4 → ~1.7e-3). The decision-relevant τ stays **unchanged and tight (1e-3)**; the rare-class drift is **disclosed, not hidden**. *(Considered + rejected: uniformly widening τ to ~2e-3 — a looser claim on the decision metrics that they don't need; and holding all-14 at 1e-3 and failing the gate — which would misrepresent a successful decision-reproduction, there being no nondeterministic op to chase since the kernel is irreducibly non-deterministic.)*

  **Result (measured; SLURM job 466, cluster RTX A4000 sm_86, seed 42, git_sha `d22a9fb`).** **REPRODUCIBLE** — verdict **GO→GO**; **gated max|Δ| = 2.90e-4 ≤ τ** (`macro_f1` the max; `min_core_f1` 0.999896→0.999820, Δ 7.6e-5; all 3 core `per_element_f1` Δ ≤ 1.8e-4); **diagnostic max|Δ| = 1.67e-3** on `per_class_f1.Stem_III` (Stem_II 5.7e-4; the rest ≤ 1.8e-4) — reported, not gated. The re-run is within-tolerance, **not bit-exact** (the A2 C2 Mamba kernel), confirming the metric-level tolerance is the correct instrument. Satisfies the PRD §18.1 "smoke run reproducible" P1 exit condition.

  **Magnitude rationale (τ = 1e-3, decision metrics).** Per-nt F1 from **argmax** on a near-perfect fit (A6: all F1 ≈ 1.0); a decision metric moves only if a tiny logit perturbation flips an argmax at a near-tied boundary position. The core elements span O(10⁴) positions, so one flip moves their F1 ~1/(2·TP) ≈ O(1e-4) and the min-core/macro/micro aggregates even less; τ = 1e-3 absorbs several such flips with margin while staying ~1–2 orders below both a verdict-flip (needs a ≥0.7 F1 drop) and a determinism *regression* (removing the seed or re-enabling TF32 moves metrics ≥1e-2 or flips the verdict). **τ is not weakenable on a fail:** an observed decision-metric diff > τ is a §7 stop-and-ask (chase the nondeterministic op, or list it unit-test-only per §8.3), never a loosened tolerance (§8.5/§10.3). The gate also **fails closed** on a config/backbone mismatch or a metric-set drift.

  **Re-validation:** `tests/ml/test_smoke_reproducible.py` (21 cases) locks the gate — Tier 1 (pure stdlib, runs + blocks in bare CI) bite-tests every guard (verdict compare, per-metric abs-diff over the key union, config/backbone match incl. an absent/explicitly-null revision, a non-weakenable tolerance override, the τ predicate at the inclusive boundary, a **diagnostic-only drift > τ that does NOT fail the gate**, and the gated/diagnostic partition + pinned-constant drift guards); Tier 2 (committed-report, pure JSON) is the exit-gate assertion over the two reports, fail-closed under `TBOX_REQUIRE_REPRO_GATE=1` (set in CI). **Verification:** artifact-based (`sacct` off, §9.3) — `slurm/p1/seg_smoke_repro.sbatch` (mirroring the A6 hardening: flock singleton, stale-artifact clearing incl. checkpoint/provenance, DONE gate) → `squeue` empty + `reports/p1/seg_smoke_repro.DONE` + a self-validated repro report. On a stack/seed/config change, re-run the smoke and re-check this gate (§8.5).

- **A8 — `multimolecule` 0.0.9 → 0.1.0 in `ml-rna` (the RiNALMo mirror *forward* is broken under the pinned transformers 5.13.0; minimal pin fix) (P1-13, 2026-07-13; `envs/ml-rna.yml` + `envs/ml-rna.conda-lock.yml`; user sign-off recorded).** A4 cleared only `import multimolecule` + `RiNALMoModel` **instantiate**, and explicitly deferred the RiNALMo **forward** / D5 parity gate to P1. The P1-13 forward gate finds `multimolecule` 0.0.9's `RiNALMoModel.forward` **raises** under the pinned transformers **5.13.0**.

  **Root cause (sourced + reproduced + shim-isolated).** `multimolecule` 0.0.9's `modeling_rinalmo.py::_create_attention_masks` calls transformers' `create_bidirectional_mask(input_embeds=embedding_output, …)`. transformers renamed that parameter `input_embeds` → `inputs_embeds` in **5.2.0** but kept a `@deprecate_kwarg("input_embeds"→"inputs_embeds")` compat alias through **5.8.1** (last-working); the alias was **removed + `**kwargs` added in 5.9.0**, so from 5.9.0 on, `input_embeds=` silently lands in `**kwargs` and the required positional `inputs_embeds` is missing → `TypeError: create_bidirectional_mask() missing 1 required positional argument: 'inputs_embeds'`. **5.13.0 behaves like 5.9.0.** Verified by reading `masking_utils.py` at the transformers v5.0.0→v5.13.0 tags + `modeling_rinalmo.py` at the `multimolecule` v0.0.9/v0.1.0 tags, reproduced on CPU under the pinned env, and confirmed the **sole** incompatibility by a one-line `input_embeds→inputs_embeds` shim that makes the full backbone + shipped SS-head forward run green. This inverts D5's original premise (`multimolecule` 0.0.9 "works on transformers 5.x") and its "**avoid 0.1.0/0.2.0** (may assume v5 internals)" note — 0.1.0 "assuming v5 internals" (it passes `inputs_embeds=`, the transformers-v5 masking API) is exactly what 5.13.0 requires.

  **Resolution (Option A, user-chosen).** Bump `multimolecule` **0.0.9 → 0.1.0** (the first release passing `inputs_embeds=`; also 0.2.0 works). **transformers stays 5.13.0** (0.1.0 leaves `transformers` unpinned; `RiNALMoConfig`/`RiNALMoModel`/`modeling_rinalmo.py` class + config schema are unchanged 0.0.9→0.2.0, so the `multimolecule/rinalmo-giga` hub weights + config still load at the pinned revision `2a71f6f9…`). Rejected: downgrade transformers → 5.8.1 (rides a *deprecated* alias slated for removal, and moves the transformers anchor that also caps hub/tokenizers/safetensors) and a permanent runtime shim (fragile/opaque, papers over the incompatibility).

  **Lockfile mechanics (a conda-lock 4.0.2 limitation, A4-class).** 0.1.0 adds two pip deps vs 0.0.9 — **`scipy`** + a **`typer`/`rich` CLI subtree** (`typer`, `rich`, `pygments`, `markdown-it-py`, `mdurl`, `shellingham`, `annotated-doc`; `torchmetrics` was already pulled via `danling[torch]`). conda-lock 4.0.2 (the latest release) **cannot** re-solve the expanded pip graph — its vendored-poetry pip solver `AssertionError`-crashes during backtracking (both a full `-f` re-solve and `--update multimolecule`), though **pip resolves it cleanly** (the env installs and the forward is green). The lock was therefore regenerated by **merging pip's proven resolution** (exact wheel URL + `sha256` for `multimolecule` 0.1.0 + the eight new pip packages, from `pip install --dry-run --report`) into the valid 0.0.9 lock **via conda-lock's own `Lockfile` model** (`parse_conda_lock_file` → update/append `LockedDependency` → `write_conda_lock_file`, canonical format), with the `metadata.content_hash.linux-64` **recomputed from the edited spec** via `conda_lock.content_hash.compute_content_hashes` — a method **validated to reproduce the prior 0.0.9 hash `b1804d9d…` exactly** before trusting the new `aaaab98c…`. Every load-bearing pin is unchanged (torch 2.7.1+cu128 + the three `sm_86` kernels + transformers 5.13.0 + numpy 2.5.1, all still URL-/version-pinned; no `--extra-index-url`).

  **Validation gate: PASSED (measured, CPU, no shim).** Under `multimolecule` **0.1.0** + transformers **5.13.0** + torch **2.7.1+cu128**, `RiNALMoModel.from_pretrained(multimolecule/rinalmo-giga, revision=2a71f6f9…)` forwards natively: `last_hidden_state` = `(1, L+2, 1280)` → CLS/EOS-stripped `(1, L, 1280)`; the shipped `RiNALMoForSecondaryStructurePrediction` emits `(1, L, L, 1)` base-pair logits; config unchanged (hidden 1280 / 33 layers / 20 heads / rotary). **Re-validation:** `tests/unit/test_env_manifest.py` gains a fail-closed guard that `ml-rna` pins `multimolecule==0.1.0` (never the broken 0.0.9) in the spec **and** the lock. The **cluster fresh `conda-lock install`** of the re-locked env (incl. `import selective_scan_cuda`, A2 C2) and the **D5 RiNALMo parity gate** run under P1-13 on a cluster A4000 (the forward here is CPU-only, the A4 precedent). **Consequence: unblocks P1-13** (the parity harness can now fine-tune + score the mirror).

- **A9 — RiNALMo mirror D5-parity: the ±2 pp refit-based gate FAILED, but the mirror backbone is proven FAITHFUL by direct evidence → commit Stage-2 to `multimolecule/rinalmo-giga` (P1-13, 2026-07-15; SLURM job 492 + laptop diagnostics; `reports/p1/rinalmo_parity.json` + `reports/p1/rinalmo_parity_diagnostics.json` + `scripts/rinalmo_weight_diff.py` + `scripts/rinalmo_ft_inference.py`; user sign-off recorded).** The D5 parity gate is operationalized (this ADR + the P1-12 published-target JSON) as: the mirror, fine-tuned per-fold with a **fresh SS head** under RiNALMo's own sourced protocol, must reproduce the published ArchiveII nine-family LOFO F1 within **±2 pp** on the mean + the eight stable families. **This refit-based operationalization FAILS:** nine-family mean **0.6960 vs published 0.719 (Δ 2.30 pp)**; 6/8 stable families > ±2 pp (5S 4.16, tRNA 4.24, 23S 3.28, 16S 3.10, RNaseP 2.47, grp1 2.39; within: SRP 1.76, tmRNA 1.99); telomerase (carved out) 0.095 vs 0.120. The gap is **systematic, unidirectional (7/8 lower), precision ≫ recall** — a conservative-fit signature, not corrupted weights.

  **Per the pre-registered §7 stop-and-ask the ±2 pp gate was NOT loosened; the FAIL was investigated to root cause (three lines of evidence):**
  1. **Protocol/head/config faithful.** The training loop (Adam lr 1e-4, 15 epochs, `LinearLR` 1→0.1 over 7000 then floored, batch 1, fp16, 12/33 top-down gradual unfreeze at lr/10, BCE-upper-triangle, greedy pseudoknot decode, valid-tuned threshold, **last-epoch / no early-stopping**), the SS head (the `multimolecule` port head is a layer-by-layer exact reproduction of native `SecStructPredictionHead`: outer-concat → `Linear(2560→64)` → 2× bottleneck ResNet2D → `Conv2d(64→1,k=3,bias)` → triu-symmetrise), and the encoder config all match `lbcb-sci/RiNALMo @2c2c5c14` + paper Methods (Penić et al. 2025). The **only** non-identity is FlashAttention (native) → SDPA (port) — an *exact-softmax* kernel swap, not an approximation.
  2. **Test 1 — encoder weights BIT-IDENTICAL to official.** Converting the official Zenodo 15043668 `rinalmo_giga_pretrained.pt` via `multimolecule` 0.1.0's `_convert_checkpoint` (a **pure rename + `Wqkv.chunk(3)` split, no value transform on any encoder tensor**) and diffing every encoder tensor against the mirror @`2a71f6f9`: **497/497 tensors identical, global max-abs-diff 0.0**. The mirror **is** the official RiNALMo encoder, byte-for-byte.
  3. **Test 2 — released fine-tuned weights reproduce published through our pipeline (subset).** RiNALMo's **released** per-family `rinalmo_giga_ss_archiveII-<fam>_ft.pt` checkpoints (fine-tuned encoder + trained SS head), overlaid on the mirror and run **inference-only** through our exact forward+decode+metric+splits, reproduce the published F1 to **~0.1 pp** at RiNALMo's native threshold: **23S 0.847 vs 0.848, 5S 0.884 vs 0.884, tRNA 0.931 vs 0.931** (vs our fresh-head refit −3.3/−4.2/−4.2 pp). So our pipeline (and the `multimolecule` forward — no TokenDropout/RoPE degradation) is faithful.

  **Conclusion + decision.** With protocol+head+config faithful, the encoder bit-identical to official, and the released weights reproducing published to ~0.1 pp, the ~2–4 pp refit gap is **fresh-head refit variance** (single-seed reproduce-by-refit vs RiNALMo's released, optimized per-family checkpoints), **not** a backbone/forward/decode/metric/split infidelity. The D5 gate's **intent** — *mirror == published RiNALMo ⇒ the shipped Stage-2 backbone is trustworthy* — is therefore **confirmed by direct evidence**. **Decision: commit Stage-2 to `multimolecule/rinalmo-giga` at the pinned revision `2a71f6f98fb41dd2e6542a5e131d3778111d1468`.** The D5 **Zenodo-weights fallback is moot** (the mirror already carries those exact weights bit-for-bit); the D6 **RNA-FM fallback is not triggered** (reserved for §10.2 VRAM/latency). **§10.3 honesty:** this is **not** a threshold loosen — the ±2 pp refit proxy stands as *failed*, reported not hidden; the commit rests on the **stronger direct evidence**, fully disclosed. **Provenance:** Zenodo 15043668 (CC-BY-4.0), `rinalmo_giga_pretrained.pt` md5 `ae92ea0…` + the three subset `*_ss_archiveII-*_ft.pt` md5s (`data/external/rinalmo_official/provenance.json`); the `.pt` weights are re-fetchable, not committed (`*.pt` ignored). **Satisfies the PRD §18.1 P1 exit-gate item "mirror parity confirmed."** (A completeness note for reviewers: Test 2 covered the 3 biggest-gap families of 9; the mechanism — bit-identical backbone + released-weight reproduction — is family-independent, so the subset is decisive; the remaining 6 families' released checkpoints were not downloaded.)

- **A10 — the FlashAttention-2 *forward* through RiNALMo is now VERIFIED on `sm_86` (P1-16, 2026-07-15; SLURM job 521; user sign-off recorded). NON-BINDING NOTE — no pin changes; recorded so a reader hitting A2 K1's caveat cold is not left guessing.** A5 confirmed only that the `flash-attn` 2.8.3.post1 **wheel imports** on a real cluster RTX A4000 — its two forward-parity tests are the *Stage-1* `selective_scan` / `causal_conv1d` kernels, and **no FA-2 forward through RiNALMo appeared anywhere in this ADR**. P1-15 therefore selected `flash_attention_2` on that import plus the pinned RiNALMo classes' `_supports_flash_attn` advertisement, and honestly recorded `forward_verified_on_sm86: false` in `reports/p1/attention_backend.json`, deferring the forward to P1-16.

  **P1-16 ran it.** On the cluster A4000 (`sm_86`, capability `[8,6]`, 15.602 GiB, driver 590.48.01), `RiNALMoModel.from_pretrained(..., attn_implementation="flash_attention_2")` **loaded and forwarded** through the parity-confirmed mirror @`2a71f6f9` under bf16 + LoRA + gradient checkpointing across a full fine-tune step (forward → loss → backward → `AdamW.step`) at batch 1–16: `reports/p1/lora_vram_smoke.json` records `attention.used == attention.selected == "flash_attention_2"`, `forward_verified_on_sm86: true`, `forward_error: null`.

  **Consequences (all confirmatory — nothing is re-decided):** (i) **D3's FA2-primary / SDPA-fallback architecture stands, and its primary path is now exercised rather than inferred**; (ii) **A2 K1's caveat is unchanged and remains correct** — *"passing `flash_attention_2` without flash-attn raises ImportError"* is **conditional**, and the condition is unmet here (the pinned wheel is present and works), which is exactly what K1's thesis of flash-attn as *"a selectable accelerator, not a hard import"* predicts; (iii) **A9 is unaffected** — its note that the mirror's only non-identity vs official RiNALMo is FlashAttn→SDPA concerns the *parity refit*, which ran under SDPA, whereas Stage-2 (P3) will run under FA-2 as selected; (iv) the SDPA fallback remains **live wiring, not decoration** — `select_attention_backend` still returns SDPA the moment any measured input goes false, and P1-16's harness fails its gate closed if the selected backend's forward does not run.

  **Scope, stated (§10.3):** what is verified is a **forward + backward through RiNALMo-giga under FA-2 on `sm_86`**, on **synthetic RNA with a placeholder loss** — a mechanics verification, **not** a numerics-parity claim (no FA-2-vs-SDPA output-equivalence test was run; A9's exact-softmax reasoning covers *why* the swap is benign, and P1-13's parity evidence stands on SDPA). Nothing in this note changes a pinned decision, so it triggers no re-validation of any prior step (D8).

## Sign-off

- **User sign-off:** ☑ recorded 2026-07-08 (bioedca), CLAUDE.md §7 item 2 — Option B anchor adopted as-is (torch 2.7.1+cu128, `mamba-ssm` 2.2.6.post3 source-built; Option A all-prebuilt is the pre-registered P0-06 fallback).
- **Amendment A1 sign-off:** ☑ recorded 2026-07-08 (bioedca), CLAUDE.md §7 item 2 — `data` env `pandas` 3.0.3 → 2.3.3 (pure-conda downgrade; bioconda `snakemake-executor-plugin-slurm` 2.7.1 over-caps pandas<3.0).
- **Amendment A2 sign-off:** ☑ recorded 2026-07-08 (bioedca), CLAUDE.md §7 item 2 — sm_86 probe result + D3 corrections **C1** (`mamba-ssm` 2.2.6.post3 is wheel-present at torch2.7 → Option B needs zero source builds; `ml.yml` pins the three kernels by release-asset URL; P0-05 ml-lock deferral reversed) / **C2** (the `Mamba(use_fast_path=False)` selective-scan fallback re-characterized — the packaged module hard-requires `selective_scan_cuda`, so a missing kernel is a capability loss; only `causal_conv1d` is speed-only) + confirmation **K1** (RiNALMo SDPA/eager path confirmed via `multimolecule` 0.0.9).
- **Amendment A4 sign-off:** ☑ recorded 2026-07-08 (bioedca), CLAUDE.md §7 item 2 — `ml` env split into `ml-dna` (transformers 4.57.5 + Caduceus) + `ml-rna` (transformers 5.13.0 + `multimolecule` 0.0.9 + RiNALMo), realizing D5's pre-registered contingency after `multimolecule` 0.0.9 proved to require transformers 5.x (disproving the "era-matched to 4.57.5" premise); folds in the P0-06b cu128 URL-closure finding (Option D — conda-lock 4.0.2 cannot lock the cu128 stack via an index) and **supersedes the never-committed A3 draft**. (A3 was never merged, so there is no standalone A3 sign-off.)
- **Amendment A5 sign-off:** ☑ recorded 2026-07-12 (bioedca), CLAUDE.md §7 item 2 — live `sm_86` kernel import/forward smoke PASSED on a cluster A4000 (SLURM job 462); the measured pure-PyTorch `selective_scan` fallback is **≈871× slower** than the CUDA kernel (parity 1.53e-5), quantitatively confirming C2 that the prebuilt kernel is load-bearing; the cluster `tbox-ml-dna` env's `causal-conv1d` partial-install was repaired via the exact pinned wheel (no `.yml`/lock change).
- **Amendment A6 sign-off:** ☑ recorded 2026-07-12 (bioedca), CLAUDE.md §7 item 2 — the **binding** per-nucleotide segmentation smoke go/no-go (D7 part ii) **PASSED (GO)** on a cluster A4000 (SLURM job 464): min per-nt F1 over the 3 core elements **0.99990** vs the 0.0 background-only baseline; the directional go/no-go thresholds (GO ≥ 0.30 / NO-GO ≤ 0.10 / borderline → §7) are finalized (non-marginal for the observed value); Stage-1 Caduceus prokaryotic transfer is **confirmed for P2** (no fallback triggered), and the smoke is disclosed as a learnability/expressivity probe (eval-on-fine-tune-set; generalization graded at P2 GATE-4).
- **Amendment A7 sign-off:** ☑ recorded 2026-07-12 (bioedca, a-priori pin) + **scope re-sign-off 2026-07-13** (bioedca), CLAUDE.md §7 item 2 — the P1-08 "smoke run reproducible" exit-gate is a two-level gate: **primary** = the go/no-go verdict reproduces (GO); **secondary** = max abs diff **≤ τ = 1e-3** over the **go/no-go-decision metrics** {min_core_f1, the 3 core per_element_f1, macro/micro}. τ was pinned a priori over all 14 metrics; the measured re-run (job 466) reproduced the verdict + all decision metrics within τ (gated max|Δ| 2.90e-4) but a **rare non-core class (Stem_III) drifted 1.67e-3 > τ**, so per the pre-registered §7 stop-and-ask the τ was **scoped to the decision metrics** (τ unchanged, NOT loosened) with the per-class table reported as a determinism diagnostic. The re-run is within-tolerance, not bit-exact (Mamba kernel A2 C2); the gate fails closed on a config/backbone or metric-set mismatch. **REPRODUCIBLE → the PRD §18.1 "smoke run reproducible" P1 exit condition is met.**
- **Amendment A8 sign-off:** ☑ recorded 2026-07-13 (bioedca), CLAUDE.md §7 item 2 — `multimolecule` **0.0.9 → 0.1.0** in `ml-rna` (Option A of the P1-13 forward-gate stop-and-ask). A4 cleared only import+instantiate; the P1-13 forward gate finds 0.0.9's `RiNALMoModel.forward` raises `TypeError` under the pinned transformers 5.13.0 (it passes `create_bidirectional_mask(input_embeds=…)`, whose compat alias transformers removed in 5.9.0; 0.1.0 is the first release passing `inputs_embeds=`). transformers stays 5.13.0 (0.1.0 unpins it; rinalmo-giga config/classes unchanged → weights load); this **inverts D5's "avoid 0.1.0" note** (written under the disproven "0.0.9 works on v5" premise). The lock was hand-merged from pip's proven resolution via conda-lock's `Lockfile` model with a **validated recomputed `content_hash`** (conda-lock 4.0.2's poetry pip-solver crashes on the 0.1.0 graph; pip resolves it fine). **Validation gate PASSED** (CPU, no shim): the RiNALMo backbone + shipped SS head forward green (`(1,L+2,1280)` / `(1,L,L,1)`). Unblocks P1-13; the cluster fresh-install + D5 parity gate remain P1-13.
- **Amendment A9 sign-off:** ☑ recorded 2026-07-15 (bioedca), CLAUDE.md §7 item 2 — the D5 RiNALMo mirror-parity **refit-based ±2 pp gate FAILED** (mirror fresh-head refit mean 0.6960 vs published 0.719; SLURM job 492), but the FAIL was root-caused (**NOT** loosened) to **fresh-head refit variance**: the training protocol + SS head + encoder config are a faithful reproduction of `lbcb-sci/RiNALMo @2c2c5c14`, **Test 1** proves the mirror encoder is **byte-identical** to the official Zenodo 15043668 pretrained backbone (497/497 tensors, max-abs-diff 0.0), and **Test 2** shows RiNALMo's released `*_ss_archiveII-*_ft.pt` reproduce the published F1 to **~0.1 pp** through our pipeline (23S 0.847/0.848, 5S 0.884/0.884, tRNA 0.931/0.931). The gate's **intent** (mirror == published RiNALMo ⇒ trustworthy shipped backbone) is **confirmed by direct evidence** ⇒ **commit Stage-2 to `multimolecule/rinalmo-giga` @`2a71f6f9…`**; no Zenodo-weights/RNA-FM fallback. §10.3: the refit proxy stands as failed (reported, not hidden); the commit rests on stronger direct evidence. Satisfies the PRD §18.1 "mirror parity confirmed" P1 exit item.
- **Amendment A10 + P1-exit hygiene sign-off:** ☑ recorded 2026-07-15 (bioedca), CLAUDE.md §7 item 2 — **three amendments approved together at the Phase-1 exit gate, bodies shown before approval**: **(a)** an **A6 step-number correction** (A6's prose labelled GTDB continued-pretraining "P1-09"; the canonical step is **P1-11** — P1-09 is the frozen-embedding seg-head fallback, per imp.md/TODO.md/PRD §10.1; flagged at P1-09 and P1-11, annotation-only, no decision changed); **(b)** a **D5 citation-precision correction** (the published parity F1 values were cited to "Fig. 4a/4b"; P1-12's independent SI extraction established they live in **Supp. Table S2, `non-weighted` column**, which Fig. 4a renders — the pinned **N = 2 pp** and every number unchanged; flagged at P1-12); **(c)** the **new non-binding A10** recording that the **FA-2 forward through RiNALMo is now VERIFIED on `sm_86`** (P1-16, job 521), which A5 never covered — confirmatory only, no pin changes, and explicitly **not** a numerics-parity claim (§10.3).
